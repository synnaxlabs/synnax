# Arc Function-Call Infrastructure: Implementation Plan

> **Companion to**
> [RFC 0039: Arc Function-Call Unification](0039-260513-arc-function-call-unification.md).
> This document is the action-oriented execution guide for an AI session implementing
> the RFC. The RFC carries design rationale; this plan tells you what to do, in what
> order, and how to verify each step. Where this plan and the RFC disagree, **the RFC
> wins**: update the plan to match.

## 0 - Quick Recap

Arc's type system today carries two parameter arrays on every function signature:
`Config` (read by the flow analyzer for the `{...}` brace block) and `Inputs` (read by
the expression analyzer for the `(...)` parens form). `ExecBoth` symbols mirror the same
list into both fields. The flow analyzer disambiguates wire-fed value from pure
activation via the heuristic `upstreamIsTrigger := Exec == ExecBoth && len(Config) > 0`,
which now lives in three sites with two divergent forms (the two flow.go sites key on
`len(Config) > 0`; `text/analyze.go` keys on `sym.AST == nil`). The `ExecBoth` symbols
today are `time.now`, `status.set`, and every user-defined function.

This refactor collapses both arrays into a single `Inputs` list. The `ExecBoth`
heuristic is replaced by an explicit `Trigger TriggerBinding` field on `Symbol` that
names which param (if any) receives the upstream wire's value in flow context. The same
change demotes `Exec` (`ExecWASM`/`ExecFlow`/`ExecBoth`) from a structural property to a
superficial gate: it no longer selects a param array or feeds the trigger decision, and
survives only as a thin permission flag (see
[RFC §4.1](0039-260513-arc-function-call-unification.md#41---explicit-trigger-binding)).
User-facing Arc syntax is unchanged.

See [RFC §3](0039-260513-arc-function-call-unification.md#3---the-problem) for why the
split became debt and
[RFC §4](0039-260513-arc-function-call-unification.md#4---proposed-design) for the
unified design.

## 1 - Decision Summary (settled with user)

| Question                          | Decision                                                                                                                                                             |
| --------------------------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| Scope                             | Full unification at the type level                                                                                                                                   |
| Unified field name                | Keep `Inputs` (not rename to `Params`)                                                                                                                               |
| Wire-fed-param tracking           | Symbol-level `Trigger TriggerBinding` field                                                                                                                          |
| Optional params                   | No `Optional` field added; existing default-value optionality preserved unchanged. Completing it deferred (RFC §8.1)                                                 |
| `Exec` modes                      | Demoted to a superficial gate. No longer drives composition; kept as a thin permission flag                                                                          |
| Surface syntax                    | Unchanged. `name(...)`, `name{...}`, and `wire -> name{...}` all keep working                                                                                        |
| Wire-format compatibility         | Breaking. Codec migration translates pre-vN persisted programs into the unified shape                                                                                |
| Trigger-as-argument future syntax | Out of scope. The structural foundation must not preclude any firing-semantics policy                                                                                |
| Staging                           | Nine stacked PRs, one per phase (Phase _N_ = PR _N_), each branched off the previous. Schema + codegen front-loaded to Phase 1; design-evaluation gate after Phase 3 |
| Codec version stamp               | `vN` placeholder. The exact target version is fixed once the major release this ships with is named                                                                  |

## 2 - Target Type Shape

Regenerated from `schemas/arc/types.oracle`:

```go
type FunctionProperties struct {
    Inputs  Params `json:"inputs"  msgpack:"inputs"`
    Outputs Params `json:"outputs" msgpack:"outputs"`
    // Config field removed. No replacement.
}

type Param struct {
    Name  string `json:"name"  msgpack:"name"`
    Type  Type   `json:"type"  msgpack:"type"`
    Value any    `json:"value" msgpack:"value"`
}
```

Param ordering is preserved as the user declared it; the migration appends the old
`Config` list before the old `Inputs` list to keep WASM ABI offsets stable.

## 3 - Target Symbol Shape

```go
type TriggerBinding struct {
    // Target names the param the upstream wire binds its value to. Empty (the
    // zero value, TriggerOnly) means the wire activates without binding a value.
    Target string
}

var TriggerOnly = TriggerBinding{}              // zero value: wire activates, binds no value
func TriggerInput(name string) TriggerBinding { // wire binds value to named param
    return TriggerBinding{Target: name}
}

type Symbol struct {
    // ... existing fields ...
    Trigger TriggerBinding
}
```

**Trigger.Target invariant:** if non-empty, must name an existing param on the same
symbol's Inputs list. Enforced by a unit test in
[arc/go/stl/stl_test.go](../../../arc/go/stl/stl_test.go) that walks every registered
symbol across the stdlib and service-layer resolvers and asserts the target resolves.
The failure mode (target points at a deleted/renamed param) surfaces at `go test` time.
Resolvers initialize as static literals; there is no runtime constructor to hook a
startup check into.

**Zero value is `TriggerOnly`.** `Trigger` describes only what an upstream wire does
with its value: activate (`TriggerOnly`) or bind to a named param (`TriggerInput`).
Whether a symbol may appear in flow context at all is the superficial `Exec` gate's
concern, not `Trigger`'s; keep the two axes separate. A flow symbol whose wire feeds a
param must declare `TriggerInput` explicitly; forgetting it drops the wire value (a
review-caught mistake). Synthetic f-string / inline-expression nodes keep the zero value
but are never consulted for it: they dispatch to `AnalyzeSingleExpression`
([arc/go/analyzer/flow/expression.go](../../../arc/go/analyzer/flow/expression.go)), a
path that never used the `upstreamIsTrigger` heuristic and does not read `Trigger`, and
the synthetic node carries no params. An upstream wire into an f-string (e.g.
`time.interval{1s} -> f"..." -> log`) is a pure activation edge, so these patterns are
unaffected. They also compile inline in func context, where `Trigger` does not apply.

## 4 - Target Trigger Assignments

The per-symbol Trigger assignments, including the stdlib / service-layer split and the
mixed-shape symbols that need concat-then-trigger, live in
[RFC §4.1](0039-260513-arc-function-call-unification.md#41---explicit-trigger-binding).
That table is the source of truth; this plan does not duplicate it.

ExecWASM-only symbols (`math.pow`, `series.*`, `string.*`, `state.*`, `error.panic`)
leave `Trigger` at the zero value; the runtime never consults `Trigger` for symbols that
can't appear in flow context, and the registration test accommodates an empty target.

User-defined functions are auto-assigned in `function.go`:
`TriggerInput(<first parens-block param>)` if a parens block is present, else
`TriggerOnly`. No per-symbol audit needed.

## 5 - Implementation Sequence

This ships as a stack of **nine PRs, one per phase**: Phase _N_ is delivered as PR _N_
on branch `-phase-N`, and the phases run in order, top to bottom. Schema sync and full
codegen land first, in **Phase 1**: isolating the generated diff in its own early PR
keeps every later PR a pure hand-written diff against the real, final generated shape.

Nine is a starting point, not a target. Branches are trivial to collapse together and
painful to split apart, so the breakdown errs granular: consolidate adjacent phases
later if a reviewer would rather see them together.

### Branch and PR structure

The root branch `sy-4297-arc-flowfunc-refactor` branches off `rc` and carries this RFC
and plan. Every phase branch stacks on the previous branch, never directly on `rc`:

```
sy-4297-arc-flowfunc-refactor-phase-1  ->  sy-4297-arc-flowfunc-refactor
sy-4297-arc-flowfunc-refactor-phase-2  ->  sy-4297-arc-flowfunc-refactor-phase-1
sy-4297-arc-flowfunc-refactor-phase-N  ->  sy-4297-arc-flowfunc-refactor-phase-(N-1)
```

Each phase PR targets its predecessor, so every review sees only that phase's diff. As
phases are approved they collapse down toward the root. **Only the root branch ever
targets `rc`**, and only once every phase has merged into it and the tree is green. `rc`
therefore never observes the broken intermediate state.

| Phase / PR | Branch     | Scope                                                           |
| ---------- | ---------- | --------------------------------------------------------------- |
| 1          | `-phase-1` | Schema sync + full codegen (Go/C++/TS bindings, `migrate_auto`) |
| 2          | `-phase-2` | Type helpers, `symbol`, hooks, `Trigger`                        |
| 3          | `-phase-3` | Analyzer collapse: `call.Analyze`, flow `Trigger` consult       |
| 4          | `-phase-4` | Downstream consumers                                            |
| 5          | `-phase-5` | Stdlib, LSP, service layer                                      |
| 6          | `-phase-6` | C++ runtime hand-written sites                                  |
| 7          | `-phase-7` | Test-fixture sweep                                              |
| 8          | `-phase-8` | Codec migration logic (`migrate.go`)                            |
| 9          | `-phase-9` | Build/test/lint, integration tests, Pluto reconciliation        |

**Don't expect intermediate PRs to build.** Phase 1's codegen removes `Config`, so the
tree stops compiling until the last `.Config` reader is converted near the end of the
stack; the first green build is Phase 9. Intermediate PRs are reviewed on their diff,
not on a green CI run. For the same reason the test-fixture sweep is consolidated into
Phase 7 rather than spread across earlier phases.

**Design-evaluation gate after Phase 3.** The analyzer collapse (Phase 3) is the core of
the design. The gate below is where the reviewer decides whether the approach holds
before the mechanical spread into stdlib, C++, tests, and migration. The service-layer
shrink (Phase 5) and the migration round-trip (Phase 8) are later confirmations, not
prerequisites for that decision.

### Phase 1: Schema sync and full codegen

Establishes the new generated shape the rest of the stack is written against. Edit the
schema, then regenerate every binding. Bumping the codec version also makes oracle
freeze the pre-refactor snapshot under the `migrations/vN/` directories (generated,
`Code generated by oracle. DO NOT EDIT.`, not hand-copied), which the Phase 8 migrator
references.

**Schema sync:**

- `schemas/arc/types.oracle`: remove `config` from `FunctionProperties`.
- `schemas/arc/ir.oracle`: remove `config` from `Function` and `Node`.

**Codegen:** run the project's code generators end-to-end. Regenerates:

- Go: `arc/go/types/{types.gen.go, codec.gen.go, pb/*.gen.go}`,
  `arc/go/ir/{types.gen.go, codec.gen.go, pb/*.gen.go}`, and the `migrate_auto.gen.go`
  files in the `migrations/vN/` directories.
- C++: `arc/cpp/types/{types.gen.h, json.gen.h, proto.gen.h}`,
  `arc/cpp/ir/{types.gen.h, json.gen.h, proto.gen.h}`.
- TS: `client/ts/src/arc/types/types.gen.ts`, `client/ts/src/arc/ir/types.gen.ts`.

Removing `Config` from the generated types breaks every `.Config` reader until Phases 2
to 8 convert them.

**Verification:** the regenerated `Param` carries `Name` / `Type` / `Value`, and no
`Config` field survives anywhere in `FunctionProperties`, `Function`, or `Node` across
all three languages.

### Phase 2: Type helpers, symbol, hooks

- `arc/go/types/type.go`: update `Equal` and `paramsEqual` to compare against the single
  Inputs list.
- `arc/go/types/fresh.go`: drop Config freshen.
- `arc/go/symbol/symbol.go`: replace `AnalyzeCall` + `AnalyzeFlowConfig` with
  `AnalyzeArguments`; add `Trigger TriggerBinding` field and the `TriggerBinding` /
  `TriggerOnly` / `TriggerInput(...)` types and helpers.
- `arc/go/symbol/hooks/hooks.go`: drop `CallHook` + `FlowConfigHook`; add
  `ArgumentsHook` and the `Argument` struct.
- `arc/go/symbol/scope.go`: `ResolveConfigChannel` reads `Inputs.Get(...)`.

### Phase 3: Analyzer collapse

The main payoff. See
[RFC §4.2](0039-260513-arc-function-call-unification.md#42---unified-call-analyzer) for
the `call.Analyze` signature and
[RFC §4.4](0039-260513-arc-function-call-unification.md#44---flow-analyzer-collapse) for
the upstream-handling collapse.

- `arc/go/analyzer/call/call.go` (new): `Argument` struct and `Analyze` function. **No
  `Form` parameter.** Whether each arg matches by name or index is derivable from
  `args[i].Name != ""`.
- `arc/go/analyzer/expression/expression.go`: replace `validateFunctionCall(...)` with
  `call.Analyze(...)`. Delete the body of `validateFunctionCall`.
- `arc/go/analyzer/flow/flow.go`: replace `validateFuncConfig(...)` with
  `call.Analyze(...)`. Delete the body. Replace the `upstreamIsTrigger` heuristic and
  the three-branch `prevIDNode` / `prevExpr` / `prevFuncNode` block with a direct
  `fn.Trigger` consult per RFC §4.4.
- `arc/go/analyzer/flow/expression.go`: same `call.Analyze` replacement at routing-table
  sites.
- `arc/go/analyzer/function/function.go`: collapse the brace-block and parens-block
  declaration walkers into one pass that appends both into a single `Inputs` slice. The
  parens-block first param (if any) becomes `Trigger.Target`. The "optional after
  required" rule applies independently within each grammar block. Collapse
  `addConfigToScope` and `addInputsToScope` into one scope-add pass over `Inputs` (the
  symbol `Kind` distinction stays via the block the param came from; track it locally).
- `arc/go/analyzer/resolve.go`: drop Config substitution loop.
- `arc/go/analyzer/expression/expression.go` and
  `arc/go/compiler/expression/compiler.go`: **leave** the
  `scope.Exec == symbol.ExecFlow` checks in place. Per
  [RFC §4.1](0039-260513-arc-function-call-unification.md#41---explicit-trigger-binding)
  these two are the superficial gate; remove only the `upstreamIsTrigger` heuristic, not
  these. `Exec` keeps no other structural role.

---

> ### 🛑 Design-Evaluation Gate (after Phase 3) 🛑
>
> Phases 1 to 3 are merged toward the root. Codegen already landed in Phase 1, so the
> generated shape is final; Phases 2 and 3 added the new type helpers and the analyzer
> collapse. This is the core of the design. Stop here and evaluate it before spreading
> the change into stdlib, C++, tests, and migration.
>
> The tree does NOT compile yet: the consumers in Phases 4 to 8 still read `.Config`.
> That is expected; review Phase 3 on its diff, not on a green build.
>
> **Inspection targets:**
>
> - Phase 3 analyzer collapse: is `call.Analyze` clean and complete? Does the flow
>   upstream-handling actually collapse around `fn.Trigger`?
> - Does the unified `Inputs` + `Trigger` shape (now real, from Phase 1 codegen) hold up
>   against the hardest stdlib symbols, or did the collapse paper over a case?
>
> **Do NOT proceed to Phase 4 (the mechanical spread) until Phase 3 is signed off.**

---

### Phase 4: Downstream consumers

- `arc/go/compiler/compiler.go`: `slices.Concat(i.Config, i.Inputs)` → `i.Inputs`.
- `arc/go/compiler/expression/compiler.go`: already reads `funcType.Inputs`; verify no
  Config reads remain.
- `arc/go/graph/analyze.go`: merge Config+Inputs bindParams; IR node construction emits
  a single Inputs list.
- `arc/go/text/analyze.go`: `Config:` field assignments → `Inputs:`; delete the
  `upstreamIsTrigger` special case.
- `arc/go/runtime/node/state.go`: iterate `Inputs` directly for edge alignment.
- `arc/go/ir/function.go` and `arc/go/ir/node.go`: drop the Config rendering branch from
  `Type()` and string output. IR rendering still distinguishes the brace vs parens
  blocks at print time, but the source-of-truth is the unified Inputs list plus the
  symbol's `Trigger`.

### Phase 5: Stdlib, LSP, service layer

The most visible win. Each stdlib symbol gains exactly one `Trigger:` line.

**Stdlib symbol declarations:**

- `arc/go/stl/{time,stable,channel,authority,math,constant,op,series,selector,strings,stateful,errors,wasm}/*.go`:
  collapse `Config:` and `Inputs:` field assignments into a single `Inputs:`. Add
  explicit `Trigger:` per symbol per the audit table in Section 4. For mirror-trick
  `ExecBoth` symbols (`time.now`, `status.set`), the duplicate list deduplicates to a
  single `Inputs:` list.
- Factory call sites that read `cfg.Node.Config.ValueMap()` or
  `cfg.Node.Config.Get(...)`: become `cfg.Node.Inputs.ValueMap()` and
  `cfg.Node.Inputs.Get(...)`.
- `arc/go/stl/wasm/wasm.go`: any `len(Config)` becomes `len(Inputs)` (or whatever the
  WASM ABI surface needs from the unified list).

**Stdlib test invariants (`arc/go/stl/stl_test.go`):**

- DELETE the existing "ExecBoth structural contract" mirroring test (the
  Inputs-mirrors-Config invariant is no longer expressible).
- ADD: `AnalyzeArguments` hooks are only attached to `KindFunction` symbols.
- ADD: `Trigger.Target`, if non-empty, names an existing param on the symbol's Inputs.

**LSP:**

- `arc/go/lsp/hover.go`: render the unified Inputs list. Show `Trigger.Target` in hover
  output where set.
- `arc/go/lsp/completion.go`: propose named-config keys from the same Inputs list.

**Service layer (the visible win):**

- `core/pkg/service/arc/status/status.go`:
  - Declare the `newSetSymbolType` param list once; assign only to `Inputs`.
  - Drop the dual hook registration; register a single `AnalyzeArguments` hook (see
    [RFC §4.3](0039-260513-arc-function-call-unification.md#43---unified-hook) for the
    collapsed body).
  - Set `Trigger: TriggerOnly`.
  - `cfg.Node.Config.ValueMap()` → `cfg.Node.Inputs.ValueMap()`.

### Phase 6: C++ runtime hand-written sites

- `arc/cpp/runtime/state/state.cpp` (alignment buffer iteration)
- `arc/cpp/runtime/wasm/factory.h`
- `arc/cpp/stl/stable/stable.h`
- `arc/cpp/stl/math/math.h`

Replace `cfg.node.config` reads with `cfg.node.inputs`. The generated headers these
depend on were already regenerated in Phase 1.

### Phase 7: Test fixture updates

Mechanical sweep across Go, C++, and TS test files. Every test that constructs a
`types.FunctionProperties`, `ir.Function`, or `ir.Node` literal with a `Config:` (or
`config:`) field folds it into `Inputs:` (or `inputs:`). Symbols that declared both
`Config:` and `Inputs:` deduplicate to a single list.

Files (non-exhaustive; grep for `Config:` and `config:` in test files under `arc/`,
`core/pkg/service/arc/`, and `client/ts/src/arc/`):

- `arc/go/{arc_test.go, types/type_test.go, types/fresh_test.go, ir/function_test.go, ir/msgpack_test.go, runtime/scheduler/scheduler_test.go, text/text_test.go, compiler/statement/statement_test.go}`
- `arc/go/analyzer/{polymorphic_test.go, resolve_test.go, constraints/constraints_test.go, function/function_test.go, flow/flow_test.go, flow/expression_test.go, flow/flow_upstream_trigger_test.go}`
- `arc/go/lsp/hover_test.go` and completion tests
- `core/pkg/service/arc/symbol/resolver_test.go`
- `core/pkg/service/arc/status/set_test.go` (drop dual Inputs/Config assertions)
- `arc/cpp/**/*_test.cpp`
- `client/ts/src/arc/**/*.spec.ts` (if any construct IR shapes directly)

New tests required by this change (added in the appropriate test files):

- `call.Analyze` shared validation: argument count, type, name resolution, unknown-name
  detection, duplicate-name detection, verified once against both parens and brace
  surface forms.
- Trigger consult: flow call to a `TriggerOnly` symbol does not type-check the upstream;
  flow call to a `TriggerInput("x")` symbol type-checks the upstream against the `x`
  param's type and rejects mismatches.
- Trigger invariant: the `stl_test.go` walker fails when a symbol declares
  `Trigger.Target = "missing_param"`. Add a negative-case fixture proving the walker
  catches it.
- `status.set` collapse: the `AnalyzeArguments` hook fires correctly for both
  `status.set(..., "errpr")` and `status.set{variant="errpr"}`, emitting one diagnostic
  per call site.

### Phase 8: Codec migration logic

Hand-write the merge logic that the auto-generated `migrate_auto.gen.go` (produced by
Phase 1 codegen) cannot infer.

**IR side (required hand-written work):**

- `arc/go/ir/migrations/vN/migrate.go`: provide `MigrateFunction` and `MigrateNode`
  overrides that wrap the auto-mapper and merge the old `Config + Inputs` lists into the
  unified `Inputs` list. Append old `Config` before old `Inputs` to preserve the WASM
  ABI ordering the compiler concat already produces today. For stdlib `ExecBoth` symbols
  where `Config == Inputs` (the mirror trick), detect the duplicate and dedupe to a
  single list.
- `arc/go/ir/migrations/vN/migrate_test.go`: round-trip pre-vN IR JSON / msgpack / proto
  through the migrator and assert the resulting IR Type-shape matches what a fresh
  declaration in the new shape produces.

**Types side:**

The types-side migration directory contains only the codec snapshot from Phase 1. No
hand-written `migrate.go` is needed because the auto-mapper can do everything the types
side needs (no Config-to-Inputs merge: types are referenced by IR, not stored
independently in persisted Arc programs).

**Note:** `migrate_auto.gen.go` already exists from Phase 1, so this `migrate.go` wraps
`AutoMigrateFunction` directly.

### Phase 9: Build, test, lint, integration, Pluto

Everything compiles end-to-end for the first time.

**Build, test, lint:**

```bash
cd arc/go && go build ./... && go test ./...
cd core && go build ./... && go test ./pkg/service/arc/...
bazel build //arc/cpp/... && bazel test //arc/cpp/...
cd client/ts && pnpm test
golangci-lint run ./... # required per house style
scripts/check_gofmt.sh  # required per house style; both must run
```

**Integration tests.** Full build sequence per
[docs/claude/testing.md](../../claude/testing.md):

```bash
pnpm build:console-vite
cp -r console/dist/* core/pkg/console/dist/
cd core && go build -tags=console -ldflags="-w -s" -o synnax . && cd ..
mkdir -p ~/synnax-data && cd ~/synnax-data && ./path/to/synnax start -mi &
cd integration && uv run tc arc
```

**Pluto reconciliation.** Grep `pluto/src/arc/` for any site directly constructing
`ir.Node` or `ir.Function` payloads with a `config:` literal. The surface is expected to
be narrow: most `config:` references in `pluto/src/arc/` are unrelated wrapper objects
(e.g. `{ arcKey }`). Update any hits to fold `config: {...}` into `inputs: [...]`. Smoke
test in console.

## 6 - Verification Checklist

After Phase 9, verify each:

- [ ] `cd arc/go && go build ./... && go test ./...`
- [ ] `cd core && go build ./... && go test ./pkg/service/arc/...`
- [ ] `bazel build //arc/cpp/... && bazel test //arc/cpp/...`
- [ ] `cd client/ts && pnpm test`
- [ ] `golangci-lint run ./...`
- [ ] `scripts/check_gofmt.sh`
- [ ] `cd integration && uv run tc arc`
- [ ] Smoke program (see RFC §11) parses, analyzes, compiles, runs identically:
  ```go
  time.interval{1s} -> status.set("my_status", "msg", "info") -> log_b
  func my_example {} () {
      log_a = status.set("Same_Name", "msg", "success")
  }
  ```
- [ ] LSP hover on `status.set` in the console editor renders the unified Inputs list.
- [ ] Migration round-trip: pre-vN persisted Arc program deserializes, migrates, and
      produces IR Type-shape identical to a fresh declaration.

## 7 - Risks and Open Questions

1. **Pluto direct IR construction.** Some tile components may construct `ir.Node` with
   `config:` literals. The Phase 9 grep finds them; the migration is mechanical.
2. **WASM ABI ordering.** WASM offsets in
   [arc/go/stl/wasm/wasm.go](../../../arc/go/stl/wasm/wasm.go) depend on the order the
   compiler concat already produces (`Config` first, then `Inputs`). The Phase 8
   migration appends in that same order, so offsets stay stable. The Phase 8 round-trip
   test verifies byte-equal IR for the same source.
3. **Hook ctx type-assertion.** Today's hooks switch on the concrete
   `acontext.Context[T]` type. The unified `AnalyzeArguments` keeps `ctx any` for this
   PR (smallest diff). A `DiagBearer` interface would be cleaner long-term but is out of
   scope.
4. **Codec version bump.** The exact `vN` value is fixed once the major release this
   refactor ships with is named. Search `git log` of `arc/go/ir/codec.gen.go` for past
   bump triggers if the convention is unclear.
5. **`paramsEqual` semantics.** Single-line `Equal`/`paramsEqual` update against the
   single Inputs list; easy to miss in code review. Flag explicitly when reviewing
   Phase 2.

## 8 - Critical Files to Modify First

The order below tracks the phase sequence. The generated shape that everything else
cascades from is established first, by the schema and codegen in Phase 1:

1. [schemas/arc/types.oracle](../../../schemas/arc/types.oracle) and
   [schemas/arc/ir.oracle](../../../schemas/arc/ir.oracle): remove `config`; the source
   of truth for codegen. Edited in Phase 1, after which codegen regenerates every
   binding.
2. [arc/go/symbol/symbol.go](../../../arc/go/symbol/symbol.go) and
   [arc/go/symbol/hooks/hooks.go](../../../arc/go/symbol/hooks/hooks.go): hook collapse
   and `TriggerBinding` shape (Phase 2).
3. [arc/go/analyzer/call/call.go](../../../arc/go/analyzer/call/call.go) (new): central
   call-validation routine. No `Form` parameter; matches `args` against `fnType.Inputs`
   end-to-end (Phase 3).
4. [arc/go/analyzer/flow/flow.go](../../../arc/go/analyzer/flow/flow.go): direct
   `fn.Trigger` consult replaces the `upstreamIsTrigger` heuristic; three-branch
   upstream-handling collapses around it (Phase 3).
5. [arc/go/analyzer/expression/expression.go](../../../arc/go/analyzer/expression/expression.go):
   delegate to `call.Analyze`; delete `validateFunctionCall` body (Phase 3).
6. [core/pkg/service/arc/status/status.go](../../../core/pkg/service/arc/status/status.go):
   the proof point. Code shrinks visibly; no mirror, single hook (Phase 5).
