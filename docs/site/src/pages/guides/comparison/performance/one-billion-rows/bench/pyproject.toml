[tool.poetry]
name = "bench"
version = "0.1.0"
description = ""
authors = ["Emiliano Bonilla <56323762+emilbon99@users.noreply.github.com>"]
packages = [{ include = "bench/**/*.py" }]

[tool.poetry.dependencies]
python = "^3.11"
pandas = "^2.2.3"
influxdb-client = "^1.46.0"
synnax = "^0.32.0"
docker = "^7.1.0"
psycopg2-binary = "^2.9.9"

[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"
