---
layout: "@/layouts/Reference.astro"
title: "Delete Data"
description: "Safe data deletion with idempotency guarantees and rollback considerations."
next: "Timestamps"
nextURL: "/reference/client/advanced/timestamps"
prev: "Writers"
prevURL: "/reference/client/advanced/writers"
---

import { Divider, Note } from "@synnaxlabs/pluto";
import { Fragment } from "react";
import { Client } from "@/components/client";
import { mdxOverrides } from "@/components/mdxOverrides";
export const components = mdxOverrides;

This guide covers deleting data from channels with safety guarantees and rollback considerations.

**Important**: Data deletion is permanent and cannot be undone. Use with caution in production environments.

<Divider.Divider x />

## Deleting Data

<Client.Tabs client:load exclude={["cpp"]}>
  <Fragment slot="console">
  </Fragment>

  <Fragment slot="python">
    <Note.Note variant="info">
      <span style={{ color: "green" }}>Present in corresponding page:</span>{" "}
        <a href="/reference/python-client/delete-data#deleting-data-from-a-channel">Python Delete Data - Deleting Data</a>
    </Note.Note>
  </Fragment>

  <Fragment slot="typescript">
    <Note.Note variant="info">
      <span style={{ color: "green" }}>Present in corresponding page:</span>{" "}
        <a href="/reference/typescript-client/delete-data#deleting-data-from-a-channel">TypeScript Delete Data - Deleting Data</a>
    </Note.Note>
  </Fragment>
</Client.Tabs>

<Divider.Divider x />

## Deleting from Multiple Channels

<Client.Tabs client:load exclude={["cpp"]}>
  <Fragment slot="console">
  </Fragment>

  <Fragment slot="python">
    <Note.Note variant="warning">
      <span style={{ color: "red" }}>No corresponding section - will be created from Deleting Data From a Channel examples</span>{" "}
    </Note.Note>
  </Fragment>

  <Fragment slot="typescript">
    <Note.Note variant="warning">
      <span style={{ color: "red" }}>No corresponding section - will be created from Deleting Data From a Channel examples</span>{" "}
    </Note.Note>
  </Fragment>
</Client.Tabs>

<Divider.Divider x />

## Idempotency

<Client.Tabs client:load exclude={["cpp"]}>
  <Fragment slot="console">
  </Fragment>

  <Fragment slot="python">
    <Note.Note variant="warning">
      <span style={{ color: "red" }}>No corresponding section - will be created from Limitations of Deletions content</span>{" "}
    </Note.Note>
  </Fragment>

  <Fragment slot="typescript">
    <Note.Note variant="warning">
      <span style={{ color: "red" }}>No corresponding section - will be created from Limitations of Deletions content</span>{" "}
    </Note.Note>
  </Fragment>
</Client.Tabs>

<Divider.Divider x />

## Safety Considerations

<Client.Tabs client:load exclude={["cpp"]}>
  <Fragment slot="console">
  </Fragment>

  <Fragment slot="python">
    <Note.Note variant="warning">
      <span style={{ color: "red" }}>No corresponding section - new content will be created</span>{" "}
    </Note.Note>
  </Fragment>

  <Fragment slot="typescript">
    <Note.Note variant="warning">
      <span style={{ color: "red" }}>No corresponding section - new content will be created</span>{" "}
    </Note.Note>
  </Fragment>
</Client.Tabs>

<Note.Note variant="info">
  **Planned content**: When to use delete operations, rollback strategies, backup recommendations, and production safety practices.
</Note.Note>
