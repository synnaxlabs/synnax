---
layout: "@/layouts/Reference.astro"
title: "Tables"
description: "View live telemetry in tables."
prev: "Schematics"
prevURL: "/reference/console/schematics"
next: "Users"
nextURL: "/reference/console/users"
---

import { Divider, Icon } from "@synnaxlabs/pluto";

import { Image } from "@/components/media/Media";
import Diagram from "@/components/diagram/Diagram.astro";
import CreateVisualization from "@/components/console/CreateVisualization.astro";
import ShareAndExport from "@/components/console/ShareAndExport.astro";
import { mdxOverrides } from "@/components/mdxOverrides";
export const components = mdxOverrides;

<Diagram style="height: 300px" preview>
  <Image
    client:only="react"
    id="console/tables/thumbnail"
    themed={false}
    style={{
      width: "500px",
      position: "absolute",
      left: "50%",
      transform: "translateX(-50%)",
      borderRadius: "1.5rem",
      boxShadow: "-1rem -1rem 1rem 3px var(--pluto-gray-l1-15)",
    }}
  />
</Diagram>

Tables display live telemetry in a tabular format. They are useful for dashboards,
viewing many channels at once, and conditionally coloring values that exceed thresholds.

<Divider.Divider x />

## Creating a Table

<CreateVisualization type="table" video="console/tables/create" />

<Divider.Divider x />

## Configuring the Table

Click the table <Icon.Table /> button in the bottom-left corner to open the Component
Toolbar and configure the table.

### Text Cells

Text cells display a line of text. You can change the alignment, weight, and size.

### Value Cells

Value cells function like values on a schematic. They display live data and update as
new data arrives.

### Redlining a Value

Redline a value to change its color when it passes a threshold. In the cell's `Redline`
tab, set the lower and upper bounds and the color that appears when the value passes
them.

<Image client:only="react" id="console/tables/redline" />

<Divider.Divider x />

## Sharing and Exporting

<ShareAndExport type="table" />
