---
layout: "@/layouts/Reference.astro"
title: "Tables"
description: "View live telemetry in tables."
prev: "Schematics"
prevURL: "/reference/console/schematics"
next: "Users"
nextURL: "/reference/console/users"
---

import { Divider } from "@synnaxlabs/pluto";

import { Image } from "@/components/media/Media";
import Diagram from "@/components/diagram/Diagram.astro";
import CreateVisualization from "@/components/console/CreateVisualization.astro";
import ShareAndExport from "@/components/console/ShareAndExport.astro";
import { mdxOverrides } from "@/components/mdxOverrides";
export const components = mdxOverrides;

<Diagram style="height: 300px" preview>
  <Image
    client:only="react"
    id="console/tables/thumbnail"
    themed={false}
    style={{
      width: "500px",
      position: "absolute",
      left: "50%",
      transform: "translateX(-50%)",
      borderRadius: "1.5rem",
      boxShadow: "-1rem -1rem 1rem 3px var(--pluto-gray-l1-15)",
    }}
  />
</Diagram>

Tables are used to view live telemetry in a tabular format. They can be used for
creating dashboards, viewing data from many different channels, and conditionally
coloring data that exceeds certain thresholds.

<Divider.Divider x />

## Creating a Table

<CreateVisualization type="table" video="console/tables/create" />

<Divider.Divider x />

## Configuring the Table

### Text Cells

In a table, a text cell can be used to display a line of text. You can also change the
text alignment, text weight, and and text size.

### Value Cells

Value cells functions like values on a schematic. These can be used to display live
data, and automatically update as new data comes in.

### Redlining a Value

You can "redline" a value to change the value's color when the number exceeds a certain
threshold. This is done by clicking the "Redline" tab in the cell's properties. The
lower and upper represent the lower and upper bounds on the value. Selecting a color
determines which color appears when the value passes the threshold.

<Image client:only="react" id="console/tables/redline" />

<Divider.Divider x />

## Sharing and Exporting

<ShareAndExport type="table" />
