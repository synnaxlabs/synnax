---
layout: "@/layouts/Reference.astro"
title: "UI overview"
heading: "Console UI overview"
description: "Navigate the Synnax Console UI."
prev: "Get started"
prevURL: "/reference/console/get-started"
next: "Projects"
nextURL: "/reference/console/projects"
---

import { Divider } from "@synnaxlabs/lyra/divider";
import { Icon } from "@synnaxlabs/lyra/icon";
import { Image, Video } from "@/components/media/Media";
import { Table } from "@/components";
import { mdxOverrides } from "@/components/mdxOverrides";
export const components = mdxOverrides;

The Synnax Console provides a flexible environment to display and interact with data
stored and streamed through a Synnax Core. It's interface is designed around two main UI
elements: the central layout mosaic and side toolbars.

The central mosaic displays visualizations such as line plots and schematics, while the
toolbars exists to provide access to resources, data ranges, tasks, and visualization
controls.

<Divider.Divider x />

## Toolbars

### <Icon.Channel />Channels

The [channels toolbar](/reference/client/channels) allows you to create, view, and
access the channels you're interested in visualizing.

### <Icon.Range />Ranges

The [ranges toolbar](/reference/client/ranges) allows you to create, view, and access
ranges that you have favorited in the Console.

### <Icon.Project />Projects

The [projects toolbar](/reference/console/projects) allows you to create and view
projects that have been made by users in your Synnax deployment.

### <Icon.Device />Devices

The [devices toolbar](/reference/driver) allows you to view and track the health of the
devices you're interested in visualizing.

### <Icon.Task />Tasks

The [tasks toolbar](/reference/driver) allows you to view, pause, and resume the current
tasks running on a Driver.

### <Icon.User />Users

The [users toolbar](/reference/console/users) allows you to view and change permissions
for users in your Synnax deployment.

### <Icon.Component />Component

The component toolbar allows you to edit the properties of
[line plots](/reference/console/line-plots), [logs](/reference/console/logs),
[schematics](/reference/console/schematics), and [tables](/reference/console/tables).

## Getting help

{/* prettier-ignore-start */}

The Synnax documentation can be opened from within the Console by clicking the
<Icon.QuestionMark /> icon.

{/* prettier-ignore-end */}

<Video client:only="react" id="console/ui-overview/documentation" />

<Divider.Divider x />

## Search and command palette

{/* prettier-ignore-start */}

The search and command palette can be used to search for resources or to activate
commands.

- On Windows, press `Ctrl+P` to search for resources or `Ctrl+Shift+P` to search for
  commands.
- On macOS, press <code><Icon.Keyboard.Command />+P</code> to search for resources or
  <code><Icon.Keyboard.Command />+<Icon.Keyboard.Shift />+P</code> to search for
  commands.


{/* prettier-ignore-end */}

<Video client:only="react" id="console/ui-overview/palette-search" />
