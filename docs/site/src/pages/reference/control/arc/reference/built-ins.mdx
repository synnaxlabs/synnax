---
layout: "@/layouts/Reference.astro"
title: "Built-In Functions"
description:
  "Arc reference for the len built-in and the status and time standard library functions"
prev: "Sequences"
prevURL: "/reference/control/arc/reference/sequences"
next: "Errors"
nextURL: "/reference/control/arc/reference/errors"
---

import { Divider, Note, Icon, Text } from "@synnaxlabs/pluto";
import Details from "@/components/details/Details.astro";
import { mdxOverrides } from "@/components/mdxOverrides";

export const components = mdxOverrides;

<Note.Note variant="info">
  Built-in functions like `len` are available in the global namespace without a module
  prefix. Other functionality (timing, statistics, signal processing, status, authority)
  lives in Arc's standard library modules and is accessed with a qualified name after
  importing the module, such as `status.set` or `math.avg`.
</Note.Note>

## Core Functions

### len

Returns the length of a series or string.

```arc
data := [1.0, 2.0, 3.0]
length := len(data) // 3 (i64)

msg := "hello"
chars := len(msg) // 5 (i64)
```

| Parameter   | Type              | Description                 |
| ----------- | ----------------- | --------------------------- |
| input       | `series` or `str` | Value to measure            |
| **Returns** | `i64`             | Number of elements or bytes |

<Divider.Divider x />

## Standard Library

Standard library functions live in modules and require an `import` before use.

### status.set

Sets a status notification (an alarm, warning, or operational state) on the Core,
addressing it by name or by key, and outputs the resolved status key. It lives in the
`status` module, so a program must `import status` before using it.

<Details>
<Text.Text slot="summary" level="p"><Icon.Details /> Parameters</Text.Text>

| Parameter     | Type  | Required | Description                                                                                                                                                                                               |
| ------------- | ----- | -------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `key_or_name` | `str` | Yes      | An existing status key or name. A match is updated in place; if none matches, a new status is created with this name; if several statuses share the name, the first is updated and a warning is surfaced. |
| `message`     | `str` | Yes      | The message body shown on the status.                                                                                                                                                                     |
| `variant`     | `str` | Yes      | One of `success`, `info`, `warning`, `error`, `loading`, or `disabled`.                                                                                                                                   |

**Output** `str`: the resolved status key that was set.

<Note.Note variant="info" className="compact">
  If a `status.set` fails (for example, an empty `key_or_name` or a database error), it
  reports a `warning` and the automation keeps running.
</Note.Note>

</Details>

```arc
trigger -> status.set{
    key_or_name = "ox_alarm",
    variant = "error",
    message = "Overpressure detected"
}

func raise_alarm() {
    alarm_key = status.set(
      "ox_alarm",
      "Overpressure detected",
      "error"
    )
}
```

### time.now

Returns the current timestamp. `time.now` works both as a flow node and as a direct
function call, so it can be wired into a chain or called inside a `func`. It lives in
the `time` module, so a program must `import time` before using it.

<Details>
<Text.Text slot="summary" level="p"><Icon.Details /> Parameters</Text.Text>

**Output** `i64 ns`: the current timestamp.

</Details>

```arc
trigger -> time.now{} -> current_time

func stamp() {
    current_time = time.now()
}
```

### time.interval

Fires repeatedly at a fixed period, emitting `1` on each tick. Common for driving
control loops, periodic sampling, and generating test data.

<Details>
<Text.Text slot="summary" level="p"><Icon.Details /> Parameters</Text.Text>

| Parameter | Type     | Required | Description                                                                |
| --------- | -------- | -------- | -------------------------------------------------------------------------- |
| `period`  | `i64 ns` | Yes      | Time between fires, written as a duration literal (such as `200ms`, `1s`). |

**Output** `u8`: emits `1` on each fire.

</Details>

```arc
// `flow` only
time.interval{period=1s} -> tick
```

### time.wait

Fires once after a fixed duration, then stops.

<Details>
<Text.Text slot="summary" level="p"><Icon.Details /> Parameters</Text.Text>

| Parameter  | Type     | Required | Description                                                                    |
| ---------- | -------- | -------- | ------------------------------------------------------------------------------ |
| `duration` | `i64 ns` | Yes      | How long to wait before firing, written as a duration literal (such as `30s`). |

**Output** `u8`: emits `1` once the duration elapses.

</Details>

```arc
// `flow` only
time.wait{duration=500ms} -> done
```

<Divider.Divider x />

## Summary Table

| Function        | Category         | Description                  |
| --------------- | ---------------- | ---------------------------- |
| `len`           | Core             | Length of series or string   |
| `status.set`    | Standard Library | Publish a status to the Core |
| `time.now`      | Standard Library | Current timestamp            |
| `time.interval` | Standard Library | Fire repeatedly at a period  |
| `time.wait`     | Standard Library | Fire once after a duration   |
