// Copyright 2026 Synnax Labs, Inc.
//
// Use of this software is governed by the Business Source License included in the file
// licenses/BSL.txt.
//
// As of the Change Date specified in that file, in accordance with the Business Source
// License, use of this software will be governed by the Apache License, Version 2.0,
// included in the file licenses/APL.txt.

// Code generated by oracle. DO NOT EDIT.

#pragma once

#include <cstdint>
#include <string>
#include <utility>

#include "x/cpp/errors/errors.h"
#include "x/cpp/json/json.h"

#include "x/go/crdt/pb/crdt.pb.h"

namespace x::crdt {

struct ID;
struct Insert;
struct Delete;

/// @brief ID uniquely identifies a character within a replicated text document. It
/// pairs the replica that created the character with that replica's per-character
/// counter, making the identifier globally unique without coordination.
struct ID {
    /// @brief replica is the replica that created the character. A zero replica marks
    /// the
    /// document root sentinel.
    std::uint32_t replica = 0;
    /// @brief counter is the per-replica counter, incremented for every character the
    /// replica inserts.
    std::uint64_t counter = 0;

    static ID parse(x::json::Parser parser);
    [[nodiscard]] x::json::json to_json() const;

    using proto_type = ::x::crdt::pb::ID;
    [[nodiscard]] std::pair<::x::crdt::pb::ID, x::errors::Error> to_proto() const;
    static std::pair<ID, x::errors::Error> from_proto(const ::x::crdt::pb::ID &pb);
};

/// @brief Insert introduces a single character into a replicated text document.
struct Insert {
    /// @brief id is the identity of the inserted character.
    ID id;
    /// @brief origin is the existing character this one anchors to. The root sentinel
    /// anchors to the start of the document.
    ID origin;
    /// @brief side is the side of origin the character anchors to: left places it
    /// immediately before the origin, right immediately after.
    std::string side;
    /// @brief char_ is the inserted Unicode code point.
    std::int32_t char_ = 0;

    static Insert parse(x::json::Parser parser);
    [[nodiscard]] x::json::json to_json() const;

    using proto_type = ::x::crdt::pb::Insert;
    [[nodiscard]] std::pair<::x::crdt::pb::Insert, x::errors::Error> to_proto() const;
    static std::pair<Insert, x::errors::Error>
    from_proto(const ::x::crdt::pb::Insert &pb);
};

/// @brief Delete tombstones the character with the given id. It carries only the id
/// because removal needs no position; the character remains in the document tree as a
/// tombstone so concurrently-inserted neighbors can still anchor to it.
struct Delete {
    /// @brief id is the identity of the character to tombstone.
    ID id;

    static Delete parse(x::json::Parser parser);
    [[nodiscard]] x::json::json to_json() const;

    using proto_type = ::x::crdt::pb::Delete;
    [[nodiscard]] std::pair<::x::crdt::pb::Delete, x::errors::Error> to_proto() const;
    static std::pair<Delete, x::errors::Error>
    from_proto(const ::x::crdt::pb::Delete &pb);
};
}
