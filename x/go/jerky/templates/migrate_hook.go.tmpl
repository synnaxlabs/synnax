package types

// Migrate{{ .TypeName }}V{{ .FromVersion }}ToV{{ .ToVersion }}Hook handles custom field mappings from V{{ .FromVersion }} to V{{ .ToVersion }}.
// This file is generated once and will NOT be overwritten by jerky.
{{- if .AddedFields }}
// New fields in V{{ .ToVersion }}: {{ range $i, $f := .AddedFields }}{{ if $i }}, {{ end }}{{ $f }}{{ end }}
{{- end }}
{{- if .RemovedFields }}
// Removed fields from V{{ .FromVersion }}: {{ range $i, $f := .RemovedFields }}{{ if $i }}, {{ end }}{{ $f }}{{ end }}
{{- end }}
func Migrate{{ .TypeName }}V{{ .FromVersion }}ToV{{ .ToVersion }}Hook(old *{{ .TypeName }}V{{ .FromVersion }}, new *{{ .TypeName }}V{{ .ToVersion }}) {
	// Add custom field mappings here. Examples:
	// new.NewField = old.OldField           // field rename
	// new.FullName = old.First + old.Last   // field merge
	// new.Role = "user"                     // default value for new field
}
