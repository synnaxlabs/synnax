// Copyright 2026 Synnax Labs, Inc.
//
// Use of this software is governed by the Business Source License included in the file
// licenses/BSL.txt.
//
// As of the Change Date specified in that file, in accordance with the Business Source
// License, use of this software will be governed by the Apache License, Version 2.0,
// included in the file licenses/APL.txt.

// Code generated by Oracle. DO NOT EDIT.

import { uuid } from "@synnaxlabs/x";
import { z } from "zod";

import { device } from "@/device";

/** Keyed is the base for every stored task configuration record. */
export const keyedZ = z.object({
  /** key is the unique identifier for the stored configuration record. */
  key: z.uuid().default(uuid.create),
});
export interface Keyed extends z.infer<typeof keyedZ> {}

export const baseStartZ = keyedZ.extend({
  autoStart: z.boolean().default(false),
});
export interface BaseStart extends z.infer<typeof baseStartZ> {}

export const baseScanZ = keyedZ.extend({
  rate: z.number().default(0.2),
  disabled: z.boolean().default(false),
});
export interface BaseScan extends z.infer<typeof baseScanZ> {}

export const basePersistZ = baseStartZ.extend({
  dataSavingDisabled: z.boolean().default(false),
});
export interface BasePersist extends z.infer<typeof basePersistZ> {}

export const baseReadZ = basePersistZ.extend({
  sampleRate: z.number().default(10),
  streamRate: z.number().default(5),
});
export interface BaseRead extends z.infer<typeof baseReadZ> {}

export const baseWriteZ = basePersistZ.extend({
  device: device.keyZ.default(""),
});
export interface BaseWrite extends z.infer<typeof baseWriteZ> {}
