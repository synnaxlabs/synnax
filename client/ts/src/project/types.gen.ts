// Copyright 2026 Synnax Labs, Inc.
//
// Use of this software is governed by the Business Source License included in the file
// licenses/BSL.txt.
//
// As of the Change Date specified in that file, in accordance with the Business Source
// License, use of this software will be governed by the Apache License, Version 2.0,
// included in the file licenses/APL.txt.

// Code generated by Oracle. DO NOT EDIT.

import { z } from "zod";

import { ontology } from "@/ontology";

export const keyZ = z.uuid();
export type Key = z.infer<typeof keyZ>;

/**
 * Project is the top-level organizational container in Synnax. A project represents the
 * configuration of a hardware system (tasks, arcs, device references, calibrations)
 * or an analytical context. Projects contain one or more workspaces, which define
 * how individual operators view the system.
 */
export const projectZ = z.object({
  /** key is the unique identifier for this project. */
  key: keyZ,
  /** name is a human-readable name for the project. */
  name: z.string().min(1, "Name is required"),
});
export interface Project extends z.infer<typeof projectZ> {}

export const newZ = projectZ.partial({ key: true });
export interface New extends z.input<typeof newZ> {}

export const ontologyID = ontology.createIDFactory<Key>("project");
export const TYPE_ONTOLOGY_ID = ontologyID("");
