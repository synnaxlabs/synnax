// Copyright 2026 Synnax Labs, Inc.
//
// Use of this software is governed by the Business Source License included in the file
// licenses/BSL.txt.
//
// As of the Change Date specified in that file, in accordance with the Business Source
// License, use of this software will be governed by the Apache License, Version 2.0,
// included in the file licenses/APL.txt.

// Code generated by Oracle. DO NOT EDIT.

import { array, crdt } from "@synnaxlabs/x";
import { z } from "zod";

/**
 * Document is the conflict-free replicated representation of the text: the operations that
 * reconstruct it when applied to an empty replica. It is the durable source of
 * truth from which raw is materialized.
 */
export const documentZ = z.object({
  /** inserts are the operations that reconstruct the document's characters. */
  inserts: array.nullishToEmpty(crdt.insertZ),
  /** deletes are the operations that tombstone deleted characters. */
  deletes: array.nullishToEmpty(crdt.deleteZ),
});
export interface Document extends z.infer<typeof documentZ> {}

/** Text is text-based Arc source code with optional parsed AST for compilation. */
export const textZ = z.object({
  /**
   * doc is the replicated source of truth for the text. It defaults to empty on create,
   * in which case the server seeds it from raw.
   */
  doc: documentZ.prefault({}),
  /** raw is the materialized Arc source code, derived from doc and not persisted. */
  raw: z.string().default(""),
});
export interface Text extends z.infer<typeof textZ> {}
