#  Copyright 2026 Synnax Labs, Inc.
#
#  Use of this software is governed by the Business Source License included in the file
#  licenses/BSL.txt.
#
#  As of the Change Date specified in that file, in accordance with the Business Source
#  License, use of this software will be governed by the Apache License, Version 2.0,
#  included in the file licenses/APL.txt.

# Code generated by Oracle. DO NOT EDIT.

from __future__ import annotations

from typing import Annotated, Literal, Union

from pydantic import BaseModel, Field

from synnax import channel as channel_
from synnax import device as device_
from synnax import task
from x import telem

TERMINAL_CONFIG_CFG_DEFAULT: Literal["Cfg_Default"] = "Cfg_Default"

TERMINAL_CONFIG_RSE: Literal["RSE"] = "RSE"

TERMINAL_CONFIG_NRSE: Literal["NRSE"] = "NRSE"

TERMINAL_CONFIG_DIFF: Literal["Diff"] = "Diff"

TERMINAL_CONFIG_PSEUDO_DIFF: Literal["PseudoDiff"] = "PseudoDiff"


TerminalConfig = Literal["Cfg_Default", "RSE", "NRSE", "Diff", "PseudoDiff"]

EXCITATION_SOURCE_INTERNAL: Literal["Internal"] = "Internal"

EXCITATION_SOURCE_EXTERNAL: Literal["External"] = "External"

EXCITATION_SOURCE_NONE: Literal["None"] = "None"


ExcitationSource = Literal["Internal", "External", "None"]

BRIDGE_CONFIG_FULL_BRIDGE: Literal["FullBridge"] = "FullBridge"

BRIDGE_CONFIG_HALF_BRIDGE: Literal["HalfBridge"] = "HalfBridge"

BRIDGE_CONFIG_QUARTER_BRIDGE: Literal["QuarterBridge"] = "QuarterBridge"


BridgeConfig = Literal["FullBridge", "HalfBridge", "QuarterBridge"]

ELECTRICAL_UNITS_M_VOLTS_PER_VOLT: Literal["mVoltsPerVolt"] = "mVoltsPerVolt"

ELECTRICAL_UNITS_VOLTS_PER_VOLT: Literal["VoltsPerVolt"] = "VoltsPerVolt"


ElectricalUnits = Literal["mVoltsPerVolt", "VoltsPerVolt"]

RESISTANCE_CONFIG_WIRE_2: Literal["2Wire"] = "2Wire"

RESISTANCE_CONFIG_WIRE_3: Literal["3Wire"] = "3Wire"

RESISTANCE_CONFIG_WIRE_4: Literal["4Wire"] = "4Wire"


ResistanceConfig = Literal["2Wire", "3Wire", "4Wire"]

Z_INDEX_PHASE_A_HIGH_B_HIGH: Literal["AHighBHigh"] = "AHighBHigh"

Z_INDEX_PHASE_A_HIGH_B_LOW: Literal["AHighBLow"] = "AHighBLow"

Z_INDEX_PHASE_A_LOW_B_HIGH: Literal["ALowBHigh"] = "ALowBHigh"

Z_INDEX_PHASE_A_LOW_B_LOW: Literal["ALowBLow"] = "ALowBLow"


ZIndexPhase = Literal["AHighBHigh", "AHighBLow", "ALowBHigh", "ALowBLow"]

UNITS_VOLTS: Literal["Volts"] = "Volts"

UNITS_AMPS: Literal["Amps"] = "Amps"

UNITS_DEG_F: Literal["DegF"] = "DegF"

UNITS_DEG_C: Literal["DegC"] = "DegC"

UNITS_DEG_R: Literal["DegR"] = "DegR"

UNITS_KELVINS: Literal["Kelvins"] = "Kelvins"

UNITS_STRAIN: Literal["Strain"] = "Strain"

UNITS_OHMS: Literal["Ohms"] = "Ohms"

UNITS_HZ: Literal["Hz"] = "Hz"

UNITS_SECONDS: Literal["Seconds"] = "Seconds"

UNITS_METERS: Literal["Meters"] = "Meters"

UNITS_INCHES: Literal["Inches"] = "Inches"

UNITS_DEGREES: Literal["Degrees"] = "Degrees"

UNITS_RADIANS: Literal["Radians"] = "Radians"

UNITS_G: Literal["g"] = "g"

UNITS_METERS_PER_SECOND_SQUARED: Literal["MetersPerSecondSquared"] = (
    "MetersPerSecondSquared"
)

UNITS_NEWTONS: Literal["Newtons"] = "Newtons"

UNITS_POUNDS: Literal["Pounds"] = "Pounds"

UNITS_KILOGRAM_FORCE: Literal["KilogramForce"] = "KilogramForce"

UNITS_POUNDS_PER_SQUARE_INCH: Literal["PoundsPerSquareInch"] = "PoundsPerSquareInch"

UNITS_BAR: Literal["Bar"] = "Bar"

UNITS_PASCALS: Literal["Pascals"] = "Pascals"

UNITS_VOLTS_PER_VOLT: Literal["VoltsPerVolt"] = "VoltsPerVolt"

UNITS_M_VOLTS_PER_VOLT: Literal["mVoltsPerVolt"] = "mVoltsPerVolt"

UNITS_NEWTON_METERS: Literal["NewtonMeters"] = "NewtonMeters"

UNITS_INCH_POUNDS: Literal["InchPounds"] = "InchPounds"

UNITS_INCH_OUNCES: Literal["InchOunces"] = "InchOunces"

UNITS_FOOT_POUNDS: Literal["FootPounds"] = "FootPounds"


Units = Literal[
    "Volts",
    "Amps",
    "DegF",
    "DegC",
    "DegR",
    "Kelvins",
    "Strain",
    "Ohms",
    "Hz",
    "Seconds",
    "Meters",
    "Inches",
    "Degrees",
    "Radians",
    "g",
    "MetersPerSecondSquared",
    "Newtons",
    "Pounds",
    "KilogramForce",
    "PoundsPerSquareInch",
    "Bar",
    "Pascals",
    "VoltsPerVolt",
    "mVoltsPerVolt",
    "NewtonMeters",
    "InchPounds",
    "InchOunces",
    "FootPounds",
]

ACCEL_SENSITIVITY_UNITS_M_VOLTS_PER_G: Literal["mVoltsPerG"] = "mVoltsPerG"

ACCEL_SENSITIVITY_UNITS_VOLTS_PER_G: Literal["VoltsPerG"] = "VoltsPerG"


AccelSensitivityUnits = Literal["mVoltsPerG", "VoltsPerG"]

ACCEL_UNITS_G: Literal["g"] = "g"

ACCEL_UNITS_METERS_PER_SECOND_SQUARED: Literal["MetersPerSecondSquared"] = (
    "MetersPerSecondSquared"
)

ACCEL_UNITS_INCHES_PER_SECOND_SQUARED: Literal["InchesPerSecondSquared"] = (
    "InchesPerSecondSquared"
)


AccelUnits = Literal["g", "MetersPerSecondSquared", "InchesPerSecondSquared"]

FORCE_UNITS_NEWTONS: Literal["Newtons"] = "Newtons"

FORCE_UNITS_POUNDS: Literal["Pounds"] = "Pounds"

FORCE_UNITS_KILOGRAM_FORCE: Literal["KilogramForce"] = "KilogramForce"


ForceUnits = Literal["Newtons", "Pounds", "KilogramForce"]

SHUNT_RESISTOR_LOCATION_DEFAULT: Literal["Default"] = "Default"

SHUNT_RESISTOR_LOCATION_INTERNAL: Literal["Internal"] = "Internal"

SHUNT_RESISTOR_LOCATION_EXTERNAL: Literal["External"] = "External"


ShuntResistorLocation = Literal["Default", "Internal", "External"]

FORCE_SENSITIVITY_UNITS_M_VOLTS_PER_NEWTON: Literal["mVoltsPerNewton"] = (
    "mVoltsPerNewton"
)

FORCE_SENSITIVITY_UNITS_M_VOLTS_PER_POUND: Literal["mVoltsPerPound"] = "mVoltsPerPound"


ForceSensitivityUnits = Literal["mVoltsPerNewton", "mVoltsPerPound"]

PRESSURE_UNITS_POUNDS_PER_SQUARE_INCH: Literal["PoundsPerSquareInch"] = (
    "PoundsPerSquareInch"
)

PRESSURE_UNITS_PASCALS: Literal["Pascals"] = "Pascals"

PRESSURE_UNITS_BAR: Literal["Bar"] = "Bar"


PressureUnits = Literal["PoundsPerSquareInch", "Pascals", "Bar"]

TORQUE_UNITS_NEWTON_METERS: Literal["NewtonMeters"] = "NewtonMeters"

TORQUE_UNITS_INCH_OUNCES: Literal["InchOunces"] = "InchOunces"

TORQUE_UNITS_INCH_POUNDS: Literal["InchPounds"] = "InchPounds"

TORQUE_UNITS_FOOT_POUNDS: Literal["FootPounds"] = "FootPounds"


TorqueUnits = Literal["NewtonMeters", "InchOunces", "InchPounds", "FootPounds"]

TEMPERATURE_UNITS_DEG_C: Literal["DegC"] = "DegC"

TEMPERATURE_UNITS_DEG_F: Literal["DegF"] = "DegF"

TEMPERATURE_UNITS_KELVINS: Literal["Kelvins"] = "Kelvins"

TEMPERATURE_UNITS_DEG_R: Literal["DegR"] = "DegR"


TemperatureUnits = Literal["DegC", "DegF", "Kelvins", "DegR"]

RTD_TYPE_PT_3750: Literal["Pt3750"] = "Pt3750"

RTD_TYPE_PT_3851: Literal["Pt3851"] = "Pt3851"

RTD_TYPE_PT_3911: Literal["Pt3911"] = "Pt3911"

RTD_TYPE_PT_3916: Literal["Pt3916"] = "Pt3916"

RTD_TYPE_PT_3920: Literal["Pt3920"] = "Pt3920"

RTD_TYPE_PT_3928: Literal["Pt3928"] = "Pt3928"


RTDType = Literal["Pt3750", "Pt3851", "Pt3911", "Pt3916", "Pt3920", "Pt3928"]

STRAIN_CONFIG_FULL_BRIDGE_I: Literal["FullBridgeI"] = "FullBridgeI"

STRAIN_CONFIG_FULL_BRIDGE_II: Literal["FullBridgeII"] = "FullBridgeII"

STRAIN_CONFIG_FULL_BRIDGE_III: Literal["FullBridgeIII"] = "FullBridgeIII"

STRAIN_CONFIG_HALF_BRIDGE_I: Literal["HalfBridgeI"] = "HalfBridgeI"

STRAIN_CONFIG_HALF_BRIDGE_II: Literal["HalfBridgeII"] = "HalfBridgeII"

STRAIN_CONFIG_QUARTER_BRIDGE_I: Literal["QuarterBridgeI"] = "QuarterBridgeI"

STRAIN_CONFIG_QUARTER_BRIDGE_II: Literal["QuarterBridgeII"] = "QuarterBridgeII"


StrainConfig = Literal[
    "FullBridgeI",
    "FullBridgeII",
    "FullBridgeIII",
    "HalfBridgeI",
    "HalfBridgeII",
    "QuarterBridgeI",
    "QuarterBridgeII",
]

THERMOCOUPLE_TYPE_J: Literal["J"] = "J"

THERMOCOUPLE_TYPE_K: Literal["K"] = "K"

THERMOCOUPLE_TYPE_N: Literal["N"] = "N"

THERMOCOUPLE_TYPE_R: Literal["R"] = "R"

THERMOCOUPLE_TYPE_S: Literal["S"] = "S"

THERMOCOUPLE_TYPE_T: Literal["T"] = "T"

THERMOCOUPLE_TYPE_B: Literal["B"] = "B"

THERMOCOUPLE_TYPE_E: Literal["E"] = "E"


ThermocoupleType = Literal["J", "K", "N", "R", "S", "T", "B", "E"]

VELOCITY_UNITS_METERS_PER_SECOND: Literal["MetersPerSecond"] = "MetersPerSecond"

VELOCITY_UNITS_INCHES_PER_SECOND: Literal["InchesPerSecond"] = "InchesPerSecond"


VelocityUnits = Literal["MetersPerSecond", "InchesPerSecond"]

VELOCITY_SENSITIVITY_UNITS_M_VOLTS_PER_MM_PER_SECOND: Literal[
    "MillivoltsPerMillimeterPerSecond"
] = "MillivoltsPerMillimeterPerSecond"

VELOCITY_SENSITIVITY_UNITS_M_VOLTS_PER_INCH_PER_SECOND: Literal[
    "MilliVoltsPerInchPerSecond"
] = "MilliVoltsPerInchPerSecond"


VelocitySensitivityUnits = Literal[
    "MillivoltsPerMillimeterPerSecond", "MilliVoltsPerInchPerSecond"
]

CHARGE_UNITS_COULOMBS: Literal["Coulombs"] = "Coulombs"

CHARGE_UNITS_PICO_COULOMBS: Literal["PicoCoulombs"] = "PicoCoulombs"


ChargeUnits = Literal["Coulombs", "PicoCoulombs"]

CI_EDGE_RISING: Literal["Rising"] = "Rising"

CI_EDGE_FALLING: Literal["Falling"] = "Falling"


CIEdge = Literal["Rising", "Falling"]

CI_MEAS_METHOD_LOW_FREQ_1_CTR: Literal["LowFreq1Ctr"] = "LowFreq1Ctr"

CI_MEAS_METHOD_HIGH_FREQ_2_CTR: Literal["HighFreq2Ctr"] = "HighFreq2Ctr"

CI_MEAS_METHOD_LARGE_RNG_2_CTR: Literal["LargeRng2Ctr"] = "LargeRng2Ctr"

CI_MEAS_METHOD_DYNAMIC_AVG: Literal["DynamicAvg"] = "DynamicAvg"


CIMeasMethod = Literal["LowFreq1Ctr", "HighFreq2Ctr", "LargeRng2Ctr", "DynamicAvg"]

CI_FREQ_UNITS_HZ: Literal["Hz"] = "Hz"

CI_FREQ_UNITS_SECONDS: Literal["Seconds"] = "Seconds"

CI_FREQ_UNITS_TICKS: Literal["Ticks"] = "Ticks"


CIFreqUnits = Literal["Hz", "Seconds", "Ticks"]

CI_TIME_UNITS_SECONDS: Literal["Seconds"] = "Seconds"

CI_TIME_UNITS_TICKS: Literal["Ticks"] = "Ticks"


CITimeUnits = Literal["Seconds", "Ticks"]

CI_COUNT_DIRECTION_COUNT_UP: Literal["CountUp"] = "CountUp"

CI_COUNT_DIRECTION_COUNT_DOWN: Literal["CountDown"] = "CountDown"

CI_COUNT_DIRECTION_EXTERNALLY_CONTROLLED: Literal["ExternallyControlled"] = (
    "ExternallyControlled"
)


CICountDirection = Literal["CountUp", "CountDown", "ExternallyControlled"]

CI_DECODING_TYPE_X1: Literal["X1"] = "X1"

CI_DECODING_TYPE_X2: Literal["X2"] = "X2"

CI_DECODING_TYPE_X4: Literal["X4"] = "X4"

CI_DECODING_TYPE_TWO_PULSE: Literal["TwoPulse"] = "TwoPulse"


CIDecodingType = Literal["X1", "X2", "X4", "TwoPulse"]

CI_LINEAR_VELOCITY_UNITS_M_PER_S: Literal["m/s"] = "m/s"

CI_LINEAR_VELOCITY_UNITS_IN_PER_S: Literal["in/s"] = "in/s"


CILinearVelocityUnits = Literal["m/s", "in/s"]

CI_ANGULAR_VELOCITY_UNITS_RPM: Literal["RPM"] = "RPM"

CI_ANGULAR_VELOCITY_UNITS_RADIANS_PER_SECOND: Literal["Radians/s"] = "Radians/s"

CI_ANGULAR_VELOCITY_UNITS_DEGREES_PER_SECOND: Literal["Degrees/s"] = "Degrees/s"


CIAngularVelocityUnits = Literal["RPM", "Radians/s", "Degrees/s"]

CI_LINEAR_POSITION_UNITS_METERS: Literal["Meters"] = "Meters"

CI_LINEAR_POSITION_UNITS_INCHES: Literal["Inches"] = "Inches"

CI_LINEAR_POSITION_UNITS_TICKS: Literal["Ticks"] = "Ticks"


CILinearPositionUnits = Literal["Meters", "Inches", "Ticks"]

CI_ANGULAR_POSITION_UNITS_DEGREES: Literal["Degrees"] = "Degrees"

CI_ANGULAR_POSITION_UNITS_RADIANS: Literal["Radians"] = "Radians"

CI_ANGULAR_POSITION_UNITS_TICKS: Literal["Ticks"] = "Ticks"


CIAngularPositionUnits = Literal["Degrees", "Radians", "Ticks"]

WAVE_TYPE_SINE: Literal["Sine"] = "Sine"

WAVE_TYPE_TRIANGLE: Literal["Triangle"] = "Triangle"

WAVE_TYPE_SQUARE: Literal["Square"] = "Square"

WAVE_TYPE_SAWTOOTH: Literal["Sawtooth"] = "Sawtooth"


WaveType = Literal["Sine", "Triangle", "Square", "Sawtooth"]


class MinMaxVal(BaseModel):
    """Bounds the expected signal range in scaled units.

    Attributes:
        min_val: Is the minimum expected value, in scaled units.
        max_val: Is the maximum expected value, in scaled units.
    """

    min_val: float = 0
    max_val: float = 1


class Terminal(BaseModel):
    """Configures the terminal wiring of an analog input channel.

    Attributes:
        terminal_config: Selects the terminal configuration (RSE, NRSE, Diff, ...).
    """

    terminal_config: TerminalConfig = "Cfg_Default"


class Sensitivity(BaseModel):
    """Specifies a sensor's sensitivity rating.

    Attributes:
        sensitivity: Is the sensor's sensitivity rating.
    """

    sensitivity: float = 0


class CurrentExcitation(BaseModel):
    """Configures the current excitation supplied to a sensor.

    Attributes:
        current_excit_source: Selects the source of the current excitation signal.
        current_excit_val: Is the current excitation level, in Amps.
    """

    current_excit_source: ExcitationSource = "Internal"
    current_excit_val: float = 0


class VoltageExcitation(BaseModel):
    """Configures the voltage excitation supplied to a sensor.

    Attributes:
        voltage_excit_source: Selects the source of the voltage excitation signal.
        voltage_excit_val: Is the voltage excitation level, in Volts.
    """

    voltage_excit_source: ExcitationSource = "Internal"
    voltage_excit_val: float = 0


class Bridge(BaseModel):
    """Configures the wiring and nominal resistance of a bridge-based sensor.

    Attributes:
        bridge_config: Selects the physical bridge wiring.
        nominal_bridge_resistance: Is the nominal resistance of the bridge, in Ohms.
    """

    bridge_config: BridgeConfig = "FullBridge"
    nominal_bridge_resistance: float = 1


class BridgePolynomial(BaseModel):
    """Scales bridge electrical output to physical units with a polynomial.

    Attributes:
        forward_coeffs: Are the coefficients mapping electrical to physical values.
        reverse_coeffs: Are the coefficients mapping physical to electrical values.
        electrical_units: Are the electrical output units of the bridge.
    """

    forward_coeffs: list[float] = Field(default_factory=list)
    reverse_coeffs: list[float] = Field(default_factory=list)
    electrical_units: ElectricalUnits = "mVoltsPerVolt"


class Resistance(BaseModel):
    """Configures the wiring of a resistance measurement.

    Attributes:
        resistance_config: Selects the number of wires used for the resistance
            measurement.
    """

    resistance_config: ResistanceConfig = "2Wire"


class Table(BaseModel):
    """Scales bridge electrical output to physical units with a lookup table.

    Attributes:
        electrical_vals: Are the electrical breakpoints of the calibration table.
        electrical_units: Are the units of the electrical breakpoints.
        physical_vals: Are the physical values corresponding to each electrical
            breakpoint.
    """

    electrical_vals: list[float] = Field(default_factory=list)
    electrical_units: ElectricalUnits = "mVoltsPerVolt"
    physical_vals: list[float] = Field(default_factory=list)


class TwoPointLin(BaseModel):
    """Scales bridge electrical output to physical units between two calibration points.

    Attributes:
        first_electrical_val: Is the first electrical calibration point.
        second_electrical_val: Is the second electrical calibration point.
        electrical_units: Are the units of the electrical calibration points.
        first_physical_val: Is the physical value at the first electrical point.
        second_physical_val: Is the physical value at the second electrical point.
    """

    first_electrical_val: float = 0
    second_electrical_val: float = 1
    electrical_units: ElectricalUnits = "mVoltsPerVolt"
    first_physical_val: float = 0
    second_physical_val: float = 1


class BaseAIChannel(BaseModel):
    """Carries the fields every NI analog input channel shares.

    Attributes:
        key: Uniquely identifies the channel within the task.
        name: Is the human-readable channel name.
        disabled: Is true when the channel is excluded from acquisition.
        channel: Is the Synnax channel that raw samples are written to.
        port: Is the physical port the channel reads from.
        device: Is the key of the device the channel belongs to.
    """

    key: str = ""
    name: str = ""
    disabled: bool = False
    channel: channel_.Key = Field(default=channel_.Key(0), ge=0, le=4294967295)
    port: int = Field(default=0, ge=-2147483648, le=2147483647)
    device: device_.Key = ""

    def __hash__(self) -> int:
        return hash(self.key)


class ZIndex(BaseModel):
    """Configures the Z-index reset behavior of an encoder.

    Attributes:
        z_index_enable: Is true when the encoder's Z index resets the count.
        z_index_val: Is the value the measurement resets to when the Z index is active.
        z_index_phase: Selects the A/B states at which the Z index is active.
        terminal_z: Is the terminal the Z index signal is wired to.
    """

    z_index_enable: bool = False
    z_index_val: float = 0
    z_index_phase: ZIndexPhase = "AHighBHigh"
    terminal_z: str = ""


class BaseCIChannel(BaseModel):
    """Carries the fields every NI counter input channel shares.

    Attributes:
        key: Uniquely identifies the channel within the task.
        name: Is the human-readable channel name.
        disabled: Is true when the channel is excluded from acquisition.
        channel: Is the Synnax channel that raw samples are written to.
        port: Is the physical counter the channel uses.
        device: Is the key of the device the counter belongs to.
    """

    key: str = ""
    name: str = ""
    disabled: bool = False
    channel: channel_.Key = Field(default=channel_.Key(0), ge=0, le=4294967295)
    port: int = Field(default=0, ge=-2147483648, le=2147483647)
    device: device_.Key = ""

    def __hash__(self) -> int:
        return hash(self.key)


class BaseAOChannel(BaseModel):
    """Carries the fields every NI analog output channel shares.

    Attributes:
        key: Uniquely identifies the channel within the task.
        disabled: Is true when the channel is excluded from the task.
        cmd_channel: Is the Synnax channel commands are read from.
        state_channel: Is the Synnax channel the output state is written to.
        cmd_channel_name: Is the human-readable name of the command channel.
        state_channel_name: Is the human-readable name of the state channel.
        port: Is the physical port the channel writes to.
    """

    key: str = ""
    disabled: bool = False
    cmd_channel: channel_.Key = Field(default=channel_.Key(0), ge=0, le=4294967295)
    state_channel: channel_.Key = Field(default=channel_.Key(0), ge=0, le=4294967295)
    cmd_channel_name: str = ""
    state_channel_name: str = ""
    port: int = Field(default=0, ge=-2147483648, le=2147483647)

    def __hash__(self) -> int:
        return hash(self.key)


class DIChannel(BaseModel):
    """Is a digital input channel the task acquires from.

    Attributes:
        key: Uniquely identifies the channel within the task.
        name: Is the human-readable channel name.
        disabled: Is true when the channel is excluded from acquisition.
        channel: Is the Synnax channel that raw samples are written to.
        port: Is the physical port the channel reads from.
        line: Is the digital line within the port the channel reads from.
    """

    key: str = ""
    name: str = ""
    disabled: bool = False
    channel: channel_.Key = Field(default=channel_.Key(0), ge=0, le=4294967295)
    port: int = Field(default=0, ge=-2147483648, le=2147483647)
    line: int = Field(default=0, ge=-2147483648, le=2147483647)

    def __hash__(self) -> int:
        return hash(self.key)


class DOChannel(BaseModel):
    """Is a digital output channel the task drives.

    Attributes:
        key: Uniquely identifies the channel within the task.
        disabled: Is true when the channel is excluded from the task.
        cmd_channel: Is the Synnax channel commands are read from.
        state_channel: Is the Synnax channel the output state is written to.
        cmd_channel_name: Is the human-readable name of the command channel.
        state_channel_name: Is the human-readable name of the state channel.
        port: Is the physical port the channel writes to.
        line: Is the digital line within the port the channel writes to.
    """

    key: str = ""
    disabled: bool = False
    cmd_channel: channel_.Key = Field(default=channel_.Key(0), ge=0, le=4294967295)
    state_channel: channel_.Key = Field(default=channel_.Key(0), ge=0, le=4294967295)
    cmd_channel_name: str = ""
    state_channel_name: str = ""
    port: int = Field(default=0, ge=-2147483648, le=2147483647)
    line: int = Field(default=0, ge=-2147483648, le=2147483647)

    def __hash__(self) -> int:
        return hash(self.key)


class WriteConfig(task.WriteConfig):
    """Carries the configuration fields shared by NI write tasks.

    Attributes:
        state_rate: Is the rate at which output state is reported to Synnax, in Hertz.
    """

    state_rate: telem.Rate = telem.Rate(10)

    def __hash__(self) -> int:
        return hash(self.key)


class ScanConfig(task.ScanConfig):
    """Configures the NI device scanner task.

    Attributes:
        ignored_models: Are regex patterns matching the device models the scan skips.
    """

    ignored_models: list[str] = Field(default_factory=lambda: ["^cRIO.*", "^nown.*"])

    def __hash__(self) -> int:
        return hash(self.key)


class BuiltInCJC(BaseModel):
    """Reads the reference temperature from the device's own sensor."""

    source: Literal["built_in"] = "built_in"


class ConstValCJC(BaseModel):
    """Uses a fixed reference temperature."""

    source: Literal["const_val"] = "const_val"
    val: float = 0


class ChanCJC(BaseModel):
    """Reads the reference temperature from another channel."""

    source: Literal["chan"] = "chan"
    port: int = Field(default=0, ge=-2147483648, le=2147483647)


# Is the cold-junction compensation for a thermocouple. The source selects where
# the reference temperature comes from.
CJC = Annotated[
    Union[BuiltInCJC, ConstValCJC, ChanCJC],
    Field(discriminator="source"),
]


class LinearScale(BaseModel):
    """Maps raw values to engineering units with a slope and intercept."""

    type: Literal["linear"] = "linear"
    slope: float = 1
    y_intercept: float = 0
    pre_scaled_units: Units = "Volts"
    scaled_units: str = "Volts"


class MapScale(BaseModel):
    """Maps a raw range linearly onto a scaled range."""

    type: Literal["map"] = "map"
    pre_scaled_min: float = 0
    pre_scaled_max: float = 1
    scaled_min: float = 0
    scaled_max: float = 1
    pre_scaled_units: Units = "Volts"
    scaled_units: str = "Volts"


class TableScale(BaseModel):
    """Maps raw values to engineering units via a lookup table."""

    type: Literal["table"] = "table"
    pre_scaled_vals: list[float] = Field(default_factory=list)
    scaled_vals: list[float] = Field(default_factory=list)
    pre_scaled_units: Units = "Volts"
    scaled_units: str = "Volts"


class PolynomialScale(BaseModel):
    """Maps raw values to engineering units with a polynomial."""

    type: Literal["polynomial"] = "polynomial"
    forward_coeffs: list[float] = Field(default_factory=list)
    reverse_coeffs: list[float] = Field(default_factory=list)
    pre_scaled_units: Units = "Volts"
    scaled_units: str = "Volts"


class NoneScale(BaseModel):
    """Applies no scaling; the raw value is used directly."""

    type: Literal["none"] = "none"


# Determines how raw sensor values are transformed to engineering units.
Scale = Annotated[
    Union[LinearScale, MapScale, TableScale, PolynomialScale, NoneScale],
    Field(discriminator="type"),
]


class DigitalReadConfig(task.ReadConfig):
    """Configures an NI digital read task.

    Attributes:
        device: Is the key of the device the task acquires from.
        channels: Are the digital input channels the task acquires.
    """

    device: device_.Key = ""
    channels: list[DIChannel] = Field(default_factory=list)

    def __hash__(self) -> int:
        return hash(self.key)


class DigitalWriteConfig(WriteConfig):
    """Configures an NI digital write task.

    Attributes:
        channels: Are the digital output channels the task drives.
    """

    channels: list[DOChannel] = Field(default_factory=list)

    def __hash__(self) -> int:
        return hash(self.key)


class CustomScale(BaseModel):
    """Applies a user-defined scale to the raw measurement.

    Attributes:
        custom_scale: Is the scale applied to the raw measurement.
    """

    custom_scale: Scale = Field(default_factory=lambda: NoneScale(type="none"))


class AIVoltageChannel(BaseAIChannel, MinMaxVal, Terminal, CustomScale):
    """Reads a voltage."""

    type: Literal["ai_voltage"] = "ai_voltage"


class AIAccelChannel(
    BaseAIChannel, MinMaxVal, Terminal, Sensitivity, CurrentExcitation, CustomScale
):
    """Reads acceleration from an accelerometer."""

    type: Literal["ai_accel"] = "ai_accel"
    units: AccelUnits = "g"
    sensitivity_units: AccelSensitivityUnits = "mVoltsPerG"


class AIBridgeChannel(BaseAIChannel, MinMaxVal, Bridge, VoltageExcitation, CustomScale):
    """Reads a generic bridge-based sensor."""

    type: Literal["ai_bridge"] = "ai_bridge"
    units: ElectricalUnits = "mVoltsPerVolt"


class AICurrentChannel(BaseAIChannel, MinMaxVal, Terminal, CustomScale):
    """Reads a current."""

    type: Literal["ai_current"] = "ai_current"
    shunt_resistor_loc: ShuntResistorLocation = "Default"
    ext_shunt_resistor_val: float = 1


class AIForceBridgeTableChannel(
    BaseAIChannel, MinMaxVal, Bridge, VoltageExcitation, Table, CustomScale
):
    """Reads force from a bridge with a calibration table."""

    type: Literal["ai_force_bridge_table"] = "ai_force_bridge_table"
    units: ForceUnits = "Newtons"
    physical_units: ForceUnits = "Newtons"


class AIForceBridgeTwoPointLinChannel(
    BaseAIChannel, MinMaxVal, Bridge, VoltageExcitation, TwoPointLin, CustomScale
):
    """Reads force from a bridge with two-point linear calibration."""

    type: Literal["ai_force_bridge_two_point_lin"] = "ai_force_bridge_two_point_lin"
    units: ForceUnits = "Newtons"
    physical_units: ForceUnits = "Newtons"


class AIForceIEPEChannel(
    BaseAIChannel, MinMaxVal, Terminal, Sensitivity, CurrentExcitation, CustomScale
):
    """Reads force from an IEPE sensor."""

    type: Literal["ai_force_iepe"] = "ai_force_iepe"
    units: ForceUnits = "Newtons"
    sensitivity_units: ForceSensitivityUnits = "mVoltsPerNewton"


class AIMicrophoneChannel(BaseAIChannel, Terminal, CurrentExcitation, CustomScale):
    """Reads sound pressure from a microphone."""

    type: Literal["ai_microphone"] = "ai_microphone"
    mic_sensitivity: float = 0
    max_snd_press_level: float = 0


class AIPressureBridgeTableChannel(
    BaseAIChannel, MinMaxVal, Bridge, VoltageExcitation, Table, CustomScale
):
    """Reads pressure from a bridge with a calibration table."""

    type: Literal["ai_pressure_bridge_table"] = "ai_pressure_bridge_table"
    units: PressureUnits = "PoundsPerSquareInch"
    physical_units: PressureUnits = "PoundsPerSquareInch"


class AIPressureBridgeTwoPointLinChannel(
    BaseAIChannel, MinMaxVal, Bridge, VoltageExcitation, TwoPointLin, CustomScale
):
    """Reads pressure from a bridge with two-point linear calibration."""

    type: Literal["ai_pressure_bridge_two_point_lin"] = (
        "ai_pressure_bridge_two_point_lin"
    )
    units: PressureUnits = "PoundsPerSquareInch"
    physical_units: PressureUnits = "PoundsPerSquareInch"


class AIResistanceChannel(
    BaseAIChannel, MinMaxVal, Resistance, CurrentExcitation, CustomScale
):
    """Reads a resistance."""

    type: Literal["ai_resistance"] = "ai_resistance"


class AIRTDChannel(BaseAIChannel, MinMaxVal, Resistance, CurrentExcitation):
    """Reads temperature from an RTD."""

    type: Literal["ai_rtd"] = "ai_rtd"
    units: TemperatureUnits = "DegC"
    rtd_type: RTDType = "Pt3750"
    r0: float = 0


class AIStrainGaugeChannel(BaseAIChannel, MinMaxVal, VoltageExcitation, CustomScale):
    """Reads strain from a strain gauge."""

    type: Literal["ai_strain_gauge"] = "ai_strain_gauge"
    strain_config: StrainConfig = "FullBridgeI"
    gage_factor: float = 0
    initial_bridge_voltage: float = 0
    nominal_gage_resistance: float = 0
    poisson_ratio: float = 0
    lead_wire_resistance: float = 0


class AITempBuiltinChannel(BaseAIChannel):
    """Reads temperature from the device's built-in sensor."""

    type: Literal["ai_temp_builtin"] = "ai_temp_builtin"
    units: TemperatureUnits = "DegC"


class AIThermocoupleChannel(BaseAIChannel, MinMaxVal):
    """Reads temperature from a thermocouple."""

    type: Literal["ai_thermocouple"] = "ai_thermocouple"
    units: TemperatureUnits = "DegC"
    thermocouple_type: ThermocoupleType = "J"
    cjc: CJC = Field(default_factory=lambda: BuiltInCJC(source="built_in"))


class AITorqueBridgeTableChannel(
    BaseAIChannel, MinMaxVal, Bridge, VoltageExcitation, Table, CustomScale
):
    """Reads torque from a bridge with a calibration table."""

    type: Literal["ai_torque_bridge_table"] = "ai_torque_bridge_table"
    units: TorqueUnits = "NewtonMeters"
    physical_units: TorqueUnits = "NewtonMeters"


class AITorqueBridgeTwoPointLinChannel(
    BaseAIChannel, MinMaxVal, Bridge, VoltageExcitation, TwoPointLin, CustomScale
):
    """Reads torque from a bridge with two-point linear calibration."""

    type: Literal["ai_torque_bridge_two_point_lin"] = "ai_torque_bridge_two_point_lin"
    units: TorqueUnits = "NewtonMeters"
    physical_units: TorqueUnits = "NewtonMeters"


class AIVelocityIEPEChannel(
    BaseAIChannel, MinMaxVal, Terminal, Sensitivity, CurrentExcitation, CustomScale
):
    """Reads velocity from an IEPE sensor."""

    type: Literal["ai_velocity_iepe"] = "ai_velocity_iepe"
    units: VelocityUnits = "MetersPerSecond"
    sensitivity_units: VelocitySensitivityUnits = "MillivoltsPerMillimeterPerSecond"


class AIAccel4WireDCVoltageChannel(
    BaseAIChannel, MinMaxVal, Terminal, Sensitivity, VoltageExcitation, CustomScale
):
    """Reads acceleration from a 4-wire DC-voltage accelerometer."""

    type: Literal["ai_accel_4_wire_dc_voltage"] = "ai_accel_4_wire_dc_voltage"
    units: AccelUnits = "g"
    sensitivity_units: AccelSensitivityUnits = "mVoltsPerG"
    use_excit_for_scaling: bool = False


class AIAccelChargeChannel(BaseAIChannel, MinMaxVal, Terminal, CustomScale):
    """Reads acceleration from a charge-mode accelerometer."""

    type: Literal["ai_accel_charge"] = "ai_accel_charge"
    units: AccelUnits = "g"
    sensitivity: float = 0
    sensitivity_units: AccelSensitivityUnits = "mVoltsPerG"


class AIChargeChannel(BaseAIChannel, MinMaxVal, Terminal, CustomScale):
    """Reads a raw charge."""

    type: Literal["ai_charge"] = "ai_charge"
    units: ChargeUnits = "Coulombs"


class AICurrentRMSChannel(BaseAIChannel, MinMaxVal, Terminal, CustomScale):
    """Reads RMS current."""

    type: Literal["ai_current_rms"] = "ai_current_rms"
    shunt_resistor_loc: ShuntResistorLocation = "Default"
    ext_shunt_resistor_val: float = 1


class AIForceBridgePolynomialChannel(
    BaseAIChannel, MinMaxVal, Bridge, VoltageExcitation, BridgePolynomial, CustomScale
):
    """Reads force from a bridge with polynomial calibration."""

    type: Literal["ai_force_bridge_polynomial"] = "ai_force_bridge_polynomial"
    units: ForceUnits = "Newtons"
    physical_units: ForceUnits = "Newtons"


class AIFreqVoltageChannel(BaseAIChannel, MinMaxVal, CustomScale):
    """Reads frequency from a voltage signal."""

    type: Literal["ai_freq_voltage"] = "ai_freq_voltage"
    threshold_level: float = 0
    hysteresis: float = 0


class AIPressureBridgePolynomialChannel(
    BaseAIChannel, MinMaxVal, Bridge, VoltageExcitation, BridgePolynomial, CustomScale
):
    """Reads pressure from a bridge with polynomial calibration."""

    type: Literal["ai_pressure_bridge_polynomial"] = "ai_pressure_bridge_polynomial"
    units: PressureUnits = "PoundsPerSquareInch"
    physical_units: PressureUnits = "PoundsPerSquareInch"


class AIThermistorIexChannel(BaseAIChannel, MinMaxVal, Resistance, CurrentExcitation):
    """Reads temperature from a current-excited thermistor."""

    type: Literal["ai_thermistor_iex"] = "ai_thermistor_iex"
    units: TemperatureUnits = "DegC"
    a: float = 0
    b: float = 0
    c: float = 0


class AIThermistorVexChannel(BaseAIChannel, MinMaxVal, Resistance, VoltageExcitation):
    """Reads temperature from a voltage-excited thermistor."""

    type: Literal["ai_thermistor_vex"] = "ai_thermistor_vex"
    units: TemperatureUnits = "DegC"
    a: float = 0
    b: float = 0
    c: float = 0
    r1: float = 0


class AITorqueBridgePolynomialChannel(
    BaseAIChannel, MinMaxVal, Bridge, VoltageExcitation, BridgePolynomial, CustomScale
):
    """Reads torque from a bridge with polynomial calibration."""

    type: Literal["ai_torque_bridge_polynomial"] = "ai_torque_bridge_polynomial"
    units: TorqueUnits = "NewtonMeters"
    physical_units: TorqueUnits = "NewtonMeters"


class AIVoltageRMSChannel(BaseAIChannel, MinMaxVal, Terminal, CustomScale):
    """Reads RMS voltage."""

    type: Literal["ai_voltage_rms"] = "ai_voltage_rms"


class AIVoltageWithExcitChannel(
    BaseAIChannel, MinMaxVal, Terminal, VoltageExcitation, CustomScale
):
    """Reads a voltage with excitation."""

    type: Literal["ai_voltage_with_excit"] = "ai_voltage_with_excit"
    bridge_config: BridgeConfig = "FullBridge"
    use_excit_for_scaling: bool = False


# Is a single NI analog input channel. The type field selects the measurement mode
# and the fields that accompany it.
AIChannel = Annotated[
    Union[
        AIVoltageChannel,
        AIAccelChannel,
        AIBridgeChannel,
        AICurrentChannel,
        AIForceBridgeTableChannel,
        AIForceBridgeTwoPointLinChannel,
        AIForceIEPEChannel,
        AIMicrophoneChannel,
        AIPressureBridgeTableChannel,
        AIPressureBridgeTwoPointLinChannel,
        AIResistanceChannel,
        AIRTDChannel,
        AIStrainGaugeChannel,
        AITempBuiltinChannel,
        AIThermocoupleChannel,
        AITorqueBridgeTableChannel,
        AITorqueBridgeTwoPointLinChannel,
        AIVelocityIEPEChannel,
        AIAccel4WireDCVoltageChannel,
        AIAccelChargeChannel,
        AIChargeChannel,
        AICurrentRMSChannel,
        AIForceBridgePolynomialChannel,
        AIFreqVoltageChannel,
        AIPressureBridgePolynomialChannel,
        AIThermistorIexChannel,
        AIThermistorVexChannel,
        AITorqueBridgePolynomialChannel,
        AIVoltageRMSChannel,
        AIVoltageWithExcitChannel,
    ],
    Field(discriminator="type"),
]


class CIFrequencyChannel(BaseCIChannel, CustomScale):
    """Measures signal frequency."""

    type: Literal["ci_frequency"] = "ci_frequency"
    min_val: float = 2
    max_val: float = 100
    units: CIFreqUnits = "Hz"
    edge: CIEdge = "Rising"
    meas_method: CIMeasMethod = "DynamicAvg"
    meas_time: float = 0.000006
    divisor: int = Field(default=4, ge=-2147483648, le=2147483647)
    terminal: str = ""


class CIEdgeCountChannel(BaseCIChannel):
    """Counts signal edges."""

    type: Literal["ci_edge_count"] = "ci_edge_count"
    active_edge: CIEdge = "Rising"
    count_direction: CICountDirection = "CountUp"
    initial_count: float = 0
    terminal: str = ""


class CIPeriodChannel(BaseCIChannel, CustomScale):
    """Measures signal period."""

    type: Literal["ci_period"] = "ci_period"
    min_val: float = 0.000001
    max_val: float = 0.100000
    units: CITimeUnits = "Seconds"
    starting_edge: CIEdge = "Rising"
    meas_method: CIMeasMethod = "DynamicAvg"
    meas_time: float = 0.001000
    divisor: int = Field(default=4, ge=-2147483648, le=2147483647)
    terminal: str = ""


class CIPulseWidthChannel(BaseCIChannel, CustomScale):
    """Measures pulse width."""

    type: Literal["ci_pulse_width"] = "ci_pulse_width"
    min_val: float = 0.000001
    max_val: float = 0.100000
    units: CITimeUnits = "Seconds"
    starting_edge: CIEdge = "Rising"
    terminal: str = ""


class CISemiPeriodChannel(BaseCIChannel, CustomScale):
    """Measures semi-period."""

    type: Literal["ci_semi_period"] = "ci_semi_period"
    min_val: float = 0.000001
    max_val: float = 0.100000
    units: CITimeUnits = "Seconds"
    terminal: str = ""


class CITwoEdgeSepChannel(BaseCIChannel, CustomScale):
    """Measures the separation between two edges."""

    type: Literal["ci_two_edge_sep"] = "ci_two_edge_sep"
    min_val: float = 0.000001
    max_val: float = 1
    units: CITimeUnits = "Seconds"
    first_edge: CIEdge = "Rising"
    second_edge: CIEdge = "Falling"
    first_terminal: str = ""
    second_terminal: str = ""


class CIVelocityLinearChannel(BaseCIChannel, CustomScale):
    """Measures linear velocity from an encoder."""

    type: Literal["ci_velocity_linear"] = "ci_velocity_linear"
    min_val: float = 0
    max_val: float = 1
    units: CILinearVelocityUnits = "m/s"
    decoding_type: CIDecodingType = "X4"
    dist_per_pulse: float = 0.001000
    terminal_a: str = ""
    terminal_b: str = ""


class CIVelocityAngularChannel(BaseCIChannel, CustomScale):
    """Measures angular velocity from an encoder."""

    type: Literal["ci_velocity_angular"] = "ci_velocity_angular"
    min_val: float = 0
    max_val: float = 1
    units: CIAngularVelocityUnits = "RPM"
    decoding_type: CIDecodingType = "X4"
    pulses_per_rev: float = 24
    terminal_a: str = ""
    terminal_b: str = ""


class CIPositionLinearChannel(BaseCIChannel, CustomScale, ZIndex):
    """Measures linear position from an encoder."""

    type: Literal["ci_position_linear"] = "ci_position_linear"
    units: CILinearPositionUnits = "Meters"
    decoding_type: CIDecodingType = "X4"
    dist_per_pulse: float = 0.000001
    initial_pos: float = 0
    terminal_a: str = ""
    terminal_b: str = ""


class CIPositionAngularChannel(BaseCIChannel, CustomScale, ZIndex):
    """Measures angular position from an encoder."""

    type: Literal["ci_position_angular"] = "ci_position_angular"
    units: CIAngularPositionUnits = "Degrees"
    decoding_type: CIDecodingType = "X4"
    pulses_per_rev: float = 24
    initial_angle: float = 0
    terminal_a: str = ""
    terminal_b: str = ""


class CIDutyCycleChannel(BaseCIChannel, CustomScale):
    """Measures the duty cycle of a signal."""

    type: Literal["ci_duty_cycle"] = "ci_duty_cycle"
    min_val: float = 2
    max_val: float = 10000
    active_edge: CIEdge = "Rising"
    terminal: str = ""


# Is a single NI counter input channel. The type field selects the measurement
# mode and the fields that accompany it.
CIChannel = Annotated[
    Union[
        CIFrequencyChannel,
        CIEdgeCountChannel,
        CIPeriodChannel,
        CIPulseWidthChannel,
        CISemiPeriodChannel,
        CITwoEdgeSepChannel,
        CIVelocityLinearChannel,
        CIVelocityAngularChannel,
        CIPositionLinearChannel,
        CIPositionAngularChannel,
        CIDutyCycleChannel,
    ],
    Field(discriminator="type"),
]


class AOCurrentChannel(BaseAOChannel, MinMaxVal, CustomScale):
    """Drives a current output."""

    type: Literal["ao_current"] = "ao_current"


class AOFuncGenChannel(BaseAOChannel):
    """Drives a function-generator output."""

    type: Literal["ao_func_gen"] = "ao_func_gen"
    wave_type: WaveType = "Sine"
    frequency: float = 0
    amplitude: float = 0
    offset: float = 0


class AOVoltageChannel(BaseAOChannel, MinMaxVal, CustomScale):
    """Drives a voltage output."""

    type: Literal["ao_voltage"] = "ao_voltage"


# Is a single NI analog output channel. The type field selects the output mode and
# the fields that accompany it.
AOChannel = Annotated[
    Union[AOCurrentChannel, AOFuncGenChannel, AOVoltageChannel],
    Field(discriminator="type"),
]


class AnalogReadConfig(task.ReadConfig):
    """Configures an NI analog read task. Each channel carries its own device.

    Attributes:
        channels: Are the analog input channels the task acquires.
    """

    channels: list[AIChannel] = Field(default_factory=list)

    def __hash__(self) -> int:
        return hash(self.key)


class CounterReadConfig(task.ReadConfig):
    """Configures an NI counter read task. Each channel carries its own device.

    Attributes:
        channels: Are the counter input channels the task acquires.
    """

    channels: list[CIChannel] = Field(default_factory=list)

    def __hash__(self) -> int:
        return hash(self.key)


class AnalogWriteConfig(WriteConfig):
    """Configures an NI analog write task.

    Attributes:
        channels: Are the analog output channels the task drives.
    """

    channels: list[AOChannel] = Field(default_factory=list)

    def __hash__(self) -> int:
        return hash(self.key)
