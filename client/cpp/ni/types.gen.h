// Copyright 2026 Synnax Labs, Inc.
//
// Use of this software is governed by the Business Source License included in the file
// licenses/BSL.txt.
//
// As of the Change Date specified in that file, in accordance with the Business Source
// License, use of this software will be governed by the Apache License, Version 2.0,
// included in the file licenses/APL.txt.

// Code generated by oracle. DO NOT EDIT.

#pragma once

#include <cstdint>
#include <string>
#include <variant>
#include <vector>

#include "client/cpp/channel/types.gen.h"
#include "client/cpp/device/types.gen.h"
#include "client/cpp/task/common/types.gen.h"
#include "x/cpp/json/json.h"
#include "x/cpp/telem/types.gen.h"

namespace synnax::ni {

struct NoneScale;
struct MinMaxVal;
struct Sensitivity;
struct BaseAIChannel;
struct BaseCIChannel;
struct BaseAOChannel;
struct WriteConfig;
struct LinearScale;
struct MapScale;
struct TableScale;
struct PolynomialScale;
struct BridgePolynomial;
struct Table;
struct TwoPointLin;
struct CurrentExcitation;
struct VoltageExcitation;
struct Terminal;
struct Bridge;
struct Resistance;
struct ZIndex;
struct DigitalReadConfig;
struct DigitalWriteConfig;
struct CustomScale;
struct AnalogReadConfig;
struct CounterReadConfig;
struct AnalogWriteConfig;

constexpr const char *UNITS_VOLTS = "Volts";
constexpr const char *UNITS_AMPS = "Amps";
constexpr const char *UNITS_DEG_F = "DegF";
constexpr const char *UNITS_DEG_C = "DegC";
constexpr const char *UNITS_DEG_R = "DegR";
constexpr const char *UNITS_KELVINS = "Kelvins";
constexpr const char *UNITS_STRAIN = "Strain";
constexpr const char *UNITS_OHMS = "Ohms";
constexpr const char *UNITS_HZ = "Hz";
constexpr const char *UNITS_SECONDS = "Seconds";
constexpr const char *UNITS_METERS = "Meters";
constexpr const char *UNITS_INCHES = "Inches";
constexpr const char *UNITS_DEGREES = "Degrees";
constexpr const char *UNITS_RADIANS = "Radians";
constexpr const char *UNITS_G = "g";
constexpr const char *UNITS_METERS_PER_SECOND_SQUARED = "MetersPerSecondSquared";
constexpr const char *UNITS_NEWTONS = "Newtons";
constexpr const char *UNITS_POUNDS = "Pounds";
constexpr const char *UNITS_KILOGRAM_FORCE = "KilogramForce";
constexpr const char *UNITS_POUNDS_PER_SQUARE_INCH = "PoundsPerSquareInch";
constexpr const char *UNITS_BAR = "Bar";
constexpr const char *UNITS_PASCALS = "Pascals";
constexpr const char *UNITS_VOLTS_PER_VOLT = "VoltsPerVolt";
constexpr const char *UNITS_M_VOLTS_PER_VOLT = "mVoltsPerVolt";
constexpr const char *UNITS_NEWTON_METERS = "NewtonMeters";
constexpr const char *UNITS_INCH_POUNDS = "InchPounds";
constexpr const char *UNITS_INCH_OUNCES = "InchOunces";
constexpr const char *UNITS_FOOT_POUNDS = "FootPounds";

constexpr const char *TERMINAL_CONFIG_CFG_DEFAULT = "Cfg_Default";
constexpr const char *TERMINAL_CONFIG_RSE = "RSE";
constexpr const char *TERMINAL_CONFIG_NRSE = "NRSE";
constexpr const char *TERMINAL_CONFIG_DIFF = "Diff";
constexpr const char *TERMINAL_CONFIG_PSEUDO_DIFF = "PseudoDiff";

constexpr const char *EXCITATION_SOURCE_INTERNAL = "Internal";
constexpr const char *EXCITATION_SOURCE_EXTERNAL = "External";
constexpr const char *EXCITATION_SOURCE_NONE = "None";

constexpr const char *BRIDGE_CONFIG_FULL_BRIDGE = "FullBridge";
constexpr const char *BRIDGE_CONFIG_HALF_BRIDGE = "HalfBridge";
constexpr const char *BRIDGE_CONFIG_QUARTER_BRIDGE = "QuarterBridge";

constexpr const char *ELECTRICAL_UNITS_M_VOLTS_PER_VOLT = "mVoltsPerVolt";
constexpr const char *ELECTRICAL_UNITS_VOLTS_PER_VOLT = "VoltsPerVolt";

constexpr const char *RESISTANCE_CONFIG_WIRE_2 = "2Wire";
constexpr const char *RESISTANCE_CONFIG_WIRE_3 = "3Wire";
constexpr const char *RESISTANCE_CONFIG_WIRE_4 = "4Wire";

constexpr const char *Z_INDEX_PHASE_A_HIGH_B_HIGH = "AHighBHigh";
constexpr const char *Z_INDEX_PHASE_A_HIGH_B_LOW = "AHighBLow";
constexpr const char *Z_INDEX_PHASE_A_LOW_B_HIGH = "ALowBHigh";
constexpr const char *Z_INDEX_PHASE_A_LOW_B_LOW = "ALowBLow";

constexpr const char *ACCEL_SENSITIVITY_UNITS_M_VOLTS_PER_G = "mVoltsPerG";
constexpr const char *ACCEL_SENSITIVITY_UNITS_VOLTS_PER_G = "VoltsPerG";

constexpr const char *ACCEL_UNITS_G = "g";
constexpr const char *ACCEL_UNITS_METERS_PER_SECOND_SQUARED = "MetersPerSecondSquared";
constexpr const char *ACCEL_UNITS_INCHES_PER_SECOND_SQUARED = "InchesPerSecondSquared";

constexpr const char *FORCE_UNITS_NEWTONS = "Newtons";
constexpr const char *FORCE_UNITS_POUNDS = "Pounds";
constexpr const char *FORCE_UNITS_KILOGRAM_FORCE = "KilogramForce";

constexpr const char *SHUNT_RESISTOR_LOCATION_DEFAULT = "Default";
constexpr const char *SHUNT_RESISTOR_LOCATION_INTERNAL = "Internal";
constexpr const char *SHUNT_RESISTOR_LOCATION_EXTERNAL = "External";

constexpr const char *FORCE_SENSITIVITY_UNITS_M_VOLTS_PER_NEWTON = "mVoltsPerNewton";
constexpr const char *FORCE_SENSITIVITY_UNITS_M_VOLTS_PER_POUND = "mVoltsPerPound";

constexpr const char *PRESSURE_UNITS_POUNDS_PER_SQUARE_INCH = "PoundsPerSquareInch";
constexpr const char *PRESSURE_UNITS_PASCALS = "Pascals";
constexpr const char *PRESSURE_UNITS_BAR = "Bar";

constexpr const char *TORQUE_UNITS_NEWTON_METERS = "NewtonMeters";
constexpr const char *TORQUE_UNITS_INCH_OUNCES = "InchOunces";
constexpr const char *TORQUE_UNITS_INCH_POUNDS = "InchPounds";
constexpr const char *TORQUE_UNITS_FOOT_POUNDS = "FootPounds";

constexpr const char *TEMPERATURE_UNITS_DEG_C = "DegC";
constexpr const char *TEMPERATURE_UNITS_DEG_F = "DegF";
constexpr const char *TEMPERATURE_UNITS_KELVINS = "Kelvins";
constexpr const char *TEMPERATURE_UNITS_DEG_R = "DegR";

constexpr const char *RTD_TYPE_PT_3750 = "Pt3750";
constexpr const char *RTD_TYPE_PT_3851 = "Pt3851";
constexpr const char *RTD_TYPE_PT_3911 = "Pt3911";
constexpr const char *RTD_TYPE_PT_3916 = "Pt3916";
constexpr const char *RTD_TYPE_PT_3920 = "Pt3920";
constexpr const char *RTD_TYPE_PT_3928 = "Pt3928";

constexpr const char *STRAIN_CONFIG_FULL_BRIDGE_I = "FullBridgeI";
constexpr const char *STRAIN_CONFIG_FULL_BRIDGE_II = "FullBridgeII";
constexpr const char *STRAIN_CONFIG_FULL_BRIDGE_III = "FullBridgeIII";
constexpr const char *STRAIN_CONFIG_HALF_BRIDGE_I = "HalfBridgeI";
constexpr const char *STRAIN_CONFIG_HALF_BRIDGE_II = "HalfBridgeII";
constexpr const char *STRAIN_CONFIG_QUARTER_BRIDGE_I = "QuarterBridgeI";
constexpr const char *STRAIN_CONFIG_QUARTER_BRIDGE_II = "QuarterBridgeII";

constexpr const char *THERMOCOUPLE_TYPE_J = "J";
constexpr const char *THERMOCOUPLE_TYPE_K = "K";
constexpr const char *THERMOCOUPLE_TYPE_N = "N";
constexpr const char *THERMOCOUPLE_TYPE_R = "R";
constexpr const char *THERMOCOUPLE_TYPE_S = "S";
constexpr const char *THERMOCOUPLE_TYPE_T = "T";
constexpr const char *THERMOCOUPLE_TYPE_B = "B";
constexpr const char *THERMOCOUPLE_TYPE_E = "E";

constexpr const char *VELOCITY_UNITS_METERS_PER_SECOND = "MetersPerSecond";
constexpr const char *VELOCITY_UNITS_INCHES_PER_SECOND = "InchesPerSecond";

constexpr const char *VELOCITY_SENSITIVITY_UNITS_M_VOLTS_PER_MM_PER_SECOND =
    "MillivoltsPerMillimeterPerSecond";
constexpr const char *VELOCITY_SENSITIVITY_UNITS_M_VOLTS_PER_INCH_PER_SECOND =
    "MilliVoltsPerInchPerSecond";

constexpr const char *CHARGE_UNITS_COULOMBS = "Coulombs";
constexpr const char *CHARGE_UNITS_PICO_COULOMBS = "PicoCoulombs";

constexpr const char *CI_EDGE_RISING = "Rising";
constexpr const char *CI_EDGE_FALLING = "Falling";

constexpr const char *CI_MEAS_METHOD_LOW_FREQ_1_CTR = "LowFreq1Ctr";
constexpr const char *CI_MEAS_METHOD_HIGH_FREQ_2_CTR = "HighFreq2Ctr";
constexpr const char *CI_MEAS_METHOD_LARGE_RNG_2_CTR = "LargeRng2Ctr";
constexpr const char *CI_MEAS_METHOD_DYNAMIC_AVG = "DynamicAvg";

constexpr const char *CI_FREQ_UNITS_HZ = "Hz";
constexpr const char *CI_FREQ_UNITS_TICKS = "Ticks";

constexpr const char *CI_TIME_UNITS_SECONDS = "Seconds";
constexpr const char *CI_TIME_UNITS_TICKS = "Ticks";

constexpr const char *CI_COUNT_DIRECTION_COUNT_UP = "CountUp";
constexpr const char *CI_COUNT_DIRECTION_COUNT_DOWN = "CountDown";
constexpr const char *CI_COUNT_DIRECTION_EXTERNALLY_CONTROLLED = "ExternallyControlled";

constexpr const char *CI_DECODING_TYPE_X_1 = "X1";
constexpr const char *CI_DECODING_TYPE_X_2 = "X2";
constexpr const char *CI_DECODING_TYPE_X_4 = "X4";
constexpr const char *CI_DECODING_TYPE_TWO_PULSE = "TwoPulse";

constexpr const char *CI_LINEAR_VELOCITY_UNITS_M_PER_S = "m/s";
constexpr const char *CI_LINEAR_VELOCITY_UNITS_IN_PER_S = "in/s";

constexpr const char *CI_ANGULAR_VELOCITY_UNITS_RPM = "RPM";
constexpr const char *CI_ANGULAR_VELOCITY_UNITS_RADIANS_PER_SECOND = "Radians/s";
constexpr const char *CI_ANGULAR_VELOCITY_UNITS_DEGREES_PER_SECOND = "Degrees/s";

constexpr const char *CI_LINEAR_POSITION_UNITS_METERS = "Meters";
constexpr const char *CI_LINEAR_POSITION_UNITS_INCHES = "Inches";
constexpr const char *CI_LINEAR_POSITION_UNITS_TICKS = "Ticks";

constexpr const char *CI_ANGULAR_POSITION_UNITS_DEGREES = "Degrees";
constexpr const char *CI_ANGULAR_POSITION_UNITS_RADIANS = "Radians";
constexpr const char *CI_ANGULAR_POSITION_UNITS_TICKS = "Ticks";

constexpr const char *WAVE_TYPE_SINE = "Sine";
constexpr const char *WAVE_TYPE_TRIANGLE = "Triangle";
constexpr const char *WAVE_TYPE_SQUARE = "Square";
constexpr const char *WAVE_TYPE_SAWTOOTH = "Sawtooth";

/// @brief NoneScale applies no scaling; the raw value is used directly.
struct NoneScale {

    static NoneScale parse(x::json::Parser parser);
    [[nodiscard]] x::json::json to_json() const;
};

/// @brief MinMaxVal bounds the expected signal range in scaled units.
struct MinMaxVal {
    /// @brief min_val is the minimum expected value, in scaled units.
    double min_val = 0;
    /// @brief max_val is the maximum expected value, in scaled units.
    double max_val = 1;

    static MinMaxVal parse(x::json::Parser parser);
    [[nodiscard]] x::json::json to_json() const;
};

/// @brief Sensitivity specifies a sensor's sensitivity rating.
struct Sensitivity {
    /// @brief sensitivity is the sensor's sensitivity rating.
    double sensitivity = 0;

    static Sensitivity parse(x::json::Parser parser);
    [[nodiscard]] x::json::json to_json() const;
};

/// @brief BaseAIChannel carries the fields every NI analog input channel shares.
struct BaseAIChannel {
    /// @brief key uniquely identifies the channel within the task.
    std::string key = "";
    /// @brief name is the human-readable channel name.
    std::string name = "";
    /// @brief disabled is true when the channel is excluded from acquisition.
    bool disabled = false;
    /// @brief channel is the Synnax channel that raw samples are written to.
    ::synnax::channel::Key channel = ::synnax::channel::Key(0);
    /// @brief port is the physical port the channel reads from.
    std::int32_t port = 0;
    /// @brief device is the key of the device the channel belongs to.
    ::synnax::device::Key device = "";

    static BaseAIChannel parse(x::json::Parser parser);
    [[nodiscard]] x::json::json to_json() const;
};

/// @brief BaseCIChannel carries the fields every NI counter input channel shares.
struct BaseCIChannel {
    /// @brief key uniquely identifies the channel within the task.
    std::string key = "";
    /// @brief name is the human-readable channel name.
    std::string name = "";
    /// @brief disabled is true when the channel is excluded from acquisition.
    bool disabled = false;
    /// @brief channel is the Synnax channel that raw samples are written to.
    ::synnax::channel::Key channel = ::synnax::channel::Key(0);
    /// @brief port is the physical counter the channel uses.
    std::int32_t port = 0;
    /// @brief device is the key of the device the counter belongs to.
    ::synnax::device::Key device = "";

    static BaseCIChannel parse(x::json::Parser parser);
    [[nodiscard]] x::json::json to_json() const;
};

/// @brief BaseAOChannel carries the fields every NI analog output channel shares.
struct BaseAOChannel {
    /// @brief key uniquely identifies the channel within the task.
    std::string key = "";
    /// @brief disabled is true when the channel is excluded from the task.
    bool disabled = false;
    /// @brief cmd_channel is the Synnax channel commands are read from.
    ::synnax::channel::Key cmd_channel = ::synnax::channel::Key(0);
    /// @brief state_channel is the Synnax channel the output state is written to.
    ::synnax::channel::Key state_channel = ::synnax::channel::Key(0);
    /// @brief cmd_channel_name is the human-readable name of the command channel.
    std::string cmd_channel_name = "";
    /// @brief state_channel_name is the human-readable name of the state channel.
    std::string state_channel_name = "";
    /// @brief port is the physical port the channel writes to.
    std::int32_t port = 0;

    static BaseAOChannel parse(x::json::Parser parser);
    [[nodiscard]] x::json::json to_json() const;
};

/// @brief WriteConfig carries the configuration fields shared by NI write tasks.
struct WriteConfig : public ::synnax::common::BaseWriteConfig {
    /// @brief state_rate is the rate at which output state is reported to Synnax, in
    /// hertz.
    ::x::telem::Rate state_rate = ::x::telem::Rate(10);

    static WriteConfig parse(x::json::Parser parser);
    [[nodiscard]] x::json::json to_json() const;
};

/// @brief CJCBuiltIn reads the reference temperature from the device's own sensor.
struct CJCBuiltIn {
    std::string source = "built_in";

    static CJCBuiltIn parse(x::json::Parser parser);
    [[nodiscard]] x::json::json to_json() const;
};

/// @brief CJCConstVal uses a fixed reference temperature.
struct CJCConstVal {
    std::string source = "const_val";
    /// @brief val is the reference temperature, in the channel's units.
    double val = 0;

    static CJCConstVal parse(x::json::Parser parser);
    [[nodiscard]] x::json::json to_json() const;
};

/// @brief CJCChan reads the reference temperature from another channel.
struct CJCChan {
    std::string source = "chan";
    /// @brief port is the port of the channel that measures the reference.
    std::int32_t port = 0;

    static CJCChan parse(x::json::Parser parser);
    [[nodiscard]] x::json::json to_json() const;
};

/// @brief CJC is the cold-junction compensation for a thermocouple. The source selects
/// where the reference temperature comes from.
using CJC = std::variant<CJCBuiltIn, CJCConstVal, CJCChan>;

CJC parse_cjc(x::json::Parser parser);
[[nodiscard]] x::json::json to_json(const CJC &value);

/// @brief DigitalInputChannel carries the fields of an NI digital input channel.
struct DigitalInputChannel {
    std::string type = "digital_input";
    /// @brief key uniquely identifies the channel within the task.
    std::string key = "";
    /// @brief name is the human-readable channel name.
    std::string name = "";
    /// @brief disabled is true when the channel is excluded from acquisition.
    bool disabled = false;
    /// @brief channel is the Synnax channel that raw samples are written to.
    ::synnax::channel::Key channel = ::synnax::channel::Key(0);
    /// @brief port is the physical port the channel reads from.
    std::int32_t port = 0;
    /// @brief line is the digital line within the port the channel reads from.
    std::int32_t line = 0;

    static DigitalInputChannel parse(x::json::Parser parser);
    [[nodiscard]] x::json::json to_json() const;
};

/// @brief DIChannel is a digital input channel the task acquires from.
using DIChannel = std::variant<DigitalInputChannel>;

DIChannel parse_di_channel(x::json::Parser parser);
[[nodiscard]] x::json::json to_json(const DIChannel &value);

/// @brief DOChannelDigitalOutput carries the fields of an NI digital output channel.
struct DOChannelDigitalOutput {
    std::string type = "digital_output";
    /// @brief key uniquely identifies the channel within the task.
    std::string key = "";
    /// @brief disabled is true when the channel is excluded from the task.
    bool disabled = false;
    /// @brief cmd_channel is the Synnax channel commands are read from.
    ::synnax::channel::Key cmd_channel = ::synnax::channel::Key(0);
    /// @brief state_channel is the Synnax channel the output state is written to.
    ::synnax::channel::Key state_channel = ::synnax::channel::Key(0);
    /// @brief cmd_channel_name is the human-readable name of the command channel.
    std::string cmd_channel_name = "";
    /// @brief state_channel_name is the human-readable name of the state channel.
    std::string state_channel_name = "";
    /// @brief port is the physical port the channel writes to.
    std::int32_t port = 0;
    /// @brief line is the digital line within the port the channel writes to.
    std::int32_t line = 0;

    static DOChannelDigitalOutput parse(x::json::Parser parser);
    [[nodiscard]] x::json::json to_json() const;
};

/// @brief DOChannel is a digital output channel the task drives.
using DOChannel = std::variant<DOChannelDigitalOutput>;

DOChannel parse_do_channel(x::json::Parser parser);
[[nodiscard]] x::json::json to_json(const DOChannel &value);

/// @brief LinearScale maps raw values to engineering units with a slope and intercept.
struct LinearScale {
    /// @brief slope is the multiplier applied to the raw value.
    double slope = 1;
    /// @brief y_intercept is the offset added after scaling.
    double y_intercept = 0;
    /// @brief pre_scaled_units are the units of the raw value before scaling.
    std::string pre_scaled_units = UNITS_VOLTS;
    /// @brief scaled_units are the units of the value after scaling.
    std::string scaled_units = "Volts";

    static LinearScale parse(x::json::Parser parser);
    [[nodiscard]] x::json::json to_json() const;
};

/// @brief MapScale maps a raw range linearly onto a scaled range.
struct MapScale {
    /// @brief pre_scaled_min is the lower bound of the raw input range.
    double pre_scaled_min = 0;
    /// @brief pre_scaled_max is the upper bound of the raw input range.
    double pre_scaled_max = 1;
    /// @brief scaled_min is the lower bound of the scaled output range.
    double scaled_min = 0;
    /// @brief scaled_max is the upper bound of the scaled output range.
    double scaled_max = 1;
    /// @brief pre_scaled_units are the units of the raw value before scaling.
    std::string pre_scaled_units = UNITS_VOLTS;
    /// @brief scaled_units are the units of the value after scaling.
    std::string scaled_units = "Volts";

    static MapScale parse(x::json::Parser parser);
    [[nodiscard]] x::json::json to_json() const;
};

/// @brief TableScale maps raw values to engineering units via a lookup table.
struct TableScale {
    /// @brief pre_scaled_vals are the raw breakpoints, monotonically increasing.
    std::vector<double> pre_scaled_vals = {};
    /// @brief scaled_vals are the engineering-unit values at each breakpoint.
    std::vector<double> scaled_vals = {};
    /// @brief pre_scaled_units are the units of the raw values before scaling.
    std::string pre_scaled_units = UNITS_VOLTS;
    /// @brief scaled_units are the units of the values after scaling.
    std::string scaled_units = "Volts";

    static TableScale parse(x::json::Parser parser);
    [[nodiscard]] x::json::json to_json() const;
};

/// @brief PolynomialScale maps raw values to engineering units with a polynomial.
struct PolynomialScale {
    /// @brief forward_coeffs are the coefficients mapping pre-scaled to scaled values.
    std::vector<double> forward_coeffs = {};
    /// @brief reverse_coeffs are the coefficients mapping scaled to pre-scaled values.
    std::vector<double> reverse_coeffs = {};
    /// @brief pre_scaled_units are the units of the raw value before scaling.
    std::string pre_scaled_units = UNITS_VOLTS;
    /// @brief scaled_units are the units of the value after scaling.
    std::string scaled_units = "Volts";

    static PolynomialScale parse(x::json::Parser parser);
    [[nodiscard]] x::json::json to_json() const;
};

/// @brief BridgePolynomial scales bridge electrical output to physical units with a
/// polynomial.
struct BridgePolynomial {
    /// @brief forward_coeffs are the coefficients mapping electrical to physical
    /// values.
    std::vector<double> forward_coeffs = {};
    /// @brief reverse_coeffs are the coefficients mapping physical to electrical
    /// values.
    std::vector<double> reverse_coeffs = {};
    /// @brief electrical_units are the electrical output units of the bridge.
    std::string electrical_units = ELECTRICAL_UNITS_M_VOLTS_PER_VOLT;

    static BridgePolynomial parse(x::json::Parser parser);
    [[nodiscard]] x::json::json to_json() const;
};

/// @brief Table scales bridge electrical output to physical units with a lookup table.
struct Table {
    /// @brief electrical_vals are the electrical breakpoints of the calibration table.
    std::vector<double> electrical_vals = {};
    /// @brief electrical_units are the units of the electrical breakpoints.
    std::string electrical_units = ELECTRICAL_UNITS_M_VOLTS_PER_VOLT;
    /// @brief physical_vals are the physical values corresponding to each electrical
    /// breakpoint.
    std::vector<double> physical_vals = {};

    static Table parse(x::json::Parser parser);
    [[nodiscard]] x::json::json to_json() const;
};

/// @brief TwoPointLin scales bridge electrical output to physical units between two
/// calibration points.
struct TwoPointLin {
    /// @brief first_electrical_val is the first electrical calibration point.
    double first_electrical_val = 0;
    /// @brief second_electrical_val is the second electrical calibration point.
    double second_electrical_val = 1;
    /// @brief electrical_units are the units of the electrical calibration points.
    std::string electrical_units = ELECTRICAL_UNITS_M_VOLTS_PER_VOLT;
    /// @brief first_physical_val is the physical value at the first electrical point.
    double first_physical_val = 0;
    /// @brief second_physical_val is the physical value at the second electrical point.
    double second_physical_val = 1;

    static TwoPointLin parse(x::json::Parser parser);
    [[nodiscard]] x::json::json to_json() const;
};

/// @brief CurrentExcitation configures the current excitation supplied to a sensor.
struct CurrentExcitation {
    /// @brief current_excit_source selects the source of the current excitation signal.
    std::string current_excit_source = EXCITATION_SOURCE_INTERNAL;
    /// @brief current_excit_val is the current excitation level, in amps.
    double current_excit_val = 0;

    static CurrentExcitation parse(x::json::Parser parser);
    [[nodiscard]] x::json::json to_json() const;
};

/// @brief VoltageExcitation configures the voltage excitation supplied to a sensor.
struct VoltageExcitation {
    /// @brief voltage_excit_source selects the source of the voltage excitation signal.
    std::string voltage_excit_source = EXCITATION_SOURCE_INTERNAL;
    /// @brief voltage_excit_val is the voltage excitation level, in volts.
    double voltage_excit_val = 0;

    static VoltageExcitation parse(x::json::Parser parser);
    [[nodiscard]] x::json::json to_json() const;
};

/// @brief Terminal configures the terminal wiring of an analog input channel.
struct Terminal {
    /// @brief terminal_config selects the terminal configuration (RSE, NRSE, Diff,
    /// ...).
    std::string terminal_config = TERMINAL_CONFIG_CFG_DEFAULT;

    static Terminal parse(x::json::Parser parser);
    [[nodiscard]] x::json::json to_json() const;
};

/// @brief Bridge configures the wiring and nominal resistance of a bridge-based sensor.
struct Bridge {
    /// @brief bridge_config selects the physical bridge wiring.
    std::string bridge_config = BRIDGE_CONFIG_FULL_BRIDGE;
    /// @brief nominal_bridge_resistance is the nominal resistance of the bridge, in
    /// ohms.
    double nominal_bridge_resistance = 1;

    static Bridge parse(x::json::Parser parser);
    [[nodiscard]] x::json::json to_json() const;
};

/// @brief Resistance configures the wiring of a resistance measurement.
struct Resistance {
    /// @brief resistance_config selects the number of wires used for the resistance
    /// measurement.
    std::string resistance_config = RESISTANCE_CONFIG_WIRE_2;

    static Resistance parse(x::json::Parser parser);
    [[nodiscard]] x::json::json to_json() const;
};

/// @brief ZIndex configures the Z-index reset behavior of an encoder.
struct ZIndex {
    /// @brief z_index_enable is true when the encoder's Z index resets the count.
    bool z_index_enable = false;
    /// @brief z_index_val is the count value the Z index resets to.
    double z_index_val = 0;
    /// @brief z_index_phase selects the A/B states at which the Z index is active.
    std::string z_index_phase = Z_INDEX_PHASE_A_HIGH_B_HIGH;
    /// @brief terminal_z is the terminal the Z index signal is wired to.
    std::string terminal_z = "";

    static ZIndex parse(x::json::Parser parser);
    [[nodiscard]] x::json::json to_json() const;
};

/// @brief DigitalReadConfig configures an NI digital read task.
struct DigitalReadConfig : public ::synnax::common::BaseReadConfig {
    /// @brief device is the key of the device the task acquires from.
    ::synnax::device::Key device = "";
    /// @brief channels are the digital input channels the task acquires.
    std::vector<DIChannel> channels;

    static DigitalReadConfig parse(x::json::Parser parser);
    [[nodiscard]] x::json::json to_json() const;
};

/// @brief DigitalWriteConfig configures an NI digital write task.
struct DigitalWriteConfig : public WriteConfig {
    /// @brief channels are the digital output channels the task drives.
    std::vector<DOChannel> channels;

    static DigitalWriteConfig parse(x::json::Parser parser);
    [[nodiscard]] x::json::json to_json() const;
};

struct ScaleLinear : public LinearScale {
    std::string type = "linear";

    static ScaleLinear parse(x::json::Parser parser);
    [[nodiscard]] x::json::json to_json() const;
};

struct ScaleMap : public MapScale {
    std::string type = "map";

    static ScaleMap parse(x::json::Parser parser);
    [[nodiscard]] x::json::json to_json() const;
};

struct ScaleTable : public TableScale {
    std::string type = "table";

    static ScaleTable parse(x::json::Parser parser);
    [[nodiscard]] x::json::json to_json() const;
};

struct ScalePolynomial : public PolynomialScale {
    std::string type = "polynomial";

    static ScalePolynomial parse(x::json::Parser parser);
    [[nodiscard]] x::json::json to_json() const;
};

struct ScaleNone : public NoneScale {
    std::string type = "none";

    static ScaleNone parse(x::json::Parser parser);
    [[nodiscard]] x::json::json to_json() const;
};

/// @brief Scale determines how raw sensor values are transformed to engineering units.
using Scale = std::
    variant<ScaleLinear, ScaleMap, ScaleTable, ScalePolynomial, ScaleNone>;

Scale parse_scale(x::json::Parser parser);
[[nodiscard]] x::json::json to_json(const Scale &value);

/// @brief CustomScale applies a user-defined scale to the raw measurement.
struct CustomScale {
    /// @brief custom_scale is the scale applied to the raw measurement.
    Scale custom_scale = ScaleNone{};

    static CustomScale parse(x::json::Parser parser);
    [[nodiscard]] x::json::json to_json() const;
};

/// @brief AIVoltageChannel reads a voltage.
struct AIVoltageChannel : public BaseAIChannel,
                          public MinMaxVal,
                          public Terminal,
                          public CustomScale {
    std::string type = "ai_voltage";
    /// @brief units are the units of the voltage measurement.
    std::string units = UNITS_VOLTS;

    static AIVoltageChannel parse(x::json::Parser parser);
    [[nodiscard]] x::json::json to_json() const;
};

/// @brief AIAccelChannel reads acceleration from an accelerometer.
struct AIAccelChannel : public BaseAIChannel,
                        public MinMaxVal,
                        public Terminal,
                        public Sensitivity,
                        public CurrentExcitation,
                        public CustomScale {
    std::string type = "ai_accel";
    /// @brief units are the units of the acceleration measurement.
    std::string units = ACCEL_UNITS_G;
    /// @brief sensitivity_units are the units of the accelerometer sensitivity.
    std::string sensitivity_units = ACCEL_SENSITIVITY_UNITS_M_VOLTS_PER_G;

    static AIAccelChannel parse(x::json::Parser parser);
    [[nodiscard]] x::json::json to_json() const;
};

/// @brief AIBridgeChannel reads a generic bridge-based sensor.
struct AIBridgeChannel : public BaseAIChannel,
                         public MinMaxVal,
                         public Bridge,
                         public VoltageExcitation,
                         public CustomScale {
    std::string type = "ai_bridge";
    /// @brief units are the electrical output units of the bridge.
    std::string units = ELECTRICAL_UNITS_M_VOLTS_PER_VOLT;

    static AIBridgeChannel parse(x::json::Parser parser);
    [[nodiscard]] x::json::json to_json() const;
};

/// @brief AICurrentChannel reads a current.
struct AICurrentChannel : public BaseAIChannel,
                          public MinMaxVal,
                          public Terminal,
                          public CustomScale {
    std::string type = "ai_current";
    /// @brief units are the units of the current measurement.
    std::string units = UNITS_AMPS;
    /// @brief shunt_resistor_loc selects where the shunt resistor is located.
    std::string shunt_resistor_loc = SHUNT_RESISTOR_LOCATION_DEFAULT;
    /// @brief ext_shunt_resistor_val is the external shunt resistor value, in ohms.
    double ext_shunt_resistor_val = 1;

    static AICurrentChannel parse(x::json::Parser parser);
    [[nodiscard]] x::json::json to_json() const;
};

/// @brief AIForceBridgeTableChannel reads force from a bridge with a calibration table.
struct AIForceBridgeTableChannel : public BaseAIChannel,
                                   public MinMaxVal,
                                   public Bridge,
                                   public VoltageExcitation,
                                   public Table,
                                   public CustomScale {
    std::string type = "ai_force_bridge_table";
    /// @brief units are the units of the force measurement.
    std::string units = FORCE_UNITS_NEWTONS;
    /// @brief physical_units are the physical units of the calibration table.
    std::string physical_units = FORCE_UNITS_NEWTONS;

    static AIForceBridgeTableChannel parse(x::json::Parser parser);
    [[nodiscard]] x::json::json to_json() const;
};

/// @brief AIForceBridgeTwoPointLinChannel reads force from a bridge with two-point
/// linear calibration.
struct AIForceBridgeTwoPointLinChannel : public BaseAIChannel,
                                         public MinMaxVal,
                                         public Bridge,
                                         public VoltageExcitation,
                                         public TwoPointLin,
                                         public CustomScale {
    std::string type = "ai_force_bridge_two_point_lin";
    /// @brief units are the units of the force measurement.
    std::string units = FORCE_UNITS_NEWTONS;
    /// @brief physical_units are the physical units of the two-point calibration.
    std::string physical_units = FORCE_UNITS_NEWTONS;

    static AIForceBridgeTwoPointLinChannel parse(x::json::Parser parser);
    [[nodiscard]] x::json::json to_json() const;
};

/// @brief AIForceIEPEChannel reads force from an IEPE sensor.
struct AIForceIEPEChannel : public BaseAIChannel,
                            public MinMaxVal,
                            public Terminal,
                            public Sensitivity,
                            public CurrentExcitation,
                            public CustomScale {
    std::string type = "ai_force_iepe";
    /// @brief units are the units of the force measurement.
    std::string units = FORCE_UNITS_NEWTONS;
    /// @brief sensitivity_units are the units of the IEPE sensor sensitivity.
    std::string sensitivity_units = FORCE_SENSITIVITY_UNITS_M_VOLTS_PER_NEWTON;

    static AIForceIEPEChannel parse(x::json::Parser parser);
    [[nodiscard]] x::json::json to_json() const;
};

/// @brief AIMicrophoneChannel reads sound pressure from a microphone.
struct AIMicrophoneChannel : public BaseAIChannel,
                             public Terminal,
                             public CurrentExcitation,
                             public CustomScale {
    std::string type = "ai_microphone";
    /// @brief units are the units of the microphone measurement.
    std::string units = UNITS_PASCALS;
    /// @brief mic_sensitivity is the microphone sensitivity, in mV/Pa.
    double mic_sensitivity = 0;
    /// @brief max_snd_press_level is the maximum expected sound pressure level, in dB.
    double max_snd_press_level = 0;

    static AIMicrophoneChannel parse(x::json::Parser parser);
    [[nodiscard]] x::json::json to_json() const;
};

/// @brief AIPressureBridgeTableChannel reads pressure from a bridge with a calibration
/// table.
struct AIPressureBridgeTableChannel : public BaseAIChannel,
                                      public MinMaxVal,
                                      public Bridge,
                                      public VoltageExcitation,
                                      public Table,
                                      public CustomScale {
    std::string type = "ai_pressure_bridge_table";
    /// @brief units are the units of the pressure measurement.
    std::string units = PRESSURE_UNITS_POUNDS_PER_SQUARE_INCH;
    /// @brief physical_units are the physical units of the calibration table.
    std::string physical_units = PRESSURE_UNITS_POUNDS_PER_SQUARE_INCH;

    static AIPressureBridgeTableChannel parse(x::json::Parser parser);
    [[nodiscard]] x::json::json to_json() const;
};

/// @brief AIPressureBridgeTwoPointLinChannel reads pressure from a bridge with
/// two-point linear calibration.
struct AIPressureBridgeTwoPointLinChannel : public BaseAIChannel,
                                            public MinMaxVal,
                                            public Bridge,
                                            public VoltageExcitation,
                                            public TwoPointLin,
                                            public CustomScale {
    std::string type = "ai_pressure_bridge_two_point_lin";
    /// @brief units are the units of the pressure measurement.
    std::string units = PRESSURE_UNITS_POUNDS_PER_SQUARE_INCH;
    /// @brief physical_units are the physical units of the two-point calibration.
    std::string physical_units = PRESSURE_UNITS_POUNDS_PER_SQUARE_INCH;

    static AIPressureBridgeTwoPointLinChannel parse(x::json::Parser parser);
    [[nodiscard]] x::json::json to_json() const;
};

/// @brief AIResistanceChannel reads a resistance.
struct AIResistanceChannel : public BaseAIChannel,
                             public MinMaxVal,
                             public Resistance,
                             public CurrentExcitation,
                             public CustomScale {
    std::string type = "ai_resistance";
    /// @brief units are the units of the resistance measurement.
    std::string units = UNITS_OHMS;

    static AIResistanceChannel parse(x::json::Parser parser);
    [[nodiscard]] x::json::json to_json() const;
};

/// @brief AIRTDChannel reads temperature from an RTD.
struct AIRTDChannel : public BaseAIChannel,
                      public MinMaxVal,
                      public Resistance,
                      public CurrentExcitation {
    std::string type = "ai_rtd";
    /// @brief units are the units of the temperature measurement.
    std::string units = TEMPERATURE_UNITS_DEG_C;
    /// @brief rtd_type selects the RTD resistance-temperature curve.
    std::string rtd_type = RTD_TYPE_PT_3750;
    /// @brief r0 is the sensor resistance at 0 degrees Celsius, in ohms.
    double r0 = 0;

    static AIRTDChannel parse(x::json::Parser parser);
    [[nodiscard]] x::json::json to_json() const;
};

/// @brief AIStrainGaugeChannel reads strain from a strain gauge.
struct AIStrainGaugeChannel : public BaseAIChannel,
                              public MinMaxVal,
                              public VoltageExcitation,
                              public CustomScale {
    std::string type = "ai_strain_gauge";
    /// @brief units are the units of the strain measurement.
    std::string units = UNITS_STRAIN;
    /// @brief strain_config selects the strain-gauge bridge configuration.
    std::string strain_config = STRAIN_CONFIG_FULL_BRIDGE_I;
    /// @brief gage_factor is the gauge factor of the strain gauge.
    double gage_factor = 0;
    /// @brief initial_bridge_voltage is the bridge output voltage in the unloaded
    /// state.
    double initial_bridge_voltage = 0;
    /// @brief nominal_gage_resistance is the nominal gauge resistance, in ohms.
    double nominal_gage_resistance = 0;
    /// @brief poisson_ratio is the Poisson ratio of the gauge material.
    double poisson_ratio = 0;
    /// @brief lead_wire_resistance is the resistance of the lead wires, in ohms.
    double lead_wire_resistance = 0;

    static AIStrainGaugeChannel parse(x::json::Parser parser);
    [[nodiscard]] x::json::json to_json() const;
};

/// @brief AITempBuiltinChannel reads temperature from the device's built-in sensor.
struct AITempBuiltinChannel : public BaseAIChannel {
    std::string type = "ai_temp_builtin";
    /// @brief units are the units of the temperature measurement.
    std::string units = TEMPERATURE_UNITS_DEG_C;

    static AITempBuiltinChannel parse(x::json::Parser parser);
    [[nodiscard]] x::json::json to_json() const;
};

/// @brief AIThermocoupleChannel reads temperature from a thermocouple.
struct AIThermocoupleChannel : public BaseAIChannel, public MinMaxVal {
    std::string type = "ai_thermocouple";
    /// @brief units are the units of the temperature measurement.
    std::string units = TEMPERATURE_UNITS_DEG_C;
    /// @brief thermocouple_type selects the thermocouple alloy type.
    std::string thermocouple_type = THERMOCOUPLE_TYPE_J;
    /// @brief cjc is the cold-junction compensation applied to the reading.
    CJC cjc = CJCBuiltIn{};

    static AIThermocoupleChannel parse(x::json::Parser parser);
    [[nodiscard]] x::json::json to_json() const;
};

/// @brief AITorqueBridgeTableChannel reads torque from a bridge with a calibration
/// table.
struct AITorqueBridgeTableChannel : public BaseAIChannel,
                                    public MinMaxVal,
                                    public Bridge,
                                    public VoltageExcitation,
                                    public Table,
                                    public CustomScale {
    std::string type = "ai_torque_bridge_table";
    /// @brief units are the units of the torque measurement.
    std::string units = TORQUE_UNITS_NEWTON_METERS;
    /// @brief physical_units are the physical units of the calibration table.
    std::string physical_units = TORQUE_UNITS_NEWTON_METERS;

    static AITorqueBridgeTableChannel parse(x::json::Parser parser);
    [[nodiscard]] x::json::json to_json() const;
};

/// @brief AITorqueBridgeTwoPointLinChannel reads torque from a bridge with two-point
/// linear calibration.
struct AITorqueBridgeTwoPointLinChannel : public BaseAIChannel,
                                          public MinMaxVal,
                                          public Bridge,
                                          public VoltageExcitation,
                                          public TwoPointLin,
                                          public CustomScale {
    std::string type = "ai_torque_bridge_two_point_lin";
    /// @brief units are the units of the torque measurement.
    std::string units = TORQUE_UNITS_NEWTON_METERS;
    /// @brief physical_units are the physical units of the two-point calibration.
    std::string physical_units = TORQUE_UNITS_NEWTON_METERS;

    static AITorqueBridgeTwoPointLinChannel parse(x::json::Parser parser);
    [[nodiscard]] x::json::json to_json() const;
};

/// @brief AIVelocityIEPEChannel reads velocity from an IEPE sensor.
struct AIVelocityIEPEChannel : public BaseAIChannel,
                               public MinMaxVal,
                               public Terminal,
                               public Sensitivity,
                               public CurrentExcitation,
                               public CustomScale {
    std::string type = "ai_velocity_iepe";
    /// @brief units are the units of the velocity measurement.
    std::string units = VELOCITY_UNITS_METERS_PER_SECOND;
    /// @brief sensitivity_units are the units of the IEPE sensor sensitivity.
    std::string
        sensitivity_units = VELOCITY_SENSITIVITY_UNITS_M_VOLTS_PER_MM_PER_SECOND;

    static AIVelocityIEPEChannel parse(x::json::Parser parser);
    [[nodiscard]] x::json::json to_json() const;
};

/// @brief AIAccel4WireDCVoltageChannel reads acceleration from a 4-wire DC-voltage
/// accelerometer.
struct AIAccel4WireDCVoltageChannel : public BaseAIChannel,
                                      public MinMaxVal,
                                      public Terminal,
                                      public Sensitivity,
                                      public VoltageExcitation,
                                      public CustomScale {
    std::string type = "ai_accel_4_wire_dc_voltage";
    /// @brief units are the units of the acceleration measurement.
    std::string units = ACCEL_UNITS_G;
    /// @brief sensitivity_units are the units of the accelerometer sensitivity.
    std::string sensitivity_units = ACCEL_SENSITIVITY_UNITS_M_VOLTS_PER_G;
    /// @brief use_excit_for_scaling is true when the excitation voltage is used to
    /// scale
    /// the reading.
    bool use_excit_for_scaling = false;

    static AIAccel4WireDCVoltageChannel parse(x::json::Parser parser);
    [[nodiscard]] x::json::json to_json() const;
};

/// @brief AIAccelChargeChannel reads acceleration from a charge-mode accelerometer.
struct AIAccelChargeChannel : public BaseAIChannel,
                              public MinMaxVal,
                              public CustomScale {
    std::string type = "ai_accel_charge";
    /// @brief units are the units of the acceleration measurement.
    std::string units = ACCEL_UNITS_G;
    /// @brief sensitivity is the sensitivity of the accelerometer.
    double sensitivity = 0;
    /// @brief sensitivity_units are the units of the accelerometer's sensitivity
    /// rating.
    std::string sensitivity_units = ACCEL_SENSITIVITY_UNITS_M_VOLTS_PER_G;

    static AIAccelChargeChannel parse(x::json::Parser parser);
    [[nodiscard]] x::json::json to_json() const;
};

/// @brief AIChargeChannel reads a raw charge.
struct AIChargeChannel : public BaseAIChannel,
                         public MinMaxVal,
                         public Terminal,
                         public CustomScale {
    std::string type = "ai_charge";
    /// @brief units are the units of the charge measurement.
    std::string units = CHARGE_UNITS_COULOMBS;

    static AIChargeChannel parse(x::json::Parser parser);
    [[nodiscard]] x::json::json to_json() const;
};

/// @brief AICurrentRMSChannel reads RMS current.
struct AICurrentRMSChannel : public BaseAIChannel,
                             public MinMaxVal,
                             public Terminal,
                             public CustomScale {
    std::string type = "ai_current_rms";
    /// @brief units are the units of the current measurement.
    std::string units = UNITS_AMPS;
    /// @brief shunt_resistor_loc selects where the shunt resistor is located.
    std::string shunt_resistor_loc = SHUNT_RESISTOR_LOCATION_DEFAULT;
    /// @brief ext_shunt_resistor_val is the external shunt resistor value, in ohms.
    double ext_shunt_resistor_val = 1;

    static AICurrentRMSChannel parse(x::json::Parser parser);
    [[nodiscard]] x::json::json to_json() const;
};

/// @brief AIForceBridgePolynomialChannel reads force from a bridge with polynomial
/// calibration.
struct AIForceBridgePolynomialChannel : public BaseAIChannel,
                                        public MinMaxVal,
                                        public Terminal,
                                        public Bridge,
                                        public VoltageExcitation,
                                        public BridgePolynomial,
                                        public CustomScale {
    std::string type = "ai_force_bridge_polynomial";
    /// @brief units are the units of the force measurement.
    std::string units = FORCE_UNITS_NEWTONS;
    /// @brief physical_units are the physical units of the polynomial calibration.
    std::string physical_units = FORCE_UNITS_NEWTONS;

    static AIForceBridgePolynomialChannel parse(x::json::Parser parser);
    [[nodiscard]] x::json::json to_json() const;
};

/// @brief AIFreqVoltageChannel reads frequency from a voltage signal.
struct AIFreqVoltageChannel : public BaseAIChannel,
                              public MinMaxVal,
                              public CustomScale {
    std::string type = "ai_freq_voltage";
    /// @brief units are the units of the frequency measurement.
    std::string units = UNITS_HZ;
    /// @brief threshold_level is the voltage level at which a cycle is counted.
    double threshold_level = 0;
    /// @brief hysteresis is the hysteresis applied around the threshold level.
    double hysteresis = 0;

    static AIFreqVoltageChannel parse(x::json::Parser parser);
    [[nodiscard]] x::json::json to_json() const;
};

/// @brief AIPressureBridgePolynomialChannel reads pressure from a bridge with
/// polynomial calibration.
struct AIPressureBridgePolynomialChannel : public BaseAIChannel,
                                           public MinMaxVal,
                                           public Bridge,
                                           public VoltageExcitation,
                                           public BridgePolynomial,
                                           public CustomScale {
    std::string type = "ai_pressure_bridge_polynomial";
    /// @brief units are the units of the pressure measurement.
    std::string units = PRESSURE_UNITS_POUNDS_PER_SQUARE_INCH;
    /// @brief physical_units are the physical units of the polynomial calibration.
    std::string physical_units = PRESSURE_UNITS_POUNDS_PER_SQUARE_INCH;

    static AIPressureBridgePolynomialChannel parse(x::json::Parser parser);
    [[nodiscard]] x::json::json to_json() const;
};

/// @brief AIThermistorIexChannel reads temperature from a current-excited thermistor.
struct AIThermistorIexChannel : public BaseAIChannel,
                                public MinMaxVal,
                                public Resistance,
                                public CurrentExcitation {
    std::string type = "ai_thermistor_iex";
    /// @brief units are the units of the temperature measurement.
    std::string units = TEMPERATURE_UNITS_DEG_C;
    /// @brief a is the first Steinhart-Hart coefficient.
    double a = 0;
    /// @brief b is the second Steinhart-Hart coefficient.
    double b = 0;
    /// @brief c is the third Steinhart-Hart coefficient.
    double c = 0;

    static AIThermistorIexChannel parse(x::json::Parser parser);
    [[nodiscard]] x::json::json to_json() const;
};

/// @brief AIThermistorVexChannel reads temperature from a voltage-excited thermistor.
struct AIThermistorVexChannel : public BaseAIChannel,
                                public MinMaxVal,
                                public Resistance,
                                public VoltageExcitation {
    std::string type = "ai_thermistor_vex";
    /// @brief units are the units of the temperature measurement.
    std::string units = TEMPERATURE_UNITS_DEG_C;
    /// @brief a is the first Steinhart-Hart coefficient.
    double a = 0;
    /// @brief b is the second Steinhart-Hart coefficient.
    double b = 0;
    /// @brief c is the third Steinhart-Hart coefficient.
    double c = 0;
    /// @brief r1 is the reference resistor value, in ohms.
    double r1 = 0;

    static AIThermistorVexChannel parse(x::json::Parser parser);
    [[nodiscard]] x::json::json to_json() const;
};

/// @brief AITorqueBridgePolynomialChannel reads torque from a bridge with polynomial
/// calibration.
struct AITorqueBridgePolynomialChannel : public BaseAIChannel,
                                         public MinMaxVal,
                                         public Bridge,
                                         public VoltageExcitation,
                                         public BridgePolynomial,
                                         public CustomScale {
    std::string type = "ai_torque_bridge_polynomial";
    /// @brief units are the units of the torque measurement.
    std::string units = TORQUE_UNITS_NEWTON_METERS;
    /// @brief physical_units are the physical units of the polynomial calibration.
    std::string physical_units = TORQUE_UNITS_NEWTON_METERS;

    static AITorqueBridgePolynomialChannel parse(x::json::Parser parser);
    [[nodiscard]] x::json::json to_json() const;
};

/// @brief AIVoltageRMSChannel reads RMS voltage.
struct AIVoltageRMSChannel : public BaseAIChannel,
                             public MinMaxVal,
                             public Terminal,
                             public CustomScale {
    std::string type = "ai_voltage_rms";
    /// @brief units are the units of the voltage measurement.
    std::string units = UNITS_VOLTS;

    static AIVoltageRMSChannel parse(x::json::Parser parser);
    [[nodiscard]] x::json::json to_json() const;
};

/// @brief AIVoltageWithExcitChannel reads a voltage with excitation.
struct AIVoltageWithExcitChannel : public BaseAIChannel,
                                   public MinMaxVal,
                                   public Terminal,
                                   public VoltageExcitation,
                                   public CustomScale {
    std::string type = "ai_voltage_with_excit";
    /// @brief units are the units of the voltage measurement.
    std::string units = UNITS_VOLTS;
    /// @brief bridge_config selects the physical bridge wiring.
    std::string bridge_config = BRIDGE_CONFIG_FULL_BRIDGE;
    /// @brief use_excit_for_scaling is true when the excitation voltage is used to
    /// scale
    /// the reading.
    bool use_excit_for_scaling = false;

    static AIVoltageWithExcitChannel parse(x::json::Parser parser);
    [[nodiscard]] x::json::json to_json() const;
};

/// @brief AIChannel is a single NI analog input channel. The type field selects the
/// measurement mode and the fields that accompany it.
using AIChannel = std::variant<
    AIVoltageChannel,
    AIAccelChannel,
    AIBridgeChannel,
    AICurrentChannel,
    AIForceBridgeTableChannel,
    AIForceBridgeTwoPointLinChannel,
    AIForceIEPEChannel,
    AIMicrophoneChannel,
    AIPressureBridgeTableChannel,
    AIPressureBridgeTwoPointLinChannel,
    AIResistanceChannel,
    AIRTDChannel,
    AIStrainGaugeChannel,
    AITempBuiltinChannel,
    AIThermocoupleChannel,
    AITorqueBridgeTableChannel,
    AITorqueBridgeTwoPointLinChannel,
    AIVelocityIEPEChannel,
    AIAccel4WireDCVoltageChannel,
    AIAccelChargeChannel,
    AIChargeChannel,
    AICurrentRMSChannel,
    AIForceBridgePolynomialChannel,
    AIFreqVoltageChannel,
    AIPressureBridgePolynomialChannel,
    AIThermistorIexChannel,
    AIThermistorVexChannel,
    AITorqueBridgePolynomialChannel,
    AIVoltageRMSChannel,
    AIVoltageWithExcitChannel>;

AIChannel parse_ai_channel(x::json::Parser parser);
[[nodiscard]] x::json::json to_json(const AIChannel &value);

/// @brief CIFrequencyChannel measures signal frequency.
struct CIFrequencyChannel : public BaseCIChannel, public CustomScale {
    std::string type = "ci_frequency";
    /// @brief min_val is the minimum expected frequency.
    double min_val = 2;
    /// @brief max_val is the maximum expected frequency.
    double max_val = 100;
    /// @brief units are the units of the frequency measurement.
    std::string units = CI_FREQ_UNITS_HZ;
    /// @brief edge selects the edge the counter responds to.
    std::string edge = CI_EDGE_RISING;
    /// @brief meas_method selects the measurement method.
    std::string meas_method = CI_MEAS_METHOD_DYNAMIC_AVG;
    /// @brief meas_time is the averaging window for high-frequency measurement, in
    /// seconds.
    double meas_time = 0.000006;
    /// @brief divisor is the counter divisor for large-range measurement.
    std::int32_t divisor = 4;
    /// @brief terminal is the terminal the counter signal is wired to.
    std::string terminal = "";

    static CIFrequencyChannel parse(x::json::Parser parser);
    [[nodiscard]] x::json::json to_json() const;
};

/// @brief CIEdgeCountChannel counts signal edges.
struct CIEdgeCountChannel : public BaseCIChannel {
    std::string type = "ci_edge_count";
    /// @brief active_edge selects the edge that increments the count.
    std::string active_edge = CI_EDGE_RISING;
    /// @brief count_direction selects the direction the counter counts.
    std::string count_direction = CI_COUNT_DIRECTION_COUNT_UP;
    /// @brief initial_count is the value the counter starts at.
    double initial_count = 0;
    /// @brief terminal is the terminal the counter signal is wired to.
    std::string terminal = "";

    static CIEdgeCountChannel parse(x::json::Parser parser);
    [[nodiscard]] x::json::json to_json() const;
};

/// @brief CIPeriodChannel measures signal period.
struct CIPeriodChannel : public BaseCIChannel, public CustomScale {
    std::string type = "ci_period";
    /// @brief min_val is the minimum expected period, in seconds.
    double min_val = 0.000001;
    /// @brief max_val is the maximum expected period, in seconds.
    double max_val = 0.100000;
    /// @brief units are the units of the period measurement.
    std::string units = CI_TIME_UNITS_SECONDS;
    /// @brief starting_edge selects the edge that starts a period measurement.
    std::string starting_edge = CI_EDGE_RISING;
    /// @brief meas_method selects the measurement method.
    std::string meas_method = CI_MEAS_METHOD_DYNAMIC_AVG;
    /// @brief meas_time is the averaging window for high-frequency measurement, in
    /// seconds.
    double meas_time = 0.001000;
    /// @brief divisor is the counter divisor for large-range measurement.
    std::int32_t divisor = 4;
    /// @brief terminal is the terminal the counter signal is wired to.
    std::string terminal = "";

    static CIPeriodChannel parse(x::json::Parser parser);
    [[nodiscard]] x::json::json to_json() const;
};

/// @brief CIPulseWidthChannel measures pulse width.
struct CIPulseWidthChannel : public BaseCIChannel, public CustomScale {
    std::string type = "ci_pulse_width";
    /// @brief min_val is the minimum expected pulse width, in seconds.
    double min_val = 0.000001;
    /// @brief max_val is the maximum expected pulse width, in seconds.
    double max_val = 0.100000;
    /// @brief units are the units of the pulse-width measurement.
    std::string units = CI_TIME_UNITS_SECONDS;
    /// @brief starting_edge selects the edge that starts a pulse-width measurement.
    std::string starting_edge = CI_EDGE_RISING;
    /// @brief terminal is the terminal the counter signal is wired to.
    std::string terminal = "";

    static CIPulseWidthChannel parse(x::json::Parser parser);
    [[nodiscard]] x::json::json to_json() const;
};

/// @brief CISemiPeriodChannel measures semi-period.
struct CISemiPeriodChannel : public BaseCIChannel, public CustomScale {
    std::string type = "ci_semi_period";
    /// @brief min_val is the minimum expected semi-period, in seconds.
    double min_val = 0.000001;
    /// @brief max_val is the maximum expected semi-period, in seconds.
    double max_val = 0.100000;
    /// @brief units are the units of the semi-period measurement.
    std::string units = CI_TIME_UNITS_SECONDS;
    /// @brief terminal is the terminal the counter signal is wired to.
    std::string terminal = "";

    static CISemiPeriodChannel parse(x::json::Parser parser);
    [[nodiscard]] x::json::json to_json() const;
};

/// @brief CITwoEdgeSepChannel measures the separation between two edges.
struct CITwoEdgeSepChannel : public BaseCIChannel, public CustomScale {
    std::string type = "ci_two_edge_sep";
    /// @brief min_val is the minimum expected separation, in seconds.
    double min_val = 0.000001;
    /// @brief max_val is the maximum expected separation, in seconds.
    double max_val = 1;
    /// @brief units are the units of the two-edge-separation measurement.
    std::string units = CI_TIME_UNITS_SECONDS;
    /// @brief first_edge selects the edge that starts the measurement.
    std::string first_edge = CI_EDGE_RISING;
    /// @brief second_edge selects the edge that stops the measurement.
    std::string second_edge = CI_EDGE_FALLING;
    /// @brief first_terminal is the terminal the first edge's signal is wired to.
    std::string first_terminal = "";
    /// @brief second_terminal is the terminal the second edge's signal is wired to.
    std::string second_terminal = "";

    static CITwoEdgeSepChannel parse(x::json::Parser parser);
    [[nodiscard]] x::json::json to_json() const;
};

/// @brief CIVelocityLinearChannel measures linear velocity from an encoder.
struct CIVelocityLinearChannel : public BaseCIChannel, public CustomScale {
    std::string type = "ci_velocity_linear";
    /// @brief min_val is the minimum expected velocity.
    double min_val = 0;
    /// @brief max_val is the maximum expected velocity.
    double max_val = 1;
    /// @brief units are the units of the velocity measurement.
    std::string units = CI_LINEAR_VELOCITY_UNITS_M_PER_S;
    /// @brief decoding_type selects the encoder decoding type.
    std::string decoding_type = CI_DECODING_TYPE_X_4;
    /// @brief dist_per_pulse is the linear distance per encoder pulse.
    double dist_per_pulse = 0.001000;
    /// @brief terminal_a is the terminal for encoder channel A.
    std::string terminal_a = "";
    /// @brief terminal_b is the terminal for encoder channel B.
    std::string terminal_b = "";

    static CIVelocityLinearChannel parse(x::json::Parser parser);
    [[nodiscard]] x::json::json to_json() const;
};

/// @brief CIVelocityAngularChannel measures angular velocity from an encoder.
struct CIVelocityAngularChannel : public BaseCIChannel, public CustomScale {
    std::string type = "ci_velocity_angular";
    /// @brief min_val is the minimum expected velocity.
    double min_val = 0;
    /// @brief max_val is the maximum expected velocity.
    double max_val = 1;
    /// @brief units are the units of the velocity measurement.
    std::string units = CI_ANGULAR_VELOCITY_UNITS_RPM;
    /// @brief decoding_type selects the encoder decoding type.
    std::string decoding_type = CI_DECODING_TYPE_X_4;
    /// @brief pulses_per_rev is the number of encoder pulses per revolution.
    double pulses_per_rev = 24;
    /// @brief terminal_a is the terminal for encoder channel A.
    std::string terminal_a = "";
    /// @brief terminal_b is the terminal for encoder channel B.
    std::string terminal_b = "";

    static CIVelocityAngularChannel parse(x::json::Parser parser);
    [[nodiscard]] x::json::json to_json() const;
};

/// @brief CIPositionLinearChannel measures linear position from an encoder.
struct CIPositionLinearChannel : public BaseCIChannel,
                                 public CustomScale,
                                 public ZIndex {
    std::string type = "ci_position_linear";
    /// @brief units are the units of the position measurement.
    std::string units = CI_LINEAR_POSITION_UNITS_METERS;
    /// @brief decoding_type selects the encoder decoding type.
    std::string decoding_type = CI_DECODING_TYPE_X_4;
    /// @brief dist_per_pulse is the linear distance per encoder pulse.
    double dist_per_pulse = 0.000001;
    /// @brief initial_pos is the position the encoder starts at.
    double initial_pos = 0;
    /// @brief terminal_a is the terminal for encoder channel A.
    std::string terminal_a = "";
    /// @brief terminal_b is the terminal for encoder channel B.
    std::string terminal_b = "";

    static CIPositionLinearChannel parse(x::json::Parser parser);
    [[nodiscard]] x::json::json to_json() const;
};

/// @brief CIPositionAngularChannel measures angular position from an encoder.
struct CIPositionAngularChannel : public BaseCIChannel,
                                  public CustomScale,
                                  public ZIndex {
    std::string type = "ci_position_angular";
    /// @brief units are the units of the position measurement.
    std::string units = CI_ANGULAR_POSITION_UNITS_DEGREES;
    /// @brief decoding_type selects the encoder decoding type.
    std::string decoding_type = CI_DECODING_TYPE_X_4;
    /// @brief pulses_per_rev is the number of encoder pulses per revolution.
    double pulses_per_rev = 24;
    /// @brief initial_angle is the angle the encoder starts at.
    double initial_angle = 0;
    /// @brief terminal_a is the terminal for encoder channel A.
    std::string terminal_a = "";
    /// @brief terminal_b is the terminal for encoder channel B.
    std::string terminal_b = "";

    static CIPositionAngularChannel parse(x::json::Parser parser);
    [[nodiscard]] x::json::json to_json() const;
};

/// @brief CIDutyCycleChannel measures the duty cycle of a signal.
struct CIDutyCycleChannel : public BaseCIChannel, public CustomScale {
    std::string type = "ci_duty_cycle";
    /// @brief min_val is the minimum expected duty-cycle frequency.
    double min_val = 2;
    /// @brief max_val is the maximum expected duty-cycle frequency.
    double max_val = 10000;
    /// @brief active_edge selects the edge the counter responds to.
    std::string active_edge = CI_EDGE_RISING;
    /// @brief terminal is the terminal the counter signal is wired to.
    std::string terminal = "";

    static CIDutyCycleChannel parse(x::json::Parser parser);
    [[nodiscard]] x::json::json to_json() const;
};

/// @brief CIChannel is a single NI counter input channel. The type field selects the
/// measurement mode and the fields that accompany it.
using CIChannel = std::variant<
    CIFrequencyChannel,
    CIEdgeCountChannel,
    CIPeriodChannel,
    CIPulseWidthChannel,
    CISemiPeriodChannel,
    CITwoEdgeSepChannel,
    CIVelocityLinearChannel,
    CIVelocityAngularChannel,
    CIPositionLinearChannel,
    CIPositionAngularChannel,
    CIDutyCycleChannel>;

CIChannel parse_ci_channel(x::json::Parser parser);
[[nodiscard]] x::json::json to_json(const CIChannel &value);

/// @brief AOCurrentChannel drives a current output.
struct AOCurrentChannel : public BaseAOChannel, public MinMaxVal, public CustomScale {
    std::string type = "ao_current";
    /// @brief units are the units of the current output.
    std::string units = UNITS_AMPS;

    static AOCurrentChannel parse(x::json::Parser parser);
    [[nodiscard]] x::json::json to_json() const;
};

/// @brief AOFuncGenChannel drives a function-generator output.
struct AOFuncGenChannel : public BaseAOChannel {
    std::string type = "ao_func_gen";
    /// @brief wave_type selects the waveform to generate.
    std::string wave_type = WAVE_TYPE_SINE;
    /// @brief frequency is the waveform frequency, in hertz.
    double frequency = 0;
    /// @brief amplitude is the waveform amplitude.
    double amplitude = 0;
    /// @brief offset is the waveform DC offset.
    double offset = 0;

    static AOFuncGenChannel parse(x::json::Parser parser);
    [[nodiscard]] x::json::json to_json() const;
};

/// @brief AOVoltageChannel drives a voltage output.
struct AOVoltageChannel : public BaseAOChannel, public MinMaxVal, public CustomScale {
    std::string type = "ao_voltage";
    /// @brief units are the units of the voltage output.
    std::string units = UNITS_VOLTS;

    static AOVoltageChannel parse(x::json::Parser parser);
    [[nodiscard]] x::json::json to_json() const;
};

/// @brief AOChannel is a single NI analog output channel. The type field selects the
/// output mode and the fields that accompany it.
using AOChannel = std::variant<AOCurrentChannel, AOFuncGenChannel, AOVoltageChannel>;

AOChannel parse_ao_channel(x::json::Parser parser);
[[nodiscard]] x::json::json to_json(const AOChannel &value);

/// @brief AnalogReadConfig configures an NI analog read task. Each channel carries its
/// own device.
struct AnalogReadConfig : public ::synnax::common::BaseReadConfig {
    /// @brief channels are the analog input channels the task acquires.
    std::vector<AIChannel> channels;

    static AnalogReadConfig parse(x::json::Parser parser);
    [[nodiscard]] x::json::json to_json() const;
};

/// @brief CounterReadConfig configures an NI counter read task. Each channel carries
/// its own device.
struct CounterReadConfig : public ::synnax::common::BaseReadConfig {
    /// @brief channels are the counter input channels the task acquires.
    std::vector<CIChannel> channels;

    static CounterReadConfig parse(x::json::Parser parser);
    [[nodiscard]] x::json::json to_json() const;
};

/// @brief AnalogWriteConfig configures an NI analog write task.
struct AnalogWriteConfig : public WriteConfig {
    /// @brief channels are the analog output channels the task drives.
    std::vector<AOChannel> channels;

    static AnalogWriteConfig parse(x::json::Parser parser);
    [[nodiscard]] x::json::json to_json() const;
};
}
