// Copyright 2026 Synnax Labs, Inc.
//
// Use of this software is governed by the Business Source License included in the file
// licenses/BSL.txt.
//
// As of the Change Date specified in that file, in accordance with the Business Source
// License, use of this software will be governed by the Apache License, Version 2.0,
// included in the file licenses/APL.txt.

// Code generated by oracle. DO NOT EDIT.

#pragma once

#include <optional>
#include <string>
#include <variant>
#include <vector>

#include "client/cpp/channel/types.gen.h"
#include "client/cpp/device/types.gen.h"
#include "client/cpp/task/common/types.gen.h"
#include "x/cpp/json/json.h"
#include "x/cpp/telem/types.gen.h"

namespace synnax::http {

struct Header;
struct QueryParam;
struct EnumEntry;
struct BaseWriteField;
struct ScanConfig;
struct ReadField;
struct ChannelField;
struct ReadEndpoint;
struct WriteEndpoint;
struct ReadConfig;
struct WriteConfig;

constexpr const char *TIME_FORMAT_ISO_8601 = "iso8601";
constexpr const char *TIME_FORMAT_UNIX_SEC = "unix_sec";
constexpr const char *TIME_FORMAT_UNIX_MS = "unix_ms";
constexpr const char *TIME_FORMAT_UNIX_US = "unix_us";
constexpr const char *TIME_FORMAT_UNIX_NS = "unix_ns";

constexpr const char *METHOD_GET = "GET";
constexpr const char *METHOD_HEAD = "HEAD";
constexpr const char *METHOD_POST = "POST";
constexpr const char *METHOD_PUT = "PUT";
constexpr const char *METHOD_DELETE = "DELETE";
constexpr const char *METHOD_PATCH = "PATCH";
constexpr const char *METHOD_OPTIONS = "OPTIONS";
constexpr const char *METHOD_TRACE = "TRACE";
constexpr const char *METHOD_CONNECT = "CONNECT";

constexpr const char *JSON_TYPE_NUMBER = "number";
constexpr const char *JSON_TYPE_STRING = "string";
constexpr const char *JSON_TYPE_BOOLEAN = "boolean";

constexpr const char *GENERATOR_TYPE_UUID = "uuid";
constexpr const char *GENERATOR_TYPE_TIMESTAMP = "timestamp";

/// @brief Header is a single HTTP header sent with a request.
struct Header {
    /// @brief name is the header name, unique within a request.
    std::string name = "";
    /// @brief value is the header value.
    std::string value = "";

    static Header parse(x::json::Parser parser);
    [[nodiscard]] x::json::json to_json() const;
};

/// @brief QueryParam is a single URL query parameter sent with a request.
struct QueryParam {
    /// @brief parameter is the parameter name, unique within a request.
    std::string parameter = "";
    /// @brief value is the parameter value.
    std::string value = "";

    static QueryParam parse(x::json::Parser parser);
    [[nodiscard]] x::json::json to_json() const;
};

/// @brief EnumEntry maps a string label in a JSON body to a numeric channel value for
/// enum-style parsing (e.g. "ON" -> 1, "OFF" -> 0).
struct EnumEntry {
    /// @brief label is the string label in the JSON body.
    std::string label = "";
    /// @brief value is the numeric channel value the label maps to.
    double value = 0;

    static EnumEntry parse(x::json::Parser parser);
    [[nodiscard]] x::json::json to_json() const;
};

/// @brief BaseWriteField carries the fields every write body field shares.
struct BaseWriteField {
    /// @brief key uniquely identifies the field within the endpoint.
    std::string key = "";
    /// @brief pointer is the JSON Pointer where the value is placed in the body.
    std::string pointer = "";

    static BaseWriteField parse(x::json::Parser parser);
    [[nodiscard]] x::json::json to_json() const;
};

/// @brief ScanConfig configures an HTTP scan task, which carries no settings.
struct ScanConfig : public ::synnax::task::common::KeyedConfig {

    static ScanConfig parse(x::json::Parser parser);
    [[nodiscard]] x::json::json to_json() const;
};

/// @brief ReadField is a single value extracted from an endpoint's JSON response.
struct ReadField {
    /// @brief key uniquely identifies the field within the endpoint.
    std::string key = "";
    /// @brief name is the human-readable field name.
    std::string name = "";
    /// @brief disabled is true when the field is excluded from polling.
    bool disabled = false;
    /// @brief channel is the Synnax channel the extracted value is written to.
    ::synnax::channel::Key channel = ::synnax::channel::Key(0);
    /// @brief pointer is the JSON Pointer to the value within the response body.
    std::string pointer = "";
    /// @brief data_type is the data type of the extracted value.
    ::x::telem::DataType data_type = ::x::telem::DataType("float64");
    /// @brief timestamp_format is the encoding of the JSON value when the target
    /// channel
    /// holds timestamps. Required for timestamp channels.
    std::optional<std::string> timestamp_format;
    /// @brief enum_values maps string labels in the response to numeric channel values.
    std::vector<EnumEntry> enum_values;

    static ReadField parse(x::json::Parser parser);
    [[nodiscard]] x::json::json to_json() const;
};

/// @brief ChannelField is the command channel value placed into a write endpoint's
/// body.
struct ChannelField {
    /// @brief pointer is the JSON Pointer where the channel value is placed in the
    /// body. An
    /// empty pointer sends the bare value as the entire body, in which case the
    /// endpoint may not declare any additional fields.
    std::string pointer = "";
    /// @brief json_type is the JSON type the channel value is serialized as.
    std::string json_type = JSON_TYPE_NUMBER;
    /// @brief channel is the Synnax command channel that triggers the request.
    ::synnax::channel::Key channel = ::synnax::channel::Key(0);
    /// @brief name is the human-readable name of the command channel.
    std::string name = "";
    /// @brief data_type is the data type of the command channel.
    ::x::telem::DataType data_type = ::x::telem::DataType("float64");
    /// @brief time_format is the output encoding when the command channel holds
    /// timestamps.
    /// Required for timestamp channels.
    std::optional<std::string> time_format;
    /// @brief enum_values maps numeric channel values to string labels. Only valid when
    /// json_type is 'string'.
    std::vector<EnumEntry> enum_values;

    static ChannelField parse(x::json::Parser parser);
    [[nodiscard]] x::json::json to_json() const;
};

/// @brief WriteFieldStatic places a fixed value in the request body.
struct WriteFieldStatic : public BaseWriteField {
    std::string type = "static";
    /// @brief json_type is the JSON type the value is serialized as.
    std::string json_type = JSON_TYPE_NUMBER;
    /// @brief value is the fixed JSON value placed at the pointer.
    x::json::json value;

    static WriteFieldStatic parse(x::json::Parser parser);
    [[nodiscard]] x::json::json to_json() const;
};

/// @brief WriteFieldGenerated places a freshly generated UUID or timestamp in the body.
struct WriteFieldGenerated : public BaseWriteField {
    std::string type = "generated";
    /// @brief generator is the generator that produces a fresh value per request.
    std::string generator = GENERATOR_TYPE_UUID;
    /// @brief time_format is the output encoding for timestamp generators. Defaults to
    /// iso8601.
    std::optional<std::string> time_format;

    static WriteFieldGenerated parse(x::json::Parser parser);
    [[nodiscard]] x::json::json to_json() const;
};

/// @brief WriteField is an additional body field on a write endpoint. The type field
/// selects whether the value is fixed or generated per request.
using WriteField = std::variant<WriteFieldStatic, WriteFieldGenerated>;

WriteField parse_write_field(x::json::Parser parser);
[[nodiscard]] x::json::json to_json(const WriteField &value);

/// @brief ReadEndpoint is a single HTTP endpoint polled by a read task.
struct ReadEndpoint {
    /// @brief key uniquely identifies the endpoint within the task.
    std::string key = "";
    /// @brief method is the HTTP method used to poll the endpoint.
    std::string method = METHOD_GET;
    /// @brief path is the URL path appended to the device's base URL.
    std::string path = "";
    /// @brief headers contains additional headers merged into the request.
    std::vector<Header> headers;
    /// @brief query_params contains query parameters appended to the request URL.
    std::vector<QueryParam> query_params;
    /// @brief body is a static body sent with each request. Empty sends no body.
    std::string body = "";
    /// @brief fields contains the values to extract from the response.
    std::vector<ReadField> fields;
    /// @brief index is the key of the field whose channel indexes the others. Empty
    /// when
    /// the task stamps samples on arrival.
    std::string index = "";

    static ReadEndpoint parse(x::json::Parser parser);
    [[nodiscard]] x::json::json to_json() const;
};

/// @brief WriteEndpoint is a single HTTP endpoint written to by a write task.
struct WriteEndpoint {
    /// @brief key uniquely identifies the endpoint within the task.
    std::string key = "";
    /// @brief disabled is true when the endpoint is excluded from the task.
    bool disabled = false;
    /// @brief method is the HTTP method used to send the request.
    std::string method = METHOD_POST;
    /// @brief path is the URL path appended to the device's base URL.
    std::string path = "";
    /// @brief headers contains additional headers merged into the request.
    std::vector<Header> headers;
    /// @brief query_params contains query parameters appended to the request URL.
    std::vector<QueryParam> query_params;
    /// @brief channel is the command channel whose writes trigger this endpoint.
    ChannelField channel;
    /// @brief fields contains additional static or generated body fields.
    std::vector<WriteField> fields;

    static WriteEndpoint parse(x::json::Parser parser);
    [[nodiscard]] x::json::json to_json() const;
};

/// @brief ReadConfig configures an HTTP read task, which polls one or more endpoints on
/// an HTTP server device and writes extracted JSON values to Synnax channels.
struct ReadConfig : public ::synnax::task::common::BasePersistConfig {
    /// @brief device is the key of the HTTP server device to poll.
    ::synnax::device::Key device = "";
    /// @brief rate is the polling rate applied to all endpoints, in hertz.
    ::x::telem::Rate rate = ::x::telem::Rate(1);
    /// @brief endpoints contains the endpoints to poll.
    std::vector<ReadEndpoint> endpoints;

    static ReadConfig parse(x::json::Parser parser);
    [[nodiscard]] x::json::json to_json() const;
};

/// @brief WriteConfig configures an HTTP write task, which sends an HTTP request
/// whenever a value is written to an endpoint's command channel.
struct WriteConfig : public ::synnax::task::common::BaseStartConfig {
    /// @brief device is the key of the HTTP server device to write to.
    ::synnax::device::Key device = "";
    /// @brief endpoints contains the endpoints to write to.
    std::vector<WriteEndpoint> endpoints;

    static WriteConfig parse(x::json::Parser parser);
    [[nodiscard]] x::json::json to_json() const;
};
}
