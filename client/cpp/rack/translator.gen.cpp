// Copyright 2026 Synnax Labs, Inc.
//
// Use of this software is governed by the Business Source License included in the file
// licenses/BSL.txt.
//
// As of the Change Date specified in that file, in accordance with the Business Source
// License, use of this software will be governed by the Apache License, Version 2.0,
// included in the file licenses/APL.txt.

// Code generated by oracle. DO NOT EDIT.

#include "client/cpp/rack/translator.gen.h"

#include "core/pkg/service/rack/pb/rack.pb.h"

namespace synnax::rack {

service::rack::StatusDetails to_pb(const StatusDetails &cpp) {
    service::rack::StatusDetails pb;
    pb.set_rack(static_cast<uint32_t>(cpp.rack));
    return pb;
}

StatusDetails from_pb(const service::rack::StatusDetails &pb) {
    StatusDetails cpp;
    cpp.rack = Key(pb.rack());
    return cpp;
}

std::vector<service::rack::StatusDetails> to_pb(const std::vector<StatusDetails> &cpp) {
    std::vector<service::rack::StatusDetails> pb;
    pb.reserve(cpp.size());
    for (const auto &item: cpp)
        pb.push_back(to_pb(item));
    return pb;
}

std::vector<StatusDetails>
from_pb(const std::vector<service::rack::StatusDetails> &pb) {
    std::vector<StatusDetails> cpp;
    cpp.reserve(pb.size());
    for (const auto &item: pb)
        cpp.push_back(from_pb(item));
    return cpp;
}

service::rack::Rack to_pb(const Payload &cpp) {
    service::rack::Rack pb;
    pb.set_key(static_cast<uint32_t>(cpp.key));
    pb.set_name(cpp.name);
    pb.set_task_counter(cpp.task_counter);
    pb.set_embedded(cpp.embedded);
    pb.set_status(cpp.status);
    return pb;
}

Payload from_pb(const service::rack::Rack &pb) {
    Payload cpp;
    cpp.key = Key(pb.key());
    cpp.name = pb.name();
    cpp.task_counter = pb.task_counter();
    cpp.embedded = pb.embedded();
    cpp.status = pb.status();
    return cpp;
}

std::vector<service::rack::Rack> to_pb(const std::vector<Payload> &cpp) {
    std::vector<service::rack::Rack> pb;
    pb.reserve(cpp.size());
    for (const auto &item: cpp)
        pb.push_back(to_pb(item));
    return pb;
}

std::vector<Payload> from_pb(const std::vector<service::rack::Rack> &pb) {
    std::vector<Payload> cpp;
    cpp.reserve(pb.size());
    for (const auto &item: pb)
        cpp.push_back(from_pb(item));
    return cpp;
}

}
