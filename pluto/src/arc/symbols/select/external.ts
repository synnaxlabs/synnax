import { Symbol } from "@/arc/symbols/select/Select";

export const SYMBOLS = {
  select: {
    key: "select",
    name: "Select",
    Form: () => null,
    Symbol,
    defaultProps: () => ({}),
    Preview: Symbol,
    zIndex: 0,
  },
};
