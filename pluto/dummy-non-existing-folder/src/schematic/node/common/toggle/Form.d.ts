import { type ReactElement } from "react";
interface ChannelFormProps {
    path: string;
    omit?: string[];
}
export declare const ChannelForm: ({ path, omit }: ChannelFormProps) => ReactElement;
export {};
//# sourceMappingURL=Form.d.ts.map