import { Component } from "@synnaxlabs/lyra/component";
import { Form } from "@synnaxlabs/lyra/form";
import { type text } from "@synnaxlabs/x";
import { type ReactElement } from "react";
export declare const SizeField: (props: Partial<Form.FieldProps<Component.Size>>) => ReactElement;
/** A size field for symbols that store a text level instead of a size. */
export declare const LevelSizeField: (props: Partial<Form.FieldProps<text.Level>>) => ReactElement;
//# sourceMappingURL=Size.d.ts.map