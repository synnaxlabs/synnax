import { type ReactElement } from "react";
import { Primitive } from "../common/primitive";
export interface CapProps extends Primitive.SVGBasedProps, Primitive.DivProps {
}
export declare const Cap: ({ className, orientation, color, scale, ...rest }: CapProps) => ReactElement;
//# sourceMappingURL=Cap.d.ts.map