import { type ReactElement } from "react";
import { Primitive } from "../common/primitive";
export interface FilterProps extends Primitive.SVGBasedProps, Primitive.DivProps {
}
export declare const Filter: ({ className, orientation, color, scale, ...rest }: FilterProps) => ReactElement;
//# sourceMappingURL=Filter.d.ts.map