import { type ReactElement } from "react";
import { Primitive } from "../common/primitive";
export interface OrificeProps extends Primitive.DivProps, Primitive.SVGBasedProps {
}
export declare const Orifice: ({ className, orientation, scale, color, ...rest }: OrificeProps) => ReactElement;
//# sourceMappingURL=Orifice.d.ts.map