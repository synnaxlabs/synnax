import { type ReactElement } from "react";
import { Primitive } from "../common/primitive";
import { Toggle } from "../common/toggle";
export interface CavityProps extends Toggle.ButtonProps, Primitive.SVGBasedProps {
}
export declare const Cavity: ({ color, className, orientation, scale, ...rest }: CavityProps) => ReactElement;
//# sourceMappingURL=Cavity.d.ts.map