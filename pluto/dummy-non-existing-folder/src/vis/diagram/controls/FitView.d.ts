import { Button } from "@synnaxlabs/lyra/button";
import { type ReactElement } from "react";
export interface FitViewProps extends Omit<Button.ToggleProps, "children" | "onChange" | "value"> {
}
export declare const FitView: ({ onClick, ...rest }: FitViewProps) => ReactElement;
//# sourceMappingURL=FitView.d.ts.map