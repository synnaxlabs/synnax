# Copyright 2026 Synnax Labs, Inc.
#
# Use of this software is governed by the Business Source License included in the file
# licenses/BSL.txt.
#
# As of the Change Date specified in that file, in accordance with the Business Source
# License, use of this software will be governed by the Apache License, Version 2.0,
# included in the file licenses/APL.txt.

version: "2"
linters:
  enable:
    - ginkgolinter
    - modernize
    - revive
    - must
    - set
    - goroutineleak
  settings:
    ginkgolinter:
      forbid-focus-container: true
      force-expect-to: true
      validate-async-intervals: true
      force-tonot: true
    govet:
      enable-all: true
      disable:
        - shadow
        - fieldalignment
    revive:
      rules:
        - name: error-naming
        - name: var-declaration
        - name: var-naming
          arguments:
            - []
            - []
            - - skip-package-name-checks: true
    custom:
      must:
        type: "module"
        description: >
          Detects Expect(err).ToNot(HaveOccurred()) patterns that can be replaced with
          MustSucceed.
      set:
        type: "module"
        description: >
          Detects map[T]struct{} and map[T]bool patterns that should use set.Set[T].
      goroutineleak:
        type: "module"
        description: >
          Enforces goroutine leak checks in Ginkgo suites: every suite must register
          ShouldNotLeakGoroutinesPerSpec, and every BeforeSuite/BeforeAll must call
          ShouldNotLeakGoroutines first.
formatters:
  enable:
    - gci
    - gofmt
    - gofumpt
    - goimports
    - golines
    - swaggo
  settings:
    gci:
      no-inline-comments: true
      no-prefix-comments: true
    gofmt:
      rewrite-rules:
        - pattern: "interface{}"
          replacement: "any"
        - pattern: "a[b:len(a)]"
          replacement: "a[b:]"
        - pattern: "a[0:b]"
          replacement: "a[:b]"
        - pattern: "time.Now().Sub(a)"
          replacement: "time.Since(a)"
        - pattern: "a.Sub(time.Now())"
          replacement: "time.Until(a)"
        - pattern: "strings.Replace(a, b, c, -1)"
          replacement: "strings.ReplaceAll(a, b, c)"
        - pattern: "!(a == b)"
          replacement: "a != b"
    gofumpt:
      extra-rules: true
    golines:
      max-len: 88
      shorten-comments: true
  exclusions:
    generated: strict
    paths:
      # Version packages freeze on version bump: Oracle's alias check compares
      # their on-disk text against template output, so formatters must not
      # reshape them.
      - versions/v\d+/
