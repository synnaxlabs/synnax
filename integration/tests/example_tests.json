{
  "$schema": "test_schema.json",
  "sequences": [
    {
      "sequence_name": "Sequential_Tests",
      "sequence_order": "sequential",
      "tests": [
        {
          "case": "example_basic"
        },
        {
          "case": "example_basic",
          "name": "inject_timeout",
          "parameters": {
            "timeout": [1, 2, 3],
            "wait_time": 4
          },
          "expect": "TIMEOUT"
        },
        {
          "case": "example_basic",
          "name": "inject_failure",
          "parameters": {
            "fail_test": true
          },
          "expect": "FAILED"
        }
      ]
    }
  ]
}
