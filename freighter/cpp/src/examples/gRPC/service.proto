syntax = "proto3";

package masa;

service messageService
{
    rpc Exec(Vales) returns (Instruction) {}
}

message Values
{
    repeated float valves = 1;
    int val_x = 2;
    int val_y = 3;
}

message Instruction
{
    string instruct = 1;
}