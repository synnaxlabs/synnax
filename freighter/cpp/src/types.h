/// @brief This file defines the internal types used with the interface.

#pragma once

/// @brief The target, used to identify and contact server.
union Target {
    /// TODO: Fill in for remaining types. 
};

/// @brief The request type.
union Request {
    /// TODO: Fill in for remaining types.
};

/// @brief The response type.
union Response {
    /// TODO: Fill in for remaining types.
};

/// @brief The stream type.
union Stream {
    /// TODO: Fill in for remaining types.
};