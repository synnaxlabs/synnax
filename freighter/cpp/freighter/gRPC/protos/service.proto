syntax = "proto3";

package test;

service Unary {
    rpc Exec(Message) returns (Message) {}
}

service Stream {
    rpc Exec(stream Message) returns (stream Message) {}
}

message Message
{
    string payload = 1;
}
