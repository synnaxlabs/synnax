/**
 * Loads the natural-language parser on mount.
 * @returns true once phrases read, so a memoized suggest function can refresh.
 */
export declare const useLanguage: () => boolean;
//# sourceMappingURL=useLanguage.d.ts.map