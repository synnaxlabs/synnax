@ts output "client/ts/src/access/role"

Role struct {
    key         uuid    @key
    name        string
    description string?
    internal    bool?

    @ontology type "role"
}

New struct extends Role {
    key uuid?

    @ts use_input
}
