@ts output "client/ts/src/workspace"

Workspace struct {
    key    uuid   @id
    name   string @validate required
    layout json

    @ontology type "workspace"
}

New struct extends Workspace {
    key uuid?

    @ts use_input
}
