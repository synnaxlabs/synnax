@ts output "client/ts/src/workspace/schematic"

Schematic struct {
    key      uuid   @key
    name     string
    data     json
    snapshot bool

    @ontology type "schematic"
}

New struct extends Schematic {
    key      uuid?
    snapshot bool?

    @ts use_input
}
