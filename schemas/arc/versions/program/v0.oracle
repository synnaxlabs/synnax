import "schemas/arc/versions/compiler/v0"
import "schemas/arc/versions/ir/v0"

Program struct extends ir.IR, compiler.Output {
    @doc value """is a compiled Arc program combining the intermediate representation with
WebAssembly bytecode, ready for execution."""
    @go marshal
}
