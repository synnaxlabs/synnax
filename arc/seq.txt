main:
start_button
=> pressurization: {
    interval{100ms} -> ox_press{}
    interval{100ms} -> fuel_press{}
    hold_button{} => hold
    copv_temp > 500 => abort
    wait{5s} => abort
    ox_pt > 500 && fuel_pt > 200 => igniter
} => igniter: {

}

abort:
