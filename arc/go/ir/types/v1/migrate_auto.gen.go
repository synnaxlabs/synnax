// Copyright 2026 Synnax Labs, Inc.
//
// Use of this software is governed by the Business Source License included in the file
// licenses/BSL.txt.
//
// As of the Change Date specified in that file, in accordance with the Business Source
// License, use of this software will be governed by the Apache License, Version 2.0,
// included in the file licenses/APL.txt.

// Code generated by oracle. DO NOT EDIT.

package v1

import (
	"context"
	ir "github.com/synnaxlabs/arc/ir"
	types "github.com/synnaxlabs/arc/types"
	typesv0 "github.com/synnaxlabs/arc/types/types/v0"
)

func AutoMigrateFunctions(ctx context.Context, old Functions) (ir.Functions, error) {
	result := make(ir.Functions, len(old))
	for i, v := range old {
		var err error
		if result[i], err = AutoMigrateFunction(ctx, v); err != nil {
			return nil, err
		}
	}
	return result, nil
}

func AutoMigrateFunction(ctx context.Context, old Function) (ir.Function, error) {
	inputs := make(types.Params, len(old.Inputs))
	for i, v := range old.Inputs {
		var err error
		if inputs[i], err = typesv0.AutoMigrateParam(ctx, v); err != nil {
			return ir.Function{}, err
		}
	}
	outputs := make(types.Params, len(old.Outputs))
	for i, v := range old.Outputs {
		var err error
		if outputs[i], err = typesv0.AutoMigrateParam(ctx, v); err != nil {
			return ir.Function{}, err
		}
	}
	return ir.Function{
		Key:      old.Key,
		Body:     ir.Body(old.Body),
		Inputs:   inputs,
		Outputs:  outputs,
		Channels: types.Channels(old.Channels),
	}, nil
}

func AutoMigrateIR(ctx context.Context, old IR) (ir.IR, error) {
	functions := make(ir.Functions, len(old.Functions))
	for i, v := range old.Functions {
		var err error
		if functions[i], err = AutoMigrateFunction(ctx, v); err != nil {
			return ir.IR{}, err
		}
	}
	nodes := make(ir.Nodes, len(old.Nodes))
	for i, v := range old.Nodes {
		var err error
		if nodes[i], err = AutoMigrateNode(ctx, v); err != nil {
			return ir.IR{}, err
		}
	}
	edges := make(ir.Edges, len(old.Edges))
	for i, v := range old.Edges {
		var err error
		if edges[i], err = AutoMigrateEdge(ctx, v); err != nil {
			return ir.IR{}, err
		}
	}
	root, err := AutoMigrateScope(ctx, old.Root)
	if err != nil {
		return ir.IR{}, err
	}
	return ir.IR{
		Functions:   functions,
		Nodes:       nodes,
		Edges:       edges,
		Authorities: ir.Authorities(old.Authorities),
		Root:        root,
	}, nil
}

func AutoMigrateNodes(ctx context.Context, old Nodes) (ir.Nodes, error) {
	result := make(ir.Nodes, len(old))
	for i, v := range old {
		var err error
		if result[i], err = AutoMigrateNode(ctx, v); err != nil {
			return nil, err
		}
	}
	return result, nil
}

func AutoMigrateNode(ctx context.Context, old Node) (ir.Node, error) {
	inputs := make(types.Params, len(old.Inputs))
	for i, v := range old.Inputs {
		var err error
		if inputs[i], err = typesv0.AutoMigrateParam(ctx, v); err != nil {
			return ir.Node{}, err
		}
	}
	outputs := make(types.Params, len(old.Outputs))
	for i, v := range old.Outputs {
		var err error
		if outputs[i], err = typesv0.AutoMigrateParam(ctx, v); err != nil {
			return ir.Node{}, err
		}
	}
	return ir.Node{
		Key:      old.Key,
		Type:     old.Type,
		Inputs:   inputs,
		Outputs:  outputs,
		Channels: types.Channels(old.Channels),
	}, nil
}

func AutoMigrateEdge(_ context.Context, old Edge) (ir.Edge, error) {
	return ir.Edge{
		Source: ir.Handle(old.Source),
		Target: ir.Handle(old.Target),
		Kind:   ir.EdgeKind(old.Kind),
	}, nil
}

func AutoMigrateScope(ctx context.Context, old Scope) (ir.Scope, error) {
	var activation *ir.Handle
	if old.Activation != nil {
		v := ir.Handle(*old.Activation)
		activation = &v
	}
	strata := make([]ir.Members, len(old.Strata))
	for i, v := range old.Strata {
		var err error
		if strata[i], err = AutoMigrateMembers(ctx, v); err != nil {
			return ir.Scope{}, err
		}
	}
	steps := make(ir.Members, len(old.Steps))
	for i, v := range old.Steps {
		var err error
		if steps[i], err = AutoMigrateMember(ctx, v); err != nil {
			return ir.Scope{}, err
		}
	}
	transitions := make([]ir.Transition, len(old.Transitions))
	for i, v := range old.Transitions {
		var err error
		if transitions[i], err = AutoMigrateTransition(ctx, v); err != nil {
			return ir.Scope{}, err
		}
	}
	return ir.Scope{
		Key:         old.Key,
		Mode:        ir.ScopeMode(old.Mode),
		Liveness:    ir.Liveness(old.Liveness),
		Activation:  activation,
		Strata:      strata,
		Steps:       steps,
		Transitions: transitions,
	}, nil
}

func AutoMigrateMembers(ctx context.Context, old Members) (ir.Members, error) {
	result := make(ir.Members, len(old))
	for i, v := range old {
		var err error
		if result[i], err = AutoMigrateMember(ctx, v); err != nil {
			return nil, err
		}
	}
	return result, nil
}

func AutoMigrateMember(ctx context.Context, old Member) (ir.Member, error) {
	var scope *ir.Scope
	if old.Scope != nil {
		v, err := AutoMigrateScope(ctx, *old.Scope)
		if err != nil {
			return ir.Member{}, err
		}
		scope = &v
	}
	return ir.Member{
		NodeKey: old.NodeKey,
		Scope:   scope,
	}, nil
}

func AutoMigrateTransition(_ context.Context, old Transition) (ir.Transition, error) {
	return ir.Transition{
		On:        ir.Handle(old.On),
		TargetKey: old.TargetKey,
	}, nil
}
