// Copyright 2026 Synnax Labs, Inc.
//
// Use of this software is governed by the Business Source License included in the file
// licenses/BSL.txt.
//
// As of the Change Date specified in that file, in accordance with the Business Source
// License, use of this software will be governed by the Apache License, Version 2.0,
// included in the file licenses/APL.txt.

// Code generated by oracle. DO NOT EDIT.

#pragma once

#include <cstdint>
#include <string>
#include <variant>
#include <vector>

#include "x/cpp/json/json.h"

#include "driver/ni/types/types.gen.h"

namespace driver::types {

inline LinearScale LinearScale::parse(x::json::Parser parser) {
    return LinearScale{
        .slope = parser.field<double>("slope"),
        .y_intercept = parser.field<double>("y_intercept"),
        .pre_scaled_units = parser.field<std::string>("pre_scaled_units"),
        .scaled_units = parser.field<std::string>("scaled_units"),
    };
}

inline x::json::json LinearScale::to_json() const {
    x::json::json j;
    j["slope"] = this->slope;
    j["y_intercept"] = this->y_intercept;
    j["pre_scaled_units"] = this->pre_scaled_units;
    j["scaled_units"] = this->scaled_units;
    return j;
}

inline MapScale MapScale::parse(x::json::Parser parser) {
    return MapScale{
        .pre_scaled_min = parser.field<double>("pre_scaled_min"),
        .pre_scaled_max = parser.field<double>("pre_scaled_max"),
        .scaled_min = parser.field<double>("scaled_min"),
        .scaled_max = parser.field<double>("scaled_max"),
    };
}

inline x::json::json MapScale::to_json() const {
    x::json::json j;
    j["pre_scaled_min"] = this->pre_scaled_min;
    j["pre_scaled_max"] = this->pre_scaled_max;
    j["scaled_min"] = this->scaled_min;
    j["scaled_max"] = this->scaled_max;
    return j;
}

inline TableScale TableScale::parse(x::json::Parser parser) {
    return TableScale{
        .pre_scaled_vals = parser.field<std::vector<double>>("pre_scaled_vals"),
        .scaled_vals = parser.field<std::vector<double>>("scaled_vals"),
    };
}

inline x::json::json TableScale::to_json() const {
    x::json::json j;
    j["pre_scaled_vals"] = this->pre_scaled_vals;
    j["scaled_vals"] = this->scaled_vals;
    return j;
}

inline NoneScale NoneScale::parse(x::json::Parser parser) {
    return NoneScale{};
}

inline x::json::json NoneScale::to_json() const {
    x::json::json j;
    return j;
}

inline BaseAIChannel BaseAIChannel::parse(x::json::Parser parser) {
    return BaseAIChannel{
        .key = parser.field<std::string>("key"),
        .name = parser.field<std::string>("name"),
        .enabled = parser.field<bool>("enabled"),
        .port = parser.field<std::int32_t>("port"),
    };
}

inline x::json::json BaseAIChannel::to_json() const {
    x::json::json j;
    j["key"] = this->key;
    j["name"] = this->name;
    j["enabled"] = this->enabled;
    j["port"] = this->port;
    return j;
}

inline MinMaxVal MinMaxVal::parse(x::json::Parser parser) {
    return MinMaxVal{
        .min_val = parser.field<double>("min_val"),
        .max_val = parser.field<double>("max_val"),
    };
}

inline x::json::json MinMaxVal::to_json() const {
    x::json::json j;
    j["min_val"] = this->min_val;
    j["max_val"] = this->max_val;
    return j;
}

inline Terminal Terminal::parse(x::json::Parser parser) {
    return Terminal{
        .terminal_config = parser.field<std::string>("terminal_config"),
    };
}

inline x::json::json Terminal::to_json() const {
    x::json::json j;
    j["terminal_config"] = this->terminal_config;
    return j;
}

inline AIVoltageFields AIVoltageFields::parse(x::json::Parser parser) {
    AIVoltageFields result;
    static_cast<MinMaxVal &>(result) = MinMaxVal::parse(parser);
    static_cast<Terminal &>(result) = Terminal::parse(parser);
    result.custom_scale = parse_scale(parser.child("custom_scale"));
    return result;
}

inline x::json::json AIVoltageFields::to_json() const {
    x::json::json j;
    for (auto &[k, v]: MinMaxVal::to_json().items())
        j[k] = v;
    for (auto &[k, v]: Terminal::to_json().items())
        j[k] = v;
    j["custom_scale"] = to_json(this->custom_scale);
    return j;
}

inline AIAccelFields AIAccelFields::parse(x::json::Parser parser) {
    AIAccelFields result;
    static_cast<MinMaxVal &>(result) = MinMaxVal::parse(parser);
    static_cast<Terminal &>(result) = Terminal::parse(parser);
    result.sensitivity = parser.field<double>("sensitivity");
    result.custom_scale = parse_scale(parser.child("custom_scale"));
    return result;
}

inline x::json::json AIAccelFields::to_json() const {
    x::json::json j;
    for (auto &[k, v]: MinMaxVal::to_json().items())
        j[k] = v;
    for (auto &[k, v]: Terminal::to_json().items())
        j[k] = v;
    j["sensitivity"] = this->sensitivity;
    j["custom_scale"] = to_json(this->custom_scale);
    return j;
}

inline ScaleLinear ScaleLinear::parse(x::json::Parser parser) {
    return ScaleLinear{
        .type = parser.field<std::string>("type"),
        .slope = parser.field<double>("slope"),
        .y_intercept = parser.field<double>("y_intercept"),
        .pre_scaled_units = parser.field<std::string>("pre_scaled_units"),
        .scaled_units = parser.field<std::string>("scaled_units"),
    };
}

inline x::json::json ScaleLinear::to_json() const {
    x::json::json j;
    j["type"] = this->type;
    j["slope"] = this->slope;
    j["y_intercept"] = this->y_intercept;
    j["pre_scaled_units"] = this->pre_scaled_units;
    j["scaled_units"] = this->scaled_units;
    return j;
}

inline ScaleMap ScaleMap::parse(x::json::Parser parser) {
    return ScaleMap{
        .type = parser.field<std::string>("type"),
        .pre_scaled_min = parser.field<double>("pre_scaled_min"),
        .pre_scaled_max = parser.field<double>("pre_scaled_max"),
        .scaled_min = parser.field<double>("scaled_min"),
        .scaled_max = parser.field<double>("scaled_max"),
    };
}

inline x::json::json ScaleMap::to_json() const {
    x::json::json j;
    j["type"] = this->type;
    j["pre_scaled_min"] = this->pre_scaled_min;
    j["pre_scaled_max"] = this->pre_scaled_max;
    j["scaled_min"] = this->scaled_min;
    j["scaled_max"] = this->scaled_max;
    return j;
}

inline ScaleTable ScaleTable::parse(x::json::Parser parser) {
    return ScaleTable{
        .type = parser.field<std::string>("type"),
        .pre_scaled_vals = parser.field<std::vector<double>>("pre_scaled_vals"),
        .scaled_vals = parser.field<std::vector<double>>("scaled_vals"),
    };
}

inline x::json::json ScaleTable::to_json() const {
    x::json::json j;
    j["type"] = this->type;
    j["pre_scaled_vals"] = this->pre_scaled_vals;
    j["scaled_vals"] = this->scaled_vals;
    return j;
}

inline ScaleNone ScaleNone::parse(x::json::Parser parser) {
    return ScaleNone{
        .type = parser.field<std::string>("type"),
    };
}

inline x::json::json ScaleNone::to_json() const {
    x::json::json j;
    j["type"] = this->type;
    return j;
}

inline AIChannelAiVoltage AIChannelAiVoltage::parse(x::json::Parser parser) {
    return AIChannelAiVoltage{
        .type = parser.field<std::string>("type"),
        .key = parser.field<std::string>("key"),
        .name = parser.field<std::string>("name"),
        .enabled = parser.field<bool>("enabled"),
        .port = parser.field<std::int32_t>("port"),
        .min_val = parser.field<double>("min_val"),
        .max_val = parser.field<double>("max_val"),
        .terminal_config = parser.field<std::string>("terminal_config"),
        .custom_scale = parse_scale(parser.child("custom_scale")),
    };
}

inline x::json::json AIChannelAiVoltage::to_json() const {
    x::json::json j;
    j["type"] = this->type;
    j["key"] = this->key;
    j["name"] = this->name;
    j["enabled"] = this->enabled;
    j["port"] = this->port;
    j["min_val"] = this->min_val;
    j["max_val"] = this->max_val;
    j["terminal_config"] = this->terminal_config;
    j["custom_scale"] = to_json(this->custom_scale);
    return j;
}

inline AIChannelAiAccel AIChannelAiAccel::parse(x::json::Parser parser) {
    return AIChannelAiAccel{
        .type = parser.field<std::string>("type"),
        .key = parser.field<std::string>("key"),
        .name = parser.field<std::string>("name"),
        .enabled = parser.field<bool>("enabled"),
        .port = parser.field<std::int32_t>("port"),
        .min_val = parser.field<double>("min_val"),
        .max_val = parser.field<double>("max_val"),
        .terminal_config = parser.field<std::string>("terminal_config"),
        .sensitivity = parser.field<double>("sensitivity"),
        .custom_scale = parse_scale(parser.child("custom_scale")),
    };
}

inline x::json::json AIChannelAiAccel::to_json() const {
    x::json::json j;
    j["type"] = this->type;
    j["key"] = this->key;
    j["name"] = this->name;
    j["enabled"] = this->enabled;
    j["port"] = this->port;
    j["min_val"] = this->min_val;
    j["max_val"] = this->max_val;
    j["terminal_config"] = this->terminal_config;
    j["sensitivity"] = this->sensitivity;
    j["custom_scale"] = to_json(this->custom_scale);
    return j;
}

inline Scale parse_scale(x::json::Parser parser) {
    const auto discriminator = parser.field<std::string>("type");
    if (discriminator == "linear") return ScaleLinear::parse(parser);
    if (discriminator == "map") return ScaleMap::parse(parser);
    if (discriminator == "table") return ScaleTable::parse(parser);
    if (discriminator == "none") return ScaleNone::parse(parser);
    parser.field_err("type", "unknown Scale type: " + discriminator);
    return {};
}

inline x::json::json to_json(const Scale &value) {
    return std::visit([](const auto &v) { return v.to_json(); }, value);
}

inline AIChannel parse_ai_channel(x::json::Parser parser) {
    const auto discriminator = parser.field<std::string>("type");
    if (discriminator == "ai_voltage") return AIChannelAiVoltage::parse(parser);
    if (discriminator == "ai_accel") return AIChannelAiAccel::parse(parser);
    parser.field_err("type", "unknown AIChannel type: " + discriminator);
    return {};
}

inline x::json::json to_json(const AIChannel &value) {
    return std::visit([](const auto &v) { return v.to_json(); }, value);
}

}
