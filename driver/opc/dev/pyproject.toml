[tool.poetry]
package-mode = false

[tool.poetry.dependencies]
asyncua = "1.1.5"
python = "^3.12"
synnax = { path = "../../../client/py", develop = true }

[tool.poetry.group.dev.dependencies]
black = "^25.1.0"
isort = "^6.0.1"
poetry = "^2.2.0"

[tool.isort]
profile = "black"
