cc_library(
    name = "lua",
    srcs = glob(["src/*.c"]),
    hdrs = glob([
        "src/*.h",
        "src/*.hpp",
    ]),
    copts = [
        "-DLUA_USE_POSIX",
        "-DLUA_USE_DLOPEN",
    ],
    defines = ["LUA_COMPAT_5_3"],
    includes = ["src"],
    linkopts = [
        "-lm",
        "-ldl",
    ],
    visibility = ["//visibility:public"],
)