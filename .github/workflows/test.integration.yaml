name: Test - Integration

run-name:
  ${{ github.event.inputs.RUN_NAME || format('Test - Integration {0}', github.ref_name)
  }}

on:
  push:
    branches:
      - sy-3345-aws-build-bots
  pull_request:
    branches:
      - main
      - rc
    paths:
      - .bazeliskrc
      - .bazelrc
      - .github/workflows/test.integration.yaml
      - alamos/go/**
      - alamos/py/**
      - alamos/ts/**
      - aspen/**
      - cesium/**
      - client/**
      - configs/ts/**
      - configs/vite/**
      - console/**
      - core/**
      - drift/**
      - driver/**
      - Earthfile
      - freighter/cpp/**
      - freighter/go/**
      - freighter/ts/**
      - freighter/py/**
      - go.work
      - go.work.sum
      - integration/**
      - MODULE.bazel
      - MODULE.bazel.lock
      - package.json
      - pluto/**
      - pnpm-lock.yaml
      - pnpm-workspace.yaml
      - turbo.json
      - vendor
      - x/cpp/**
      - x/go/**
      - x/media/**
      - x/ts/**

  workflow_dispatch:
    inputs:
      RUN_NAME:
        description: "Custom name for this workflow run"
        required: false
        default: ""
        type: string
      SKIP_BUILD:
        description: "Skip build process and use artifacts from another run"
        required: false
        default: false
        type: boolean
      REF_RUN_ID:
        description:
          "Reference run ID for artifacts (when SKIP_BUILD is true, blank for most
          recent)"
        required: false
        default: ""
        type: string
      SEQUENCE:
        description:
          "Test case sequence to run (optional - will auto-discover if empty)"
        required: false
        default: ""
        type: string
      BUILD_UBUNTU:
        description: "Build and test on Ubuntu"
        required: false
        default: true
        type: boolean
      BUILD_WINDOWS:
        description: "Build and test on Windows"
        required: false
        default: true
        type: boolean

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

env:
  CACHE_SCOPE: deploy-synnax

jobs:
  lint-python:
    name: Lint - Python
    runs-on: ubuntu-latest
    steps:
      - name: Checkout Repository
        uses: actions/checkout@v5

      - name: Set up Poetry
        run: pipx install poetry

      - name: Set up Python
        uses: actions/setup-python@v6
        with:
          python-version-file: integration/pyproject.toml
          cache: poetry
          cache-dependency-path: integration/poetry.lock

      - name: Install Dependencies
        run: |
          poetry install
        working-directory: integration

      - name: isort
        run: poetry run isort -c .
        working-directory: integration

      - name: Format (Black)
        run: poetry run black --check --diff --color .
        working-directory: integration

      - name: Type Check (mypy)
        run: poetry run mypy .
        working-directory: integration

  setup:
    runs-on: ubuntu-latest
    outputs:
      VERSION: ${{ steps.version.outputs.VERSION }}
      SKIP_BUILD:
        ${{ steps.update-outputs.outputs.SKIP_BUILD ||
        steps.set-outputs.outputs.SKIP_BUILD }}
      REF_RUN_ID:
        ${{ steps.update-outputs.outputs.REF_RUN_ID ||
        steps.set-outputs.outputs.REF_RUN_ID }}
      SEQUENCE_ARG: ${{ steps.set-outputs.outputs.SEQUENCE_ARG }}
      BUILD_UBUNTU: ${{ steps.set-outputs.outputs.BUILD_UBUNTU }}
      BUILD_WINDOWS: ${{ steps.set-outputs.outputs.BUILD_WINDOWS }}
    steps:
      - name: Checkout Repository
        uses: actions/checkout@v5

      - name: Get Version
        id: version
        working-directory: core
        run: |
          if [ "${GITHUB_REF}" == "refs/heads/main" ]; then
            echo "VERSION=$(cat pkg/version/VERSION)" >> $GITHUB_OUTPUT
          else
            echo "VERSION=$(cat pkg/version/VERSION)-rc" >> $GITHUB_OUTPUT
          fi

      - name: Set outputs
        id: set-outputs
        env:
          SKIP_BUILD: ${{ github.event.inputs.SKIP_BUILD || 'false' }}
          REF_RUN_ID: ${{ github.event.inputs.REF_RUN_ID || '' }}
          SEQUENCE: ${{ github.event.inputs.SEQUENCE || '' }}
          BUILD_UBUNTU: ${{ github.event.inputs.BUILD_UBUNTU || 'true' }}
          BUILD_WINDOWS: ${{ github.event.inputs.BUILD_WINDOWS || 'true' }}
          GH_TOKEN: ${{ github.token }}
        run: |
          if [ -z "${SEQUENCE}" ]; then
            echo "SEQUENCE_ARG=" >> $GITHUB_OUTPUT
          else
            echo "SEQUENCE_ARG=--sequence ${SEQUENCE}" >> $GITHUB_OUTPUT
          fi

          echo "SKIP_BUILD=${SKIP_BUILD}" >> $GITHUB_OUTPUT
          if [ "${SKIP_BUILD}" = "true" ]; then
            if [ -z "${REF_RUN_ID}" ]; then
              echo "Empty REF_RUN_ID. Setting SKIP_BUILD to false."
              echo "SKIP_BUILD=false" >> $GITHUB_OUTPUT
            else
              echo "REF_RUN_ID=${REF_RUN_ID}" >> $GITHUB_OUTPUT
            fi
          fi
      - name: Check for Cached Artifacts
        if: ${{ steps.set-outputs.outputs.SKIP_BUILD == 'false' }}
        id: check-cache
        env:
          GH_TOKEN: ${{ github.token }}
        run: integration/scripts/check_artifact_cache.sh all

      - name: Update Outputs Based on Cache
        if: ${{ steps.set-outputs.outputs.SKIP_BUILD == 'false' }}
        id: update-outputs
        run: |
          if [ "${{ steps.check-cache.outputs.CACHE_HIT }}" = "true" ]; then
            echo "Using cached artifacts from run ${{ steps.check-cache.outputs.CACHED_RUN_ID }}"
            echo "SKIP_BUILD=true" >> $GITHUB_OUTPUT
            echo "REF_RUN_ID=${{ steps.check-cache.outputs.CACHED_RUN_ID }}" >> $GITHUB_OUTPUT
          else
            echo "Using this runs artifacts - ${{ github.run_id }}"
            echo "REF_RUN_ID=${{ github.run_id }}" >> $GITHUB_OUTPUT
          fi

  build-ubuntu:
    name: Build (ubuntu)
    timeout-minutes: 30
    runs-on: ubuntu-build-bot
    if: ${{ needs.setup.outputs.BUILD_UBUNTU == 'true' }}
    needs: [setup]
    env:
      TURBO_TOKEN: ${{ secrets.TURBO_TOKEN }}
      TURBO_TEAM: ${{ vars.TURBO_TEAM }}
      HOME: /home/ubuntu
    steps:
      - name: Skip Build
        if: ${{ needs.setup.outputs.SKIP_BUILD == 'true' }}
        run: echo "Build skipped - using reference artifacts"

      - name: Checkout Repository
        if: ${{ needs.setup.outputs.SKIP_BUILD == 'false' }}
        uses: actions/checkout@v5

      - name: Update Submodules
        if: ${{ needs.setup.outputs.SKIP_BUILD == 'false' }}
        run: git submodule update --init --recursive

      - name: Setup Bazel
        if: ${{ needs.setup.outputs.SKIP_BUILD == 'false' }}
        uses: bazel-contrib/setup-bazel@0.15.0
        with:
          bazelisk-cache: true
          disk-cache: ${{ env.CACHE_SCOPE }}-ubuntu
          repository-cache: true

      - name: Build Driver
        if: ${{ needs.setup.outputs.SKIP_BUILD == 'false' }}
        run: |
          bazel build --enable_platform_specific_config -c opt --config=hide_symbols --announce_rc //driver

      - name: Move Driver to Assets
        if: ${{ needs.setup.outputs.SKIP_BUILD == 'false' }}
        run: |
          mv bazel-bin/driver/driver core/pkg/service/hardware/embedded/assets/driver

      - name: Set up Go
        if: ${{ needs.setup.outputs.SKIP_BUILD == 'false' }}
        uses: actions/setup-go@v6
        with:
          go-version-file: go.work
          cache-dependency-path: |
            go.work.sum
            **/go.sum

      - name: Set up pnpm
        if: ${{ needs.setup.outputs.SKIP_BUILD == 'false' }}
        uses: pnpm/action-setup@v4

      - name: Set up Node.js
        if: ${{ needs.setup.outputs.SKIP_BUILD == 'false' }}
        uses: actions/setup-node@v5
        with:
          node-version-file: package.json
          cache: pnpm

      - name: Install Node Dependencies
        if: ${{ needs.setup.outputs.SKIP_BUILD == 'false' }}
        run: pnpm install

      - name: Build Console Web Assets
        if: ${{ needs.setup.outputs.SKIP_BUILD == 'false' }}
        env:
          NODE_OPTIONS: --max_old_space_size=8192
        run: pnpm build:console-vite

      - name: Copy Console Assets to Server
        if: ${{ needs.setup.outputs.SKIP_BUILD == 'false' }}
        run: cp -r console/dist/* core/pkg/service/console/dist/

      - name: Build
        if: ${{ needs.setup.outputs.SKIP_BUILD == 'false' }}
        working-directory: core
        run: |
          go build -tags console,driver -o synnax-v${{ needs.setup.outputs.VERSION }}-linux

      - name: Upload Core Binary as Artifact
        if: ${{ needs.setup.outputs.SKIP_BUILD == 'false' }}
        uses: actions/upload-artifact@v4
        with:
          name: synnax-core-linux
          path: core/synnax-v${{ needs.setup.outputs.VERSION }}-linux
          retention-days: 7
          if-no-files-found: error

      - name: Debug Build Environment
        if: failure()
        run: integration/scripts/debug_build_environment_unix.sh

  build-windows:
    name: Build (windows)
    timeout-minutes: 30
    runs-on: windows-build-bot
    if: ${{ needs.setup.outputs.BUILD_WINDOWS == 'true' }}
    needs: [setup]
    env:
      TURBO_TOKEN: ${{ secrets.TURBO_TOKEN }}
      TURBO_TEAM: ${{ vars.TURBO_TEAM }}
    steps:
      - name: Skip Build
        if: ${{ needs.setup.outputs.SKIP_BUILD == 'true' }}
        run: echo "Build skipped - using reference artifacts"

      - name: Checkout Repository
        if: ${{ needs.setup.outputs.SKIP_BUILD == 'false' }}
        uses: actions/checkout@v5

      - name: Update Submodules
        if: ${{ needs.setup.outputs.SKIP_BUILD == 'false' }}
        run: git submodule update --init --recursive

      - name: Setup Bazel
        if: ${{ needs.setup.outputs.SKIP_BUILD == 'false' }}
        uses: bazel-contrib/setup-bazel@0.15.0
        with:
          bazelisk-cache: true
          disk-cache: ${{ env.CACHE_SCOPE }}-windows
          repository-cache: true

      - name: Build Driver
        if: ${{ needs.setup.outputs.SKIP_BUILD == 'false' }}
        shell: powershell
        run: |
          bazel --output_user_root=C:/tmp build --enable_platform_specific_config -c opt --announce_rc //driver

      - name: Move Driver to Assets
        if: ${{ needs.setup.outputs.SKIP_BUILD == 'false' }}
        shell: powershell
        run: |
          Move-Item -Force bazel-bin/driver/driver.exe core/pkg/service/hardware/embedded/assets/driver.exe

      - name: Set up Go
        if: ${{ needs.setup.outputs.SKIP_BUILD == 'false' }}
        uses: actions/setup-go@v6
        with:
          go-version-file: go.work
          cache-dependency-path: |
            go.work.sum
            **/go.sum

      - name: Set up pnpm
        if: ${{ needs.setup.outputs.SKIP_BUILD == 'false' }}
        uses: pnpm/action-setup@v4

      - name: Set up Node.js
        if: ${{ needs.setup.outputs.SKIP_BUILD == 'false' }}
        uses: actions/setup-node@v5
        with:
          node-version-file: package.json
          cache: pnpm

      - name: Install Node Dependencies
        if: ${{ needs.setup.outputs.SKIP_BUILD == 'false' }}
        run: pnpm install

      - name: Build Console Web Assets
        if: ${{ needs.setup.outputs.SKIP_BUILD == 'false' }}
        env:
          NODE_OPTIONS: --max_old_space_size=8192
        run: pnpm build:console-vite

      - name: Copy Console Assets to Server
        if: ${{ needs.setup.outputs.SKIP_BUILD == 'false' }}
        shell: powershell
        run: |
          Copy-Item -Recurse -Force console/dist/* core/pkg/service/console/dist/

      - name: Build
        if: ${{ needs.setup.outputs.SKIP_BUILD == 'false' }}
        working-directory: core
        run: |
          go build -tags console,driver -o synnax-v${{ needs.setup.outputs.VERSION }}-windows.exe

      - name: Upload Core Binary as Artifact
        if: ${{ needs.setup.outputs.SKIP_BUILD == 'false' }}
        uses: actions/upload-artifact@v4
        with:
          name: synnax-core-windows
          path: core/synnax-v${{ needs.setup.outputs.VERSION }}-windows.exe
          retention-days: 7
          if-no-files-found: error

      - name: Debug Build Environment
        if: failure()
        shell: cmd
        run: integration/scripts/DebugBuildEnvironmentWindows.cmd

  deploy-and-test-windows:
    name: Deploy and Test (windows)
    timeout-minutes: 30
    runs-on:
      - self-hosted
      - windows-latest
    if: ${{ needs.setup.outputs.BUILD_WINDOWS == 'true' }}
    needs: [setup, build-windows]
    steps:
      - name: Checkout Repository
        uses: actions/checkout@v5

      - name: Force Quit Existing Synnax Processes (Windows)
        shell: cmd
        run: integration/scripts/KillSynnaxProcessesWindows.cmd

      - name: Download and Setup Windows Artifacts
        shell: cmd
        env:
          GH_TOKEN: ${{ github.token }}
          REF_RUN_ID: ${{ needs.setup.outputs.REF_RUN_ID }}
        run: integration/scripts/DownloadArtifactsWindows.cmd

      - name: Update Submodules
        run: git submodule update --init --recursive

      - name: Set up Python
        uses: actions/setup-python@v6
        with:
          python-version-file: integration/pyproject.toml

      - name: Install Poetry & Dependencies
        shell: cmd
        run: integration/scripts/InstallPoetryDepsWindows.cmd

      - name: Install Playwright
        shell: cmd
        working-directory: integration
        run: |
          set PATH=%APPDATA%\Python\Scripts;%APPDATA%\pypoetry\venv\Scripts;%PATH%
          poetry run playwright install --with-deps

      - name: Start Core
        timeout-minutes: 1
        shell: powershell
        env:
          SYNNAX_LICENSE_KEY: ${{ secrets.SYNNAX_LICENSE_KEY }}
        run: integration/scripts/StartCoreWindows.ps1

      - name: Test Conductor
        id: test-conductor
        timeout-minutes: 20
        shell: cmd
        working-directory: integration
        env:
          PYTHONIOENCODING: utf-8
        run: |
          set PATH=%APPDATA%\Python\Scripts;%APPDATA%\pypoetry\venv\Scripts;%PATH%
          poetry run test-conductor --name tc_win ${{ needs.setup.outputs.SEQUENCE_ARG }}

      - name: Upload Test Results
        uses: actions/upload-artifact@v4
        if: always()
        with:
          name: test-results-windows
          path: integration/tests/results/*
          retention-days: 7

      - name: Debug Test Environment
        if: failure()
        shell: cmd
        run: integration/scripts/DebugTestEnvironmentWindows.cmd

  deploy-and-test-ubuntu:
    name: Deploy and Test (ubuntu)
    timeout-minutes: 30
    runs-on:
      - self-hosted
      - ubuntu-latest
    if: ${{ needs.setup.outputs.BUILD_UBUNTU == 'true' }}
    needs: [setup, build-ubuntu]
    steps:
      - name: Checkout Repository
        uses: actions/checkout@v5

      - name: Force Quit Existing Synnax Processes
        continue-on-error: true
        run: integration/scripts/kill_synnax_processes_unix.sh

      - name: Download and Setup Linux Artifacts
        env:
          GH_TOKEN: ${{ github.token }}
          REF_RUN_ID: ${{ needs.setup.outputs.REF_RUN_ID }}
        run: integration/scripts/download_artifacts_unix.sh

      - name: Update Submodules
        run: git submodule update --init --recursive

      - name: Set up Python
        uses: actions/setup-python@v6
        with:
          python-version-file: integration/pyproject.toml

      - name: Install Poetry & Dependencies
        run: integration/scripts/install_poetry_deps_unix.sh

      - name: Install Playwright
        run: integration/scripts/install_playwright_unix.sh

      - name: Start Core
        timeout-minutes: 1
        env:
          SYNNAX_LICENSE_KEY: ${{ secrets.SYNNAX_LICENSE_KEY }}
        run: integration/scripts/start_core_unix.sh

      - name: Test Conductor
        id: test-conductor
        timeout-minutes: 20
        working-directory: integration
        run:
          poetry run test-conductor --name tc_unix ${{ needs.setup.outputs.SEQUENCE_ARG
          }}

      - name: Upload Test Results
        uses: actions/upload-artifact@v4
        if: always()
        with:
          name: test-results-linux
          path: integration/tests/results/*
          retention-days: 7

      - name: Debug Test Environment
        if: failure()
        run: integration/scripts/debug_test_environment_unix.sh
