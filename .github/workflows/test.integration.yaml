name: "Test - Integration"

on:
  pull_request:
    branches:
      - rc
      - main
  push:
    branches:
      - rc
      - main
      - 'leo/sy-685-integration-testing'
  workflow_dispatch:

permissions:
  contents: write
  packages: write

jobs:
  test-integration:
    runs-on: self-hosted
    steps:
      - name: Checkout Repository
        uses: actions/checkout@v3

      - name: Diff Changes
        uses: dorny/paths-filter@v3
        id: filter
        with:
          filters: |
            changed:
                - "**/*"

      - name: Install Dependencies
        run: pnpm i

      - name: Build things
        run: pnpm build:client

      - name: Poetry install
        run: poetry install

      - name: Test
        run: ./run-integration.sh basic.json
        working-directory: ./integration
