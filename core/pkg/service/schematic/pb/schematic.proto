// Copyright 2026 Synnax Labs, Inc.
//
// Use of this software is governed by the Business Source License included in the file
// licenses/BSL.txt.
//
// As of the Change Date specified in that file, in accordance with the Business Source
// License, use of this software will be governed by the Apache License, Version 2.0,
// included in the file licenses/APL.txt.

// Code generated by oracle. DO NOT EDIT.

syntax = "proto3";

package service.schematic.pb;

import "google/protobuf/struct.proto";
import "x/go/border/pb/border.proto";
import "x/go/color/pb/color.proto";
import "x/go/notation/pb/notation.proto";
import "x/go/spatial/pb/spatial.proto";
import "x/go/text/pb/text.proto";

option go_package = "github.com/synnaxlabs/synnax/pkg/service/schematic/pb";

// FlexAlignment is a cross-axis flex alignment for laid-out content.
enum FlexAlignment {
  FLEX_ALIGNMENT_START = 0;
  FLEX_ALIGNMENT_CENTER = 1;
  FLEX_ALIGNMENT_END = 2;
  FLEX_ALIGNMENT_STRETCH = 3;
}

// ComponentSize is a discrete size preset for interactive components.
enum ComponentSize {
  COMPONENT_SIZE_TINY = 0;
  COMPONENT_SIZE_SMALL = 1;
  COMPONENT_SIZE_MEDIUM = 2;
  COMPONENT_SIZE_LARGE = 3;
  COMPONENT_SIZE_HUGE = 4;
}

// ButtonMode is the actuation behavior of a button: fire writes once on click,
// momentary writes on press and clears on release, and pulse writes a one-cycle pulse.
enum ButtonMode {
  BUTTON_MODE_FIRE = 0;
  BUTTON_MODE_MOMENTARY = 1;
  BUTTON_MODE_PULSE = 2;
}

// Node is a diagram node representing a symbol in the schematic.
message Node {
  // key is the unique node identifier within the schematic.
  string key = 1;
  // position is the top-left position of the node.
  .x.spatial.pb.XY position = 2;
  // z_index is the stacking order of the node within the schematic. Higher values render
  // above lower values. Set by the user via send-to-back / bring-to-front actions.
  int32 z_index = 3;
  // measured is the rendered pixel size of the node. Populated by the renderer after the
  // node is mounted and used to keep diagram measurements consistent across re-renders.
  .x.spatial.pb.Dimensions measured = 4;
}

// Handle is a reference to a specific connection point on a specific node. For
// schematics, param is the symbol handle key (e.g. inlet, outlet).
message Handle {
  // node is the node identifier.
  string node = 1;
  // param is the connection point identifier on the node.
  string param = 2;
}

// Edge is a connection between two nodes in the schematic.
message Edge {
  // key is the unique edge identifier within the schematic.
  string key = 1;
  // source is the source endpoint of the edge.
  Handle source = 2;
  // target is the target endpoint of the edge.
  Handle target = 3;
}

// Segment is a single orthogonal run in an edge's connector path.
message Segment {
  // direction is the axis along which the segment travels.
  .x.spatial.pb.Direction direction = 1;
  // length is the signed length of the segment in pixels.
  double length = 2;
}

// SegmentedEdgeConfig is the configuration shared by every segmented edge variant.
message SegmentedEdgeConfig {
  // color is the stroke color of the edge.
  optional .x.color.pb.Color color = 1;
  // segments is the ordered list of orthogonal runs that trace the connector path from
  // the source handle to the target handle.
  repeated Segment segments = 2;
}

// LabelConfig is the text label configuration shared by schematic symbols.
message LabelConfig {
  // label is the text content of the label.
  string label = 1;
  // level is the typography level of the label text.
  optional .x.text.pb.Level level = 2;
  // orientation is the placement of the label relative to the symbol.
  optional .x.spatial.pb.Location orientation = 3;
  // direction is the axis along which the label text flows.
  optional .x.spatial.pb.Direction direction = 4;
  // max_inline_size is the maximum inline size of the label in pixels before wrapping.
  optional double max_inline_size = 5;
  // align is the alignment of the label text within its box.
  optional FlexAlignment align = 6;
}

// LabeledConfig is the base configuration for any symbol that carries a label.
message LabeledConfig {
  // label is the symbol's label configuration.
  optional LabelConfig label = 1;
  // orientation is the orientation of the symbol's primitive within the diagram.
  optional .x.spatial.pb.OuterLocation orientation = 2;
}

// ControlStateConfig is the control authority and state display configuration for
// actuated symbols.
message ControlStateConfig {
  // authority is the control authority requested when the symbol acquires its command
  // channel. Defaults to absolute authority when unset.
  optional uint32 authority = 1;
  // show indicates whether the control state widget is visible.
  bool show = 2;
  // show_chip indicates whether the authority chip is visible.
  bool show_chip = 3;
  // show_indicator indicates whether the state indicator is visible.
  bool show_indicator = 4;
  // orientation is the placement of the control state widget relative to the symbol.
  optional .x.spatial.pb.Location orientation = 5;
}

// ToggleConfig is the base configuration for symbols actuated through a boolean
// telemetry pair.
message ToggleConfig {
  // label is the symbol's label configuration.
  optional LabelConfig label = 1;
  // orientation is the orientation of the symbol's primitive within the diagram.
  optional .x.spatial.pb.OuterLocation orientation = 2;
  // state_channel is the channel whose value drives the symbol's active state.
  optional uint32 state_channel = 3;
  // command_channel is the channel actuation commands are written to.
  optional uint32 command_channel = 4;
  // control is the control state display configuration.
  optional ControlStateConfig control = 5;
  // on_click_delay is the debounce delay applied to clicks, in milliseconds.
  double on_click_delay = 6;
}

// StaticSymbolConfig is the configuration for non-interactive labeled symbols.
message StaticSymbolConfig {
  // label is the symbol's label configuration.
  optional LabelConfig label = 1;
  // orientation is the orientation of the symbol's primitive within the diagram.
  optional .x.spatial.pb.OuterLocation orientation = 2;
  // color is the stroke color of the symbol.
  optional .x.color.pb.Color color = 3;
  // scale is the rendered scale multiplier of the symbol.
  optional double scale = 4;
}

// ToggleSymbolConfig is the configuration for telemetry-actuated toggle symbols.
message ToggleSymbolConfig {
  // label is the symbol's label configuration.
  optional LabelConfig label = 1;
  // orientation is the orientation of the symbol's primitive within the diagram.
  optional .x.spatial.pb.OuterLocation orientation = 2;
  // state_channel is the channel whose value drives the symbol's active state.
  optional uint32 state_channel = 3;
  // command_channel is the channel actuation commands are written to.
  optional uint32 command_channel = 4;
  // control is the control state display configuration.
  optional ControlStateConfig control = 5;
  // on_click_delay is the debounce delay applied to clicks, in milliseconds.
  double on_click_delay = 6;
  // color is the stroke color of the symbol.
  optional .x.color.pb.Color color = 7;
  // scale is the rendered scale multiplier of the symbol.
  optional double scale = 8;
}

// DummyToggleSymbolConfig is the configuration for symbols that toggle their appearance
// from local state without binding to telemetry.
message DummyToggleSymbolConfig {
  // label is the symbol's label configuration.
  optional LabelConfig label = 1;
  // orientation is the orientation of the symbol's primitive within the diagram.
  optional .x.spatial.pb.OuterLocation orientation = 2;
  // enabled indicates whether the symbol renders in its active state.
  bool enabled = 3;
  // clickable indicates whether clicking the symbol toggles its state.
  bool clickable = 4;
  // color is the stroke color of the symbol.
  optional .x.color.pb.Color color = 5;
  // scale is the rendered scale multiplier of the symbol.
  optional double scale = 6;
}

// StateMapping maps a numeric channel value to a named, colored state.
message StateMapping {
  // key is the unique identifier for the mapping entry.
  string key = 1;
  // name is the display name for this state.
  string name = 2;
  // value is the channel value this state corresponds to.
  double value = 3;
  // color is the display color associated with this state.
  optional .x.color.pb.Color color = 4;
}

// Redline maps a numeric range to a color gradient for limit visualization.
message Redline {
  // bounds is the numeric range mapped onto the gradient.
  .x.spatial.pb.Bounds bounds = 1;
  // gradient is the color gradient applied across the bounds.
  repeated .x.color.pb.Stop gradient = 2;
}

// Schematic is a visual diagram editor component for drawing system schematics, control
// flows, and process diagrams. Schematics support interactive symbols, connection
// handles, and dynamic state visualization.
message Schematic {
  // key is the unique identifier for this schematic.
  string key = 1;
  // name is a human-readable name for the schematic.
  string name = 2;
  // snapshot indicates whether this schematic represents a saved snapshot state.
  bool snapshot = 3;
  // nodes contains all diagram nodes in the schematic.
  repeated Node nodes = 4;
  // edges contains all connections between nodes.
  repeated Edge edges = 5;
  // configs contains per-element configuration keyed by node or edge key. The variant of
  // each value selects the symbol or edge style it configures.
  map<string, ElementConfig> configs = 6;
}
message NodeConfigBoxPayload {
  // label is the box's label configuration.
  optional LabelConfig label = 1;
  // orientation is the orientation of the box within the diagram.
  optional .x.spatial.pb.OuterLocation orientation = 2;
  // color is the border color of the box.
  optional .x.color.pb.Color color = 3;
  // background_color is the fill color of the box.
  optional .x.color.pb.Color background_color = 4;
  // dimensions is the rendered size of the box in pixels.
  optional .x.spatial.pb.Dimensions dimensions = 5;
  // border_radius is the uniform corner radius of the box in pixels.
  optional double border_radius = 6;
  // stroke_width is the border stroke width in pixels.
  optional double stroke_width = 7;
}
message NodeConfigButtonPayload {
  // label is the symbol's label configuration.
  optional LabelConfig label = 1;
  // orientation is the orientation of the symbol's primitive within the diagram.
  optional .x.spatial.pb.OuterLocation orientation = 2;
  // size is the rendered size preset of the button.
  optional ComponentSize size = 3;
  // level is the typography level of the button text.
  optional .x.text.pb.Level level = 4;
  // on_click_delay is the debounce delay applied to clicks, in milliseconds.
  double on_click_delay = 5;
  // command_channel is the channel button presses are written to.
  optional uint32 command_channel = 6;
  // mode is the actuation behavior of the button.
  optional ButtonMode mode = 7;
  // color is the background color of the button.
  optional .x.color.pb.Color color = 8;
  // control is the control state display configuration.
  optional ControlStateConfig control = 9;
}
message NodeConfigCirclePayload {
  // label is the symbol's label configuration.
  optional LabelConfig label = 1;
  // orientation is the orientation of the symbol's primitive within the diagram.
  optional .x.spatial.pb.OuterLocation orientation = 2;
  // radius is the radius of the circle in pixels.
  double radius = 3;
  // color is the border color of the circle.
  optional .x.color.pb.Color color = 4;
  // background_color is the fill color of the circle.
  optional .x.color.pb.Color background_color = 5;
  // stroke_width is the border stroke width in pixels.
  optional double stroke_width = 6;
}
message NodeConfigGaugePayload {
  // label is the symbol's label configuration.
  optional LabelConfig label = 1;
  // orientation is the orientation of the symbol's primitive within the diagram.
  optional .x.spatial.pb.OuterLocation orientation = 2;
  // position is the offset of the gauge contents within the symbol.
  optional .x.spatial.pb.XY position = 3;
  // color is the accent color of the gauge arc.
  optional .x.color.pb.Color color = 4;
  // bounds is the numeric range displayed by the gauge.
  optional .x.spatial.pb.Bounds bounds = 5;
  // bar_width is the thickness of the gauge arc in pixels.
  optional double bar_width = 6;
  // channel is the channel whose value the gauge displays.
  optional uint32 channel = 7;
  // rolling_average is the sample window for rolling-average smoothing.
  optional int32 rolling_average = 8;
  // precision is the number of decimal places shown.
  optional double precision = 9;
  // min_width is the minimum rendered width of the value in pixels.
  optional double min_width = 10;
  // width is the rendered width of the gauge in pixels.
  optional double width = 11;
  // notation is the numeric notation used to format the value.
  optional .x.notation.pb.Notation notation = 12;
  // location is the anchor of the value within the gauge.
  optional .x.spatial.pb.LocationXY location = 13;
  // units is the unit suffix displayed after the value.
  string units = 14;
  // level is the typography level of the displayed value.
  optional .x.text.pb.Level level = 15;
}
message NodeConfigInputPayload {
  // label is the symbol's label configuration.
  optional LabelConfig label = 1;
  // orientation is the orientation of the symbol's primitive within the diagram.
  optional .x.spatial.pb.OuterLocation orientation = 2;
  // size is the rendered size preset of the input.
  optional ComponentSize size = 3;
  // command_channel is the channel submitted values are written to.
  optional uint32 command_channel = 4;
  // dimensions is the rendered size of the input in pixels.
  optional .x.spatial.pb.Dimensions dimensions = 5;
  // color is the accent color of the input.
  optional .x.color.pb.Color color = 6;
  // disabled indicates whether the input rejects interaction.
  bool disabled = 7;
  // control is the control state display configuration.
  optional ControlStateConfig control = 8;
}
message NodeConfigLightPayload {
  // label is the symbol's label configuration.
  optional LabelConfig label = 1;
  // orientation is the orientation of the symbol's primitive within the diagram.
  optional .x.spatial.pb.OuterLocation orientation = 2;
  // channel is the channel whose value drives the light's on state.
  optional uint32 channel = 3;
  // threshold is the value range within which the light is considered on.
  optional .x.spatial.pb.Bounds threshold = 4;
  // color is the illuminated color of the light.
  optional .x.color.pb.Color color = 5;
  // scale is the rendered scale multiplier of the symbol.
  optional double scale = 6;
}
message NodeConfigOffPageReferencePayload {
  // orientation is the direction the reference arrow points.
  optional .x.spatial.pb.OuterLocation orientation = 1;
  // label is the label displayed inside the reference.
  LabelConfig label = 2;
  // level is the typography level of the reference text.
  optional .x.text.pb.Level level = 3;
  // color is the fill color of the reference.
  optional .x.color.pb.Color color = 4;
  // page is the key of the schematic this reference links to.
  string page = 5;
  // dbl_click_nav indicates whether double-clicking navigates to the linked schematic.
  bool dbl_click_nav = 6;
}
message NodeConfigPolygonPayload {
  // label is the symbol's label configuration.
  optional LabelConfig label = 1;
  // orientation is the orientation of the symbol's primitive within the diagram.
  optional .x.spatial.pb.OuterLocation orientation = 2;
  // num_sides is the number of sides of the polygon.
  double num_sides = 3;
  // side_length is the length of each side in pixels.
  double side_length = 4;
  // rotation is the rotation of the polygon in degrees.
  optional double rotation = 5;
  // corner_rounding is the corner rounding radius in pixels.
  optional double corner_rounding = 6;
  // color is the border color of the polygon.
  optional .x.color.pb.Color color = 7;
  // background_color is the fill color of the polygon.
  optional .x.color.pb.Color background_color = 8;
  // stroke_width is the border stroke width in pixels.
  optional double stroke_width = 9;
}
message NodeConfigSelectPayload {
  // label is the symbol's label configuration.
  optional LabelConfig label = 1;
  // orientation is the orientation of the symbol's primitive within the diagram.
  optional .x.spatial.pb.OuterLocation orientation = 2;
  // size is the rendered size preset of the select.
  optional ComponentSize size = 3;
  // command_channel is the channel the selected value is written to.
  optional uint32 command_channel = 4;
  // color is the accent color of the select.
  optional .x.color.pb.Color color = 5;
  // inline_size is the inline size of the select in pixels.
  optional double inline_size = 6;
  // options is the set of selectable states.
  repeated StateMapping options = 7;
  // disabled indicates whether the select rejects interaction.
  bool disabled = 8;
  // control is the control state display configuration.
  optional ControlStateConfig control = 9;
}
message NodeConfigSetpointPayload {
  // label is the symbol's label configuration.
  optional LabelConfig label = 1;
  // orientation is the orientation of the symbol's primitive within the diagram.
  optional .x.spatial.pb.OuterLocation orientation = 2;
  // size is the rendered size preset of the setpoint.
  optional ComponentSize size = 3;
  // state_channel is the channel whose value displays as the current setpoint.
  optional uint32 state_channel = 4;
  // command_channel is the channel submitted setpoints are written to.
  optional uint32 command_channel = 5;
  // dimensions is the rendered size of the setpoint in pixels.
  optional .x.spatial.pb.Dimensions dimensions = 6;
  // color is the accent color of the setpoint.
  optional .x.color.pb.Color color = 7;
  // units is the unit suffix displayed after the value.
  string units = 8;
  // disabled indicates whether the setpoint rejects interaction.
  bool disabled = 9;
  // control is the control state display configuration.
  optional ControlStateConfig control = 10;
}
message NodeConfigStateIndicatorPayload {
  // label is the symbol's label configuration.
  optional LabelConfig label = 1;
  // orientation is the orientation of the symbol's primitive within the diagram.
  optional .x.spatial.pb.OuterLocation orientation = 2;
  // channel is the channel whose value selects the displayed state.
  optional uint32 channel = 3;
  // color is the fallback color when no state matches.
  optional .x.color.pb.Color color = 4;
  // inline_size is the inline size of the indicator in pixels.
  optional double inline_size = 5;
  // options is the set of displayable states.
  repeated StateMapping options = 6;
}
message NodeConfigTextBoxPayload {
  // label is the symbol's label configuration.
  optional LabelConfig label = 1;
  // orientation is the orientation of the symbol's primitive within the diagram.
  optional .x.spatial.pb.OuterLocation orientation = 2;
  // color is the text color.
  optional .x.color.pb.Color color = 3;
  // width is the rendered width of the text box in pixels.
  optional double width = 4;
  // align is the alignment of the text within the box.
  optional FlexAlignment align = 5;
  // auto_fit indicates whether the box resizes to fit its content.
  bool auto_fit = 6;
  // level is the typography level of the text.
  optional .x.text.pb.Level level = 7;
  // value is the text content of the box.
  string value = 8;
}
message NodeConfigValuePayload {
  // label is the symbol's label configuration.
  optional LabelConfig label = 1;
  // orientation is the orientation of the symbol's primitive within the diagram.
  optional .x.spatial.pb.OuterLocation orientation = 2;
  // position is the offset of the value contents within the symbol.
  optional .x.spatial.pb.XY position = 3;
  // color is the background color of the value.
  optional .x.color.pb.Color color = 4;
  // text_color is the color of the displayed text.
  optional .x.color.pb.Color text_color = 5;
  // tooltip is the list of tooltip lines shown on hover.
  repeated string tooltip = 6;
  // redline is the bounds-to-gradient mapping applied to the background.
  optional Redline redline = 7;
  // units is the unit suffix displayed after the value.
  string units = 8;
  // inline_size is the inline size of the value in pixels.
  optional double inline_size = 9;
  // channel is the channel whose value the symbol displays.
  optional uint32 channel = 10;
  // rolling_average is the sample window for rolling-average smoothing.
  optional int32 rolling_average = 11;
  // level is the typography level of the displayed value.
  optional .x.text.pb.Level level = 12;
  // precision is the number of decimal places shown.
  optional double precision = 13;
  // staleness_timeout is the duration in seconds after which the value is considered
  // stale.
  optional double staleness_timeout = 14;
  // staleness_color is the color applied when the value is stale.
  optional .x.color.pb.Color staleness_color = 15;
  // min_width is the minimum rendered width of the value in pixels.
  optional double min_width = 16;
  // notation is the numeric notation used to format the value.
  optional .x.notation.pb.Notation notation = 17;
  // location is the anchor of the value within the symbol.
  optional .x.spatial.pb.LocationXY location = 18;
  // use_width_for_background indicates whether the background spans the full configured
  // width.
  bool use_width_for_background = 19;
  // value_background_shift is the offset applied to the value background.
  optional .x.spatial.pb.XY value_background_shift = 20;
  // value_background_over_scan is the extra padding applied around the value background.
  optional .x.spatial.pb.XY value_background_over_scan = 21;
}
message NodeConfigSolenoidValvePayload {
  // label is the symbol's label configuration.
  optional LabelConfig label = 1;
  // orientation is the orientation of the symbol's primitive within the diagram.
  optional .x.spatial.pb.OuterLocation orientation = 2;
  // state_channel is the channel whose value drives the symbol's active state.
  optional uint32 state_channel = 3;
  // command_channel is the channel actuation commands are written to.
  optional uint32 command_channel = 4;
  // control is the control state display configuration.
  optional ControlStateConfig control = 5;
  // on_click_delay is the debounce delay applied to clicks, in milliseconds.
  double on_click_delay = 6;
  // color is the stroke color of the symbol.
  optional .x.color.pb.Color color = 7;
  // scale is the rendered scale multiplier of the symbol.
  optional double scale = 8;
  // normally_open indicates whether the valve is open when unpowered.
  bool normally_open = 9;
}
message NodeConfigCylinderPayload {
  // label is the symbol's label configuration.
  optional LabelConfig label = 1;
  // orientation is the orientation of the symbol's primitive within the diagram.
  optional .x.spatial.pb.OuterLocation orientation = 2;
  // dimensions is the rendered size of the cylinder in pixels.
  optional .x.spatial.pb.Dimensions dimensions = 3;
  // border_radius is the corner radius of the cylinder.
  optional .x.border.pb.Radius border_radius = 4;
  // color is the border color of the cylinder.
  optional .x.color.pb.Color color = 5;
  // background_color is the fill color of the cylinder.
  optional .x.color.pb.Color background_color = 6;
}
message NodeConfigTankPayload {
  // label is the symbol's label configuration.
  optional LabelConfig label = 1;
  // orientation is the orientation of the symbol's primitive within the diagram.
  optional .x.spatial.pb.OuterLocation orientation = 2;
  // color is the border color of the tank.
  optional .x.color.pb.Color color = 3;
  // background_color is the fill color of the tank.
  optional .x.color.pb.Color background_color = 4;
  // dimensions is the rendered size of the tank in pixels.
  optional .x.spatial.pb.Dimensions dimensions = 5;
  // border_radius is the corner radius of the tank.
  optional .x.border.pb.Radius border_radius = 6;
}
message NodeConfigCustomActuatorPayload {
  // label is the symbol's label configuration.
  optional LabelConfig label = 1;
  // orientation is the orientation of the symbol's primitive within the diagram.
  optional .x.spatial.pb.OuterLocation orientation = 2;
  // state_channel is the channel whose value drives the symbol's active state.
  optional uint32 state_channel = 3;
  // command_channel is the channel actuation commands are written to.
  optional uint32 command_channel = 4;
  // control is the control state display configuration.
  optional ControlStateConfig control = 5;
  // on_click_delay is the debounce delay applied to clicks, in milliseconds.
  double on_click_delay = 6;
  // spec_key is the key of the custom symbol spec this instance renders.
  string spec_key = 7;
  // color is the stroke color of the symbol.
  optional .x.color.pb.Color color = 8;
  // scale is the rendered scale multiplier of the symbol.
  optional double scale = 9;
  // state_overrides contains per-instance overrides of the spec's visual states. Each
  // entry mirrors the symbol service's State shape; the wire format stores it opaquely,
  // consistent with how the symbol service stores specs.
  repeated google.protobuf.Struct state_overrides = 10;
}
message NodeConfigCustomStaticPayload {
  // label is the symbol's label configuration.
  optional LabelConfig label = 1;
  // orientation is the orientation of the symbol's primitive within the diagram.
  optional .x.spatial.pb.OuterLocation orientation = 2;
  // spec_key is the key of the custom symbol spec this instance renders.
  string spec_key = 3;
  // color is the stroke color of the symbol.
  optional .x.color.pb.Color color = 4;
  // scale is the rendered scale multiplier of the symbol.
  optional double scale = 5;
  // state_overrides contains per-instance overrides of the spec's visual states. Each
  // entry mirrors the symbol service's State shape; the wire format stores it opaquely,
  // consistent with how the symbol service stores specs.
  repeated google.protobuf.Struct state_overrides = 6;
}

// EdgeConfig is the per-edge configuration stored in the schematic configs map. The
// variant selects the visual style of the connection.
message EdgeConfig {
  oneof variant {
    SegmentedEdgeConfig pipe = 1;
    SegmentedEdgeConfig electric = 2;
    SegmentedEdgeConfig secondary = 3;
    SegmentedEdgeConfig jacketed = 4;
    SegmentedEdgeConfig hydraulic = 5;
    SegmentedEdgeConfig pneumatic = 6;
    SegmentedEdgeConfig data = 7;
  }
}

// NodeConfig is the per-node configuration stored in the schematic configs map. The
// variant selects the symbol rendered for the node and the fields that accompany it.
message NodeConfig {
  oneof variant {
    StaticSymbolConfig cap = 1;
    StaticSymbolConfig filter = 2;
    StaticSymbolConfig flow_straightener = 3;
    StaticSymbolConfig heater_element = 4;
    StaticSymbolConfig iso_cap = 5;
    StaticSymbolConfig iso_filter = 6;
    StaticSymbolConfig nozzle = 7;
    StaticSymbolConfig orifice = 8;
    StaticSymbolConfig orifice_plate = 9;
    StaticSymbolConfig strainer = 10;
    StaticSymbolConfig strainer_cone = 11;
    ToggleSymbolConfig thruster = 12;
    StaticSymbolConfig vent = 13;
    StaticSymbolConfig flowmeter_general = 14;
    StaticSymbolConfig flowmeter_electromagnetic = 15;
    StaticSymbolConfig flowmeter_variable_area = 16;
    StaticSymbolConfig flowmeter_coriolis = 17;
    StaticSymbolConfig flowmeter_nozzle = 18;
    StaticSymbolConfig flowmeter_venturi = 19;
    StaticSymbolConfig flowmeter_ring_piston = 20;
    StaticSymbolConfig flowmeter_positive_displacement = 21;
    StaticSymbolConfig flowmeter_turbine = 22;
    StaticSymbolConfig flowmeter_pulse = 23;
    StaticSymbolConfig flowmeter_float_sensor = 24;
    StaticSymbolConfig flowmeter_orifice = 25;
    NodeConfigBoxPayload box = 26;
    NodeConfigButtonPayload button = 27;
    NodeConfigCirclePayload circle = 28;
    NodeConfigGaugePayload gauge = 29;
    NodeConfigInputPayload input = 30;
    NodeConfigLightPayload light = 31;
    NodeConfigOffPageReferencePayload off_page_reference = 32;
    NodeConfigPolygonPayload polygon = 33;
    NodeConfigSelectPayload select = 34;
    NodeConfigSetpointPayload setpoint = 35;
    NodeConfigStateIndicatorPayload state_indicator = 36;
    ToggleSymbolConfig switch = 37;
    NodeConfigTextBoxPayload text_box = 38;
    NodeConfigValuePayload value = 39;
    ToggleSymbolConfig agitator = 40;
    ToggleSymbolConfig cross_beam_agitator = 41;
    ToggleSymbolConfig flat_blade_agitator = 42;
    StaticSymbolConfig heat_exchanger_general = 43;
    StaticSymbolConfig heat_exchanger_m = 44;
    StaticSymbolConfig heat_exchanger_straight_tube = 45;
    ToggleSymbolConfig helical_agitator = 46;
    ToggleSymbolConfig paddle_agitator = 47;
    ToggleSymbolConfig propeller_agitator = 48;
    ToggleSymbolConfig rotary_mixer = 49;
    StaticSymbolConfig static_mixer = 50;
    ToggleSymbolConfig cavity_pump = 51;
    ToggleSymbolConfig centrifugal_compressor = 52;
    ToggleSymbolConfig compressor = 53;
    ToggleSymbolConfig diaphragm_pump = 54;
    ToggleSymbolConfig ejection_pump = 55;
    ToggleSymbolConfig ejector_compressor = 56;
    ToggleSymbolConfig liquid_ring_compressor = 57;
    ToggleSymbolConfig piston_pump = 58;
    ToggleSymbolConfig pump = 59;
    ToggleSymbolConfig roller_vane_compressor = 60;
    ToggleSymbolConfig screw_pump = 61;
    ToggleSymbolConfig turbo_compressor = 62;
    ToggleSymbolConfig vacuum_pump = 63;
    StaticSymbolConfig burst_disc = 64;
    StaticSymbolConfig flame_arrestor = 65;
    StaticSymbolConfig flame_arrestor_detonation = 66;
    StaticSymbolConfig flame_arrestor_explosion = 67;
    StaticSymbolConfig flame_arrestor_fire_res = 68;
    StaticSymbolConfig flame_arrestor_fire_res_detonation = 69;
    StaticSymbolConfig iso_burst_disc = 70;
    ToggleSymbolConfig angled_valve = 71;
    DummyToggleSymbolConfig angled_relief_valve = 72;
    DummyToggleSymbolConfig angled_spring_loaded_relief_valve = 73;
    ToggleSymbolConfig ball_valve = 74;
    DummyToggleSymbolConfig breather_valve = 75;
    ToggleSymbolConfig butterfly_valve_one = 76;
    ToggleSymbolConfig butterfly_valve_two = 77;
    StaticSymbolConfig check_valve = 78;
    StaticSymbolConfig check_valve_with_arrow = 79;
    StaticSymbolConfig electric_regulator = 80;
    StaticSymbolConfig electric_regulator_motorized = 81;
    ToggleSymbolConfig four_way_valve = 82;
    ToggleSymbolConfig gate_valve = 83;
    StaticSymbolConfig iso_check_valve = 84;
    DummyToggleSymbolConfig manual_valve = 85;
    DummyToggleSymbolConfig needle_valve = 86;
    StaticSymbolConfig regulator = 87;
    StaticSymbolConfig regulator_manual = 88;
    DummyToggleSymbolConfig relief_valve = 89;
    NodeConfigSolenoidValvePayload solenoid_valve = 90;
    DummyToggleSymbolConfig spring_loaded_relief_valve = 91;
    ToggleSymbolConfig three_way_valve = 92;
    ToggleSymbolConfig three_way_ball_valve = 93;
    ToggleSymbolConfig valve = 94;
    StaticSymbolConfig cross_junction = 95;
    NodeConfigCylinderPayload cylinder = 96;
    NodeConfigTankPayload tank = 97;
    StaticSymbolConfig t_junction = 98;
    NodeConfigCustomActuatorPayload custom_actuator = 99;
    NodeConfigCustomStaticPayload custom_static = 100;
  }
}

// ElementConfig is the per-element configuration stored in the schematic configs map: a
// node config or an edge config, discriminated by variant.
message ElementConfig {
  oneof variant {
    StaticSymbolConfig cap = 1;
    StaticSymbolConfig filter = 2;
    StaticSymbolConfig flow_straightener = 3;
    StaticSymbolConfig heater_element = 4;
    StaticSymbolConfig iso_cap = 5;
    StaticSymbolConfig iso_filter = 6;
    StaticSymbolConfig nozzle = 7;
    StaticSymbolConfig orifice = 8;
    StaticSymbolConfig orifice_plate = 9;
    StaticSymbolConfig strainer = 10;
    StaticSymbolConfig strainer_cone = 11;
    ToggleSymbolConfig thruster = 12;
    StaticSymbolConfig vent = 13;
    StaticSymbolConfig flowmeter_general = 14;
    StaticSymbolConfig flowmeter_electromagnetic = 15;
    StaticSymbolConfig flowmeter_variable_area = 16;
    StaticSymbolConfig flowmeter_coriolis = 17;
    StaticSymbolConfig flowmeter_nozzle = 18;
    StaticSymbolConfig flowmeter_venturi = 19;
    StaticSymbolConfig flowmeter_ring_piston = 20;
    StaticSymbolConfig flowmeter_positive_displacement = 21;
    StaticSymbolConfig flowmeter_turbine = 22;
    StaticSymbolConfig flowmeter_pulse = 23;
    StaticSymbolConfig flowmeter_float_sensor = 24;
    StaticSymbolConfig flowmeter_orifice = 25;
    NodeConfigBoxPayload box = 26;
    NodeConfigButtonPayload button = 27;
    NodeConfigCirclePayload circle = 28;
    NodeConfigGaugePayload gauge = 29;
    NodeConfigInputPayload input = 30;
    NodeConfigLightPayload light = 31;
    NodeConfigOffPageReferencePayload off_page_reference = 32;
    NodeConfigPolygonPayload polygon = 33;
    NodeConfigSelectPayload select = 34;
    NodeConfigSetpointPayload setpoint = 35;
    NodeConfigStateIndicatorPayload state_indicator = 36;
    ToggleSymbolConfig switch = 37;
    NodeConfigTextBoxPayload text_box = 38;
    NodeConfigValuePayload value = 39;
    ToggleSymbolConfig agitator = 40;
    ToggleSymbolConfig cross_beam_agitator = 41;
    ToggleSymbolConfig flat_blade_agitator = 42;
    StaticSymbolConfig heat_exchanger_general = 43;
    StaticSymbolConfig heat_exchanger_m = 44;
    StaticSymbolConfig heat_exchanger_straight_tube = 45;
    ToggleSymbolConfig helical_agitator = 46;
    ToggleSymbolConfig paddle_agitator = 47;
    ToggleSymbolConfig propeller_agitator = 48;
    ToggleSymbolConfig rotary_mixer = 49;
    StaticSymbolConfig static_mixer = 50;
    ToggleSymbolConfig cavity_pump = 51;
    ToggleSymbolConfig centrifugal_compressor = 52;
    ToggleSymbolConfig compressor = 53;
    ToggleSymbolConfig diaphragm_pump = 54;
    ToggleSymbolConfig ejection_pump = 55;
    ToggleSymbolConfig ejector_compressor = 56;
    ToggleSymbolConfig liquid_ring_compressor = 57;
    ToggleSymbolConfig piston_pump = 58;
    ToggleSymbolConfig pump = 59;
    ToggleSymbolConfig roller_vane_compressor = 60;
    ToggleSymbolConfig screw_pump = 61;
    ToggleSymbolConfig turbo_compressor = 62;
    ToggleSymbolConfig vacuum_pump = 63;
    StaticSymbolConfig burst_disc = 64;
    StaticSymbolConfig flame_arrestor = 65;
    StaticSymbolConfig flame_arrestor_detonation = 66;
    StaticSymbolConfig flame_arrestor_explosion = 67;
    StaticSymbolConfig flame_arrestor_fire_res = 68;
    StaticSymbolConfig flame_arrestor_fire_res_detonation = 69;
    StaticSymbolConfig iso_burst_disc = 70;
    ToggleSymbolConfig angled_valve = 71;
    DummyToggleSymbolConfig angled_relief_valve = 72;
    DummyToggleSymbolConfig angled_spring_loaded_relief_valve = 73;
    ToggleSymbolConfig ball_valve = 74;
    DummyToggleSymbolConfig breather_valve = 75;
    ToggleSymbolConfig butterfly_valve_one = 76;
    ToggleSymbolConfig butterfly_valve_two = 77;
    StaticSymbolConfig check_valve = 78;
    StaticSymbolConfig check_valve_with_arrow = 79;
    StaticSymbolConfig electric_regulator = 80;
    StaticSymbolConfig electric_regulator_motorized = 81;
    ToggleSymbolConfig four_way_valve = 82;
    ToggleSymbolConfig gate_valve = 83;
    StaticSymbolConfig iso_check_valve = 84;
    DummyToggleSymbolConfig manual_valve = 85;
    DummyToggleSymbolConfig needle_valve = 86;
    StaticSymbolConfig regulator = 87;
    StaticSymbolConfig regulator_manual = 88;
    DummyToggleSymbolConfig relief_valve = 89;
    NodeConfigSolenoidValvePayload solenoid_valve = 90;
    DummyToggleSymbolConfig spring_loaded_relief_valve = 91;
    ToggleSymbolConfig three_way_valve = 92;
    ToggleSymbolConfig three_way_ball_valve = 93;
    ToggleSymbolConfig valve = 94;
    StaticSymbolConfig cross_junction = 95;
    NodeConfigCylinderPayload cylinder = 96;
    NodeConfigTankPayload tank = 97;
    StaticSymbolConfig t_junction = 98;
    NodeConfigCustomActuatorPayload custom_actuator = 99;
    NodeConfigCustomStaticPayload custom_static = 100;
    SegmentedEdgeConfig pipe = 101;
    SegmentedEdgeConfig electric = 102;
    SegmentedEdgeConfig secondary = 103;
    SegmentedEdgeConfig jacketed = 104;
    SegmentedEdgeConfig hydraulic = 105;
    SegmentedEdgeConfig pneumatic = 106;
    SegmentedEdgeConfig data = 107;
  }
}
