// Copyright 2026 Synnax Labs, Inc.
//
// Use of this software is governed by the Business Source License included in the file
// licenses/BSL.txt.
//
// As of the Change Date specified in that file, in accordance with the Business Source
// License, use of this software will be governed by the Apache License, Version 2.0,
// included in the file licenses/APL.txt.

// Code generated by oracle. DO NOT EDIT.

package kv

import (
	"context"
	"encoding/binary"

	"github.com/synnaxlabs/x/gorp"
)

var _ = binary.BigEndian

const (
	PairFieldRange = 0
	PairFieldKey   = 1
	PairFieldValue = 2
	PairFieldCount = 3
)

type pairCodec struct{}

func (pairCodec) Marshal(
	_ context.Context,
	s Pair,
) ([]byte, error) {
	buf := make([]byte, 0, 80)
	buf = append(buf, s.Range[:]...)
	buf = binary.BigEndian.AppendUint32(buf, uint32(len(s.Key)))
	buf = append(buf, s.Key...)
	buf = binary.BigEndian.AppendUint32(buf, uint32(len(s.Value)))
	buf = append(buf, s.Value...)
	return buf, nil
}

func (pairCodec) Unmarshal(
	_ context.Context,
	data []byte,
) (Pair, error) {
	var r Pair
	copy(r.Range[:], data[:16])
	data = data[16:]
	{
		_n := binary.BigEndian.Uint32(data[:4])
		data = data[4:]
		r.Key = string(data[:_n])
		data = data[_n:]
	}
	{
		_n := binary.BigEndian.Uint32(data[:4])
		data = data[4:]
		r.Value = string(data[:_n])
		data = data[_n:]
	}
	return r, nil
}

var PairCodec gorp.Codec[Pair] = pairCodec{}
